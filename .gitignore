.DS_Store
.build/
node_modules/
*.log
.env
.env.local

# Playwright MCP scratch
.playwright-mcp/

# Verification screenshots dropped at repo root
/*.png

# Design-system build scratch state
.funnel-state.json

# respcheck's "have I already measured this build?" state — a per-machine
# record of the last clean sweep, not a fact about the product
.respcheck-state.json
