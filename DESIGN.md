# TalentNext — DESIGN.md

**The baseline for all four portals.** Read this in full before any UI change on any of them.
Every value below is copied from the layer that states it, with the `file:line`, so it can be
re-derived and never retyped from memory. If a rule here and a reference screenshot disagree,
the rule wins unless Maryam says otherwise. **A new rule she gives is written HERE first, as one
line, in the same session — then implemented.** The argument behind each line is in
`docs/HISTORY.md` under the same § number; the primary source is the comment above the rule in
its `hifi/build/` layer.

## 1. The four portals

| Portal | File | On the design system | Rebuild |
|---|---|---|---|
| Candidate | `hifi/` (compiled from `hifi/build/`) | source of it | `cd hifi/build && python3 build.py` |
| Cohort Leader | `hifi/` — same renderer, `S.portal='leader'`, `lead*.js` | source of it | same |
| Talent Agent | `tn-agent-portal.html`, hand-written | yes — links `design-system/talentnext-ds.css` + `.js` | `cd design-system && python3 build-ds.py` |
| Super Admin | `tn-admin-portal.html`, hand-written | yes — links `design-system/talentnext-ds.css` + `.js` | `cd design-system && python3 build-ds.py` |

Collisions: "candidate portal" = `hifi/` unless "wireframe"/"early prototype" is said; "agent"
in `hifi/` is somebody the candidate books, in `tn-agent-portal.html` it is the signed-in user.

## 2. Tokens

**Colour** — `hifi/build/01-foundation.css:145-164, 309-315`, `10-lattice.css:43`, `12-tone.css:13-14`, `70-ainative.css:169-262`

| Token | Value | Role |
|---|---|---|
| `--accent` | `#f9423a` | the brand red — fills, marks, the selected rail row. Solid, not a gradient (Maryam 9 Sep 2026, was #bd2a2a → #ff3733) |
| `--accent-2` | `#d63932` | one step deeper — the black card's haze, hot strokes (re-derived x0.86 of the accent, 9 Sep 2026) |
| `--accent-text` | `#c2332d` | the accent as INK (5.5:1 on white, AA) — never `--accent` for words except the stated exceptions (x0.78, 9 Sep 2026) |
| `--text-primary` / `-secondary` / `-helper` | `#111111` / `#828282` / `#666563` | body; every description and eyebrow (`-secondary` #828282, Maryam 7 Sep 2026, was #525250); floor tier only (timestamps, legal, axes) |
| `--rule` | `#efefef` | THE hairline — every stroke, border, outline, separator, card edge and table line, one value, every portal (Maryam 7 Sep 2026, was #e9e9e9). The control-edge tokens `--border-subtle-02` #c7c6c3 / `--border-strong-01` #8e8d8a stay darker so an input is findable |
| `--surface-2` / `--surface-3` / `--brand-tint` | `#fcfcfc` | the tinted panel / the page margin / the quiet grey — all the grey backgrounds, one value #fcfcfc (Maryam 7 Sep 2026, were #f7f7f7 / #fbfbfb / #f7f7f7) |
| `--gray-100` | `#111111` | the black card's ground; on it the inks are `--on-dark` / `--on-dark-2` |
| `--ai-1 / --ai-2 / --ai-3` | `#f9423a / #dd8080 / #f9423a` | Tal's text ramp (label, stars, `<b>` in summaries); accent stops track `--accent`, light middle #dd8080 unchanged (9 Sep 2026) |
| `--ai-line-2`, `--ai-line-alpha` | `#cce7cb`, 70% | the ask dock's border and comet — the LINE ramp, a different middle from the text ramp |
| `--star` | `#ffcb05` | the rating star, always, never the accent |
| `--cal-iv` / `--cal-reiv` / `--cal-open` | `#cce7cb` / `#e0c0ff` / `#e9dedc` | calendar slot fills — first interview / re-interview / open (a SET, like the journey's states) |

**Type** — `63-typography.css:140-236`; face `11-type.css:294` (**Plus Jakarta Sans**)

| Role | size / lh | Role | size / lh |
|---|---|---|---|
| `--t-display` | 34 / 40 — hero NUMERALS only | `--t-body` / `--t-compact` | 13.5 / 22 · 13.5 / 19 |
| `--t-h1` (≥900: 28) | 24 / 30 | `--t-label` · `--t-desc` | 12.5 / 17 · **13.5 / auto** |
| `--t-h2` | 20 / 26 | `--t-eyebrow` / `--t-caption` | 11.5 / 16 |
| `--t-h3` | 17 / 23 | `--t-sec` | **16 / 22 — every section heading, every width, every ground** |
| `--t-h4` | 14 / 19 | `--t-sec-lg` | 18 / 24 — one reader, `.dc-t` |

Weights: `--t-w-book` **400**, `--t-w-strong` **500**. Nothing else is loaded for a role.
§63 is the last layer: no layer after it may set a size, a weight, a `text-transform` or a text
colour. Take a role; if none fits, state the exception inside §63 §7.

**Space** — `01-foundation.css:389-390, 414, 435, 438`; `10-lattice.css:46`

`--s01..--s12` = 2 / 4 / 8 / 12 / 16 / 24 / 32 / 40 / 48 / 64 / 80 / 96. Section padding `--s06`
top and bottom (48 between sections). `--sec-h-gap` 12 (heading → its content). `--sec-desc-gap` 8
(heading → its own description). `--radius` **0px**. `--label-col` 184px (the desktop label column).

## 3. Standing rules — the checklist

Type and case
- Two weights, 400 and 500. Never 600+. "No bold" means Medium. The ONE stated exception is the head band label "Summary by Tal" at 600 (§63 §10, off Figma 871:6425). (§63, memory 4 Sep)
- Nothing is set in capitals except the card wordmarks. Words go in the markup in sentence case. (§63 §2)
- Buttons are Title Case, every word, by `text-transform:capitalize` on `.btn` — not by editing strings. (§63 §4b)
- Section heading is `--t-sec` 16px everywhere; `aiHead`'s title shares the token. (§63 §8b)
- A FIGURE-CARD TITLE IS BLACK, not the grey eyebrow: `.stats .l` / `.facts .l` / `.pf-facts .pf-l` take `--text-primary` (keeping the eyebrow size/weight); the sub-line `.d` under it stays `--text-secondary`. On a dark card the §6a ink rule still wins (`--on-dark`). All portals via the DS. (§63, Maryam 9 Sep 2026: "the four card titles should be black always ... revise the 4 cards structure everywhere")
- Display size is hero numerals only; a level NAME is h2. (§63)
- A DESCRIPTION (`--t-desc`, every `.t-desc` / `.u-desc` and the assignments in §63 §3) is **13.5px with an AUTO line height** (`line-height:normal`, the face's own leading), in `--text-secondary`. It no longer shares the label's 12.5 / 17; a description is read, a label is scanned. The one role in the scale without a stated leading — do not give it a px value. (§63 §1, Maryam 8 Sep 2026, was 12.5 / 17)
- A DESCRIPTION IS `--text-secondary` **#828282 EVERYWHERE, and it reads the token — never a hardcoded grey** (Maryam 9 Sep 2026: "i want the desc color to be #828282, i want it everywhere"). This is a global rule, not a per-screen fix: when it comes up, SWEEP every layer for description/supporting greys set as a hex and route them to `var(--text-secondary)`. Stragglers cleaned so far: `.qa-b > span` (#717171, quick-action descriptions), `.jrn-i` upcoming journey steps (#515151), its onboarding icon `.ob-jr-ic`, `.rec-f` (#414141). The floor tier `--text-helper` #666563 (timestamps, legal, chart axes, captions) and state colours (the journey `.done` green / `.on` accent, the star) are NOT descriptions and stay. (§63, all portals via the DS)

Colour and line
- One hairline for every stroke/border/outline, `--rule` **#efefef** (Maryam 7 Sep 2026). Panels `--surface-2`, page margin `--surface-3`. Control edges (input/select/checkbox) keep the darker `--border-subtle-02`/`--border-strong-01` — a field must stay findable. (§10, §12, §18)
- Never hardcode a hex; read the token. State colours are a SET and do not follow the accent: the journey's green/orange/grey, the rating star. (§63 §14, re-hue notes)
- The accent as ink is `--accent-text`; `--accent` on words is a stated exception (the selected rail label, `.jrn-pill`). (§63 §41)
- An UNSELECTED side-nav module (icon AND name) is `#828282` grey — the rail de-emphasises everything you are not on; the SELECTED module is `--accent` (icon, label and the 3px bar). Set on `.sn-item` in §01/§10 and `.sn-item.sn-sub` in §63; the `.on` state keeps `--accent`. All portals via the DS. (Maryam 7 Sep 2026, was #dfdfdf → #b1b1b1 → #828282)
- A field's line is `--rule` at rest and `#FF3733` at 2px on focus. (§12)
- The side-nav divider is ONE full-height line, drawn once by `.app::after` (≥900), never by the app-bar cell AND the drawer both — two stacked 1px borders read as a double rule on fractional-DPR screens. `.nav-t` and `.sidenav` carry no `border-right`; the line sits at `--rail-w-nav` collapsed / `--drawer-w` open and slides with the rail. It is SUPPRESSED (`content:none`) on the full-bleed scenes that have no rail — account creation (`.app:has(> .auth-card)`, which also covers the onboarding split step), the Tal onboarding intro (`.ob-open`) and the Next-in-Leadership run-up (`.nil`) — else it draws a stray line down an empty page. (§14 §1, §34, Maryam 7 Sep 2026; scene exception 9 Sep 2026)
- All cards and containers have a white fill; grey only for intentional tracks and hover. (memory, admin)
- The cohort leader dashboard's "Your upcoming calls" cards (`.lcal`) other than the black one (`.lcal-next`): a `--surface-2` **#fcfcfc** grey fill inside a `--rule` **#efefef** 1px border (Maryam 9 Sep 2026, iterated the same day: no-border+#efefef, then #fcfcfc fill, then #efefef border). The black cell keeps `border-color:transparent` so all three share the grid track's 1px height (§113.3). Both read the token, never the hex — a deliberate exception to the "cards are a white fill" rule, by instruction. The card MARKUP: the "Cohort N call" title leads the header row top-left (the cohort-number `.gcard-art` badge is gone), the date is top-right, and the sub-line is the track + week only (the "N candidates" count is removed). The section has no description. (§113/lead.js, Maryam 9 Sep 2026)
- The cohort leader dashboard scrolls plainly — the sticky figure-band "position indicator" is removed (Maryam 9 Sep 2026: "hide the fix tabs interaction, go with the generic scroll just like other pages"). The four figures are plain static `.stat` cells (not `data-jump` buttons), the `.lead-bar`/`.lead-tabs` sticky tab strip is gone, and `leadStick` + its scroll/resize/click listeners are removed. The four sections keep their ids for the counts. (§113/lead.js, Maryam 9 Sep 2026)
- `gcard(kind,tag,title,sub,go,art,at)` draws NO left icon unless a real cover (`art.src`) is passed — the old `.cardrow-ic` `GC_IC[kind]` fallback mark is gone (Maryam 9 Sep 2026: "remove the left side cohort icons"). Its one live caller, the leader dashboard's cohort rows, passes `null` art, so those rows are text + arrow. (views.js, Maryam 9 Sep 2026)
- The leader's black COHORT CALL CARD (`leadCallCard`, on the Sessions and Cohort pages): header reads "Upcoming Cohort Session" (not "Cohort N call"); `crow` draws NO left mark (`leadCall`'s `who` has no `i`, and `crow` omits `.crow-ph` when a record has neither initials nor photo); the action is `crow`'s gated primary Join — the disabled grey "Join call" while the call is >1 min away — not "Generate the brief". (§113/lead.js/views.js, Maryam 9 Sep 2026)
- A CANDIDATE-TURNED-COHORT-LEADER LEADS ONE COHORT AT A TIME; a MODERATOR (a cohort leader an admin added, `u.addedBy` set) may lead multiple (Maryam 14 Sep 2026). On the hifi cohort leader portal (the persona Priya is a candidate-leader) this is "one running cohort, keep previous led cohorts": `LEAD_COHORTS` holds only the ONE active cohort (Cohort 41, `status:'active'`), so every current-work surface (identity line, dashboard figures, attention queue, calls diary, "Your cohorts", chat) reads as one cohort automatically; completed/past led cohorts live in `LEAD_PAST` (Cohort 33, `status:'completed'`), reachable via `lcoOf`/`LEAD_ALL()` for the Evaluations demo, the Course Reports selector and a "Past cohorts" section on the Cohorts page. Multi-cohort concurrency (a second live cohort, cross-cohort aggregation, "which cohort needs me most") is hidden, not deleted — the machinery survives for a moderator build. (lead.js/lead2.js/lead5.js, Maryam 14 Sep 2026)
- On the SUPER ADMIN portal `u.addedBy` is the moderator marker (Maryam 14 Sep 2026, "addedBy = moderator; cap the rest"): a leader WITH `addedBy` is a moderator (admin-added, may lead multiple); a candidate+leader WITHOUT it is a candidate-turned-leader capped at ONE running cohort. The Users dashboard Cohort-Leader/Moderator split keys on `addedBy` (not the older `u.volunteer`); the Users → Cohort Leader role tab carries a "Leads" column showing how many cohorts each leads (candidate-leader shows 1, moderator shows its count). Seed: `u4` Maryam is the candidate-leader (no `addedBy`, `volunteer:true`, leads one — Cohort 2); `u40` Bruno is the moderator (`addedBy` set, `volunteer:false`, leads two — Cohort 1 + Cohort 4); `COHORTS.leader` strings aligned to match. (tn-admin-portal.html, Maryam 14 Sep 2026)
- The leader COHORT CARD (`cohortCard`, Cohorts page): no image placeholder / cohort-number disc (`.cco-art` removed); the detail line is "Level E3 · Week N of 13" (the level via `c.level`, not the intent); the candidate count is an inline pill (`.cco-pill-in`) under that line, not a corner badge. A COMPLETED past cohort (`LEAD_PAST`, `status:'completed'`) draws a quieter `pastCohortCard` — level, "Completed", candidate count, no pace ring / flags / next-call. (§92/lead2.js, Maryam 9–14 Sep 2026)
- The leader COHORT page (`V.leadCohort`) has FOUR tabs: Candidate Progress (default), Discussion, Ranking, Members (`S.ldrCTab` default `progress`). The Candidate Progress table is the first tab's content, not a standalone section above the strip. (lead2.js, Maryam 9 Sep 2026)
- The CANDIDATE PROGRESS table (leader Cohort page + Course Reports) carries a "Course" column (2nd, `lcourse(c)` = `courseOf(c.level)`) and shows Attempts as a WHOLE number (`Math.round(m.att)`, not `.toFixed(1)` — "attempts are 1 or 2, not 1.1"). (lead2.js, Maryam 9 Sep 2026)
- The leader 90-DAY SUMMARY / evaluation screen (`V.leadSum`): the header action reads "View Candidates Progress" (not "Their full record"); the "What the 90 days produced" figures have no "read-only" caption and no foot divider (`sec-noline`), flowing into a "Your notes on {first}" section — the leader's own `S.ldrNotes` for the candidate, drawn READ-ONLY (no edit/delete chips) — then "Your recommendation" (no "next agent reads" caption); "Send Recommendation" carries no tick icon. (lead3.js, Maryam 9 Sep 2026)
- CANDIDATES take DIFFERENT courses: `mcourse(m)` / `mlevel(m)` spread the four catalog courses (`COURSE_NAME`) across members by a stable name-hash (authored variety), so the Candidates Progress "Course" column varies down the table. The leader cohort card's third row is "Total Courses" = distinct courses in the cohort (was "Assessment"). The section/tab is "Candidates Progress" (plural). (lead.js/lead2.js, Maryam 9 Sep 2026)
- The leader MEMBER page (`V.leadMember`): figure sub-lines are plain descriptors, NOT insights ("of the course", "course average", "per chapter" — no "-N against pace" / "going round twice"); Attempts is a whole number; below the figures a "Course" card shows the candidate's course NAME + DESC (`mcourse`/`courseDesc`, ENROL_COURSE for E3/E4, authored for E1/E2 — no course photo asset in this build), left-aligned. (lead2.js, Maryam 9 Sep 2026)
- The cohort leader PROFILE has a Specialities `.skl` chip cloud on BOTH the General and Public tabs (`LEADER.specs`). (lead4.js, Maryam 9 Sep 2026)
- The Messages DM list row (`.ldr-dm-t`) is the SAME height as the thread header (`.ldr-dm-h`) — both `--s04 --s05` padding (~61px). (§36, Maryam 9 Sep 2026)
- THE NOTIFICATION PANEL (`.notif`, all four portals via the DS §15): its close (`.x`) sits at the RIGHT of "Mark all read" on one header row (`.notif-all` + `.x` in the flex `.notif-h`, h2 `margin-right:auto`) — never a `.notif-h-a` wrapper or a `.btn`. Each row's mark is a BARE glyph, no chip: `.nrow-ic` has `border:0; background:transparent; background-image:none` (kills §24's `--tal-chip` radial) — an UNREAD mark reads `--accent`, a READ mark reads `--text-secondary` #828282 (the side-nav icon grey). The old 6px unread square (`.nrow.un::after` and the admin's `.dot`) is GONE — state is carried by the mark's ink + the title weight. The notification TIME (`.nrow-w`) drops BELOW the message a size smaller — see the reference-refinement rule below (this SUPERSEDES the 15 Sep "right end, sibling of `.nrow-b`" placement). EVERY divider pays the gutter — the header rule, the rules between rows and the footer rule are inset `::after`/`::before` lines at `--pad-x` both sides, never a wall-to-wall border. OPENING the panel dims the WHOLE frame incl. the app bar (`.notif-scrim`, z 49, only ≥900 — on a phone the panel is full-bleed §28 so there is nothing to dim): the panel + scrim are a child of `.app` (positioned), so on the admin they moved OUT of `.shell-body`. The panel width is `max-width:450px` (§27, Maryam 15 Sep 2026). NO "View all notifications" footer — removed on the admin (the only portal that had one) 15 Sep 2026; the full-record page `home/notifs` stays reachable by deep link. (§15/§27/§24/§19, Maryam 15 Sep 2026)
- THE NOTIFICATION ROW IS A CALM, SPACIOUS, MESSAGE-FIRST BLOCK (`.notif .nrow`, all four portals via the DS §15; Maryam 21 Sep 2026, off a reference used only for spacing/hierarchy quality, NOT its tabs/footer/buttons). The row is a two-column GRID — mark, then message — with the mark TOP-ALIGNED to the title (`.nrow-ic` sheds its 40px box: `width/height:auto`, `align-self:start`, `margin-top:1px`, glyph 20px) rather than centred on the whole block, `column-gap:--s04` mark→message, roomier `padding:--s05 --pad-x` (the row now pays the same `--pad-x` as its `::after` divider, so mark/message/divider share one left edge). The TIME (`.nrow-w`) is grid-row 2 in the message column (`row-gap:--s02`), indented past the mark, at `--t-caption` 11.5px in `--text-helper` — noticeably smaller than the 13.5 message and always the floor grey (read + unread), so the message is the focus and the time is secondary (§63 sizes/inks it; the layout is §15). Message hierarchy unchanged: title h4 (unread 500/primary, read 400/secondary), desc 13.5 secondary. Scoped to `.notif` so the admin full-page list (`home/notifs`, `.mem` rows) and any other `.nrow` reuse keep the flat row. NO in-notification action buttons — the notification informs and the whole row navigates to the owning module, which is the documented model ("acting on any item happens in the module that owns it"); this is deliberate, not the reference's Resend/Copy pattern. (§15/§63, Maryam 21 Sep 2026)
- A NOTIFICATION NEVER CARRIES A FILLED ICON — a filled glyph in any notification is swapped to its STROKED pair (FILL 0), or, where the mark has no stroked pair, to another stroked glyph that fits (Maryam 21 Sep 2026). Applied: candidate `NOTIF` `checkFilled` → `checkOutline` (Account created / Interview complete / Course complete, ×4); agent `AGENT_NOTIF` `star` → `starOutline` (a rated interview). Leader/admin notif data was already all FILL 0. This is the notification DATA only — the Earnings/payout ledgers and the permissions matrix keep their own state glyphs. (data.js/tn-agent-portal.html, Maryam 21 Sep 2026)

- THE SUPER ADMIN CANDIDATE DETAIL (`candidatesDetail`, `candidates/detail/<id>`): NO Tal summary (`detailHead` with no `sum`, back arrow only). The identity row is the candidate portal's `.idhead` — a ROUND 64px portrait (`.idhead-ph`) on the LEFT of the name, the actions a KEBAB menu (`rowActs`, floated right by `.idhead-act`) holding Edit / **Mark As Cohort Leader** (only for an eligible NON-leader; there is NO "Remove Cohort Leader" here — Maryam 20 Sep 2026: "do not give remove a cohort leader option in candidate detail page menu", removal lives on the Cohort Leaders table `cohortsAssign`, so an already-leading candidate shows no toggle) / **Deactivate** (was "Suspend" — the word is "Deactivate"/"Deactivated" now on the candidate confirm, status tag and Status filter too) / Erase — never inline buttons. The record is the candidate portal's eight-fact GRID (`.facts.pf-facts` via `admFact`, `candProfile` data — authored §74, Maryam's u4 matches the candidate portal exactly), third-person: "My track"→"Track", "My level"→"Current Level". NO black level card, NO level-history section, NO separate scenes carousel. Interviews (heading only, NO description, NO "All Bookings") are the candidate portal's COLLAPSED `.found` record (`admIvRow`, `S.ivOpen`, `data-ivopen`): chevron on the LEFT, NO status tag, agent name the title with the kind·fee·when as a sub-line, and a trailing **"View Transcript"** accent-ink link (`.iv-tx-link`, `--accent-text`, `data-go="market/recording/<bookingId>"`) at the row's right end that opens the transcript page — a sibling of `.found-t`, so it navigates rather than toggling the accordion (Maryam 15 Sep 2026). Each opens to the three scenes kept from it in a STATIC grid (`.scv-static`, no carousel/scroll for three), the scene titles in BLACK, NO per-scene "Talent Agent" attribution row and NO void-the-score action. Below Interviews come the candidate's CV — Experience / Education / Licenses & certifications / Skills — the candidate portal's own sections shown READ-ONLY (`candCV`, `cvRow`/`cvArt`, `CAND_CV`; authored placeholder §74). The issuer logos ride in `PF_ART`, now embedded into the design system (`build-ds.py`, off `hifi/build/logos/`, in `DS_HAVE`). Quiz results (`admQuiz`/`.adm-quiz`) is TEXT ONLY — the lede + the two bands lists, heading aligned with the page; the ROSE PIE CHART is REMOVED (Maryam 15 Sep 2026: "remove this quiz pie chart"), and the "Where this puts them on the ladder" rows stay removed. Generous gaps between the parts (Maryam 15 Sep 2026: "add some gaps"): `.adm-quiz-txt` s07 between lede and the two lists, `.adm-quiz .qzp-g` s05 title→rows, `.adm-quiz .qzp-l` s03 between rows — scoped to `.adm-quiz` so the shared DS quiz-peek keeps its own spacing. (tn-admin-portal.html, Maryam 15 Sep 2026)
- THE SUPER ADMIN CANDIDATE-DETAIL COURSES (`candCourses`/`candCourseRow`, `.crs-*`): the accordion body runs FULL WIDTH against the course row — `.crs-list .acc-b` opts out of the DS `--measure` reading cap (`max-width:none`) so the completed course's black card and the four-cell `.facts.pf-facts` insight band fill the width, not a 68ch column (Maryam 15 Sep 2026: "full width against the course"). The certificate card is STRIPPED: NO "Course completion certificate" header (`.crs-cert-h` gone), NO Download/Share buttons (`.crs-cert-a` gone) — just the badge + course name + "TALENTnext"; the badge (`.crs-cert-mk`) shrinks 56×60 → 40×44 (Maryam 15 Sep 2026: "do not give share or download buttons and remove the 'Course completion certificate' ... reduce the badge icon"). A NOT-STARTED course is HIDDEN — `candCourses` filters `status !== 'Not started'`, so only completed/in-progress courses list (Maryam 15 Sep 2026). Admin-local. The COLLAPSED course ROWS are FULL WIDTH too (Maryam 17 Sep 2026: "the course section rows should have full width"): the old `.acc.crs-list{width:100%}` PINNED the accordion to the section content box while the DS `.sec > .acc` bleed shifted it left `--pad-x`, so every row sat 32px short on the RIGHT under the facts grid; dropping the pin lets the bleed work — row content aligns to the content column, the hairlines bleed full. An IN-PROGRESS course OPENS to the candidate portal's Course Progress content (Maryam 17 Sep 2026: "show the course details just like ... the course progress page ... the performance graph, the progress by chapter") — `candCourseProgress(c)`: `coursePerf(c)` (the DS `.perf-*` line chart — one green line, red low point, dashed cohort rule, the average-score ring callout, "Your score / Cohort average N%" legend, a "Key insight" foot) over a "Progress by chapter" `.tile-stack` of `candChRow` rows (`chRow`'s markup READ-ONLY — the green tick + `min · %` meta on assessed chapters, growth-area sparkle, the not-started circle; NO Restart/Resume and NO `.ch.open` "Continue →", the admin is viewing not taking). Data on the record (`series`/`assessed`/`avg`/`cohort`/`growth`/`insight`), authored §74; chapters from `COURSE_CH`. Admin-local. (tn-admin-portal.html, Maryam 17 Sep 2026)
- EVERY PERSON PHOTO ON THE SUPER ADMIN PORTAL IS A REAL PHOTOGRAPH, NEVER AN ILLUSTRATED AVATAR (Maryam 20 Sep 2026: "get rid of avatars and use real images — the other images we are using as candidate or talent agent profile images overall on the platform"). `agentFace(name)` — the one helper behind every admin face (`personCell`, the detail portraits, the scenes carousel, the leader/cohort/linked-account discs) — draws from the REAL photo pool the design system ships and the marketplace + call surfaces already use: `AV.priya/owen/lena/samuel/hana` (five agent portraits) + `CALL_ART.faceW/faceM` (two interview-call faces), seven photos, all reachable as DS globals in this linked portal. A person whose first name IS one of the five agents gets their own portrait; everyone else is assigned one of the seven deterministically by a name hash (stable per person). This REVERSES the same-day choice of `hifi/build/avatars/` (av1 + b1..b4 + g1..g5) — those are the candidate portal's ILLUSTRATED placeholder discs, used there only for the ANONYMISED same-level Ranking, never a real profile (build.py's note). Admin-local (`ADM_FACES`/`ADM_FACE_POOL`/`agentFace`). (tn-admin-portal.html, Maryam 20 Sep 2026) — the APP-BAR account avatar (`.shell-avatar`, top-right) follows the same rule: it shows `agentFace(ME.name)` as an `<img>`, not the "DS" initials (Maryam 21 Sep 2026: "use a profile image of the super admin on the top right of the portal header").
- THE TAL FLOATING DOCK IS HIDDEN ON THE SUPER ADMIN PORTAL (Maryam 21 Sep 2026: "hide tal floating bottom bar from the super admin portal"). The frame's dock slot renders nothing where it used to call `askDock()`; the bulk-action bar still rides that slot when table rows are selected (it is a selection tool, not Tal). `askDock` stays defined — restore it by putting it back in the `bulkCtx() ? bulkBar() : …` ternary. (tn-admin-portal.html, Maryam 21 Sep 2026)
- EVERY CANDIDATE- OR AGENT-NAME CELL IN A SUPER ADMIN TABLE carries the person's PHOTO — a small round 28px disc beside the name (Maryam 15 Sep 2026: "add agent and candidate images with their names in all tables, do this for whole portal"). `personCell(name, sub)` (a real photo via `agentFace` — see the real-photograph rule above, so one face per person everywhere) replaces `idCell` at every person column: Users, Candidates (name + Agent), Talent Agents, Agent Applications, Cohort Leaders (assign), Bookings (candidate + agent), Recordings (candidate + agent). A non-person cell — a missing agent (—/Unassigned/blank), and every `idCell` that is NOT a person (service health, audit action, case ref, transaction/invoice/fee, employer company, lead) — falls back to the plain two-line `idCell`, so no face is invented. The name's weight/ink still come from the cell (`.pcell-n` inherits), so the DS first-column strong-ink and the row-hover/selected accent reach it unchanged. Admin-local (`.pcell*`). (tn-admin-portal.html, Maryam 15 Sep 2026)
- THE SUPER ADMIN RECORDINGS REGISTER (`marketRecordings`, `market/recordings`) is a LEAN list — Candidate / Agent / Interview / Length / Transcript / Actions — and a ROW OPENS THE INTERVIEW TRANSCRIPT (Maryam 15 Sep 2026: "these rows should be clickable ... show the interview scenes and transcript"). The SCENES-consent, Employer-visibility, Retention and Status columns are REMOVED from the table (their state still drives the row's kebab actions — play / download / withdraw consent / delete — and lives on in the data); the "Expiring soon" toggle and the consent/status filter dropdowns are removed too — search only. Candidate and agent cells are `personCell` (photos). The row's `go` is `market/recording/<id>`. (tn-admin-portal.html, Maryam 15 Sep 2026) — THE ROW ACTIONS ARE **View / Download / Delete**, three only (Maryam 21 Sep 2026: "the action items should be View / Download / Delete"): **View** → `market/recording/<id>` (the detail page, which now carries the recording AND the transcript — see the transcript rule below); **Download** → `dltranscript:<id>` (the transcript .txt, shown only when `transcript==='Ready'`); **Delete** → `recdelete:<id>` (avail rows). The old "Open transcript", the "Play recording" modal (`recplay`/`SHEETS.recording`, kept defined but no longer reached from the kebab) and "Withdraw SCENES consent" are dropped from the kebab. (tn-admin-portal.html, Maryam 21 Sep 2026) — THE PAGE HEADING READS **"Recordings & Transcripts"** (Maryam 21 Sep 2026), matching the nav sub-item / breadcrumb (was "Recordings"). The table gains a **"Scenes"** column between Length and Transcript, reading **"N scenes"** — the same count the recording's Scenes tab lists (`ADM_SCENE_SET.length` per interview with an agent, so table and tab agree). The **Transcript** cell is now COLOURED via `tag()`/`.statw` (not plain text): **Ready** green, **Generating** amber, **Failed** red — `TAGV` gains `ready:'green'` + `generating:'warn'` (`failed` was already red). (tn-admin-portal.html, Maryam 21 Sep 2026)
- THE SUPER ADMIN AGENT INTERVIEW DETAIL (`agentsInterview` PAST record, `agents/interview/<id>`) carries TWO TABS — **About Interview** and **Transcript** (Maryam 17 Sep 2026: "i need two tabs on this page, About Interview and Transcript, the transcript from this page will be shown there"). The identity header (`aiHead('Interview with <cand>', '<agent> · <kind> · <when>')`) stays above the strip; the strip is `filterStrip('ivtab', 'about', …)` in a `.sec.sec-cs` (reusing the `data-filter`/`getFilter` tab mechanism, no new handler); About holds the facts + Tal's/agent's evaluations + the candidate's rating & review, Transcript holds `ivTranscript(iv)` (moved off the page foot). The facts are a HORIZONTAL `.facts pf-facts` block of FOUR — Candidate / Interview fee / Candidate rating / When — the **Kind row is removed** (Maryam 17 Sep 2026: "remove the kind row and show the other 4 in the horizontal blocks view"); the rating cell is hand-written (`ic('starOutline')` mark + `ratingCell` HTML) because `admFact` escapes its value. The upcoming (non-past) interview view shows the four facts as a `.facts pf-facts` block, and — for a FIRST interview only (`!agIvIsRe`) — ALSO the candidate's levelling-quiz read (`admQuiz({name})`, the shared §74 read personalised by first name) and a **"What {first} wants to talk about"** section (heading personalised with the candidate's first name, NO description line): the note the candidate wrote the agent at booking, AUTHORED PLACEHOLDER `ivTopicOf(b)` (§74 — the admin seed has no per-booking note; a stable leadership-situation answer per candidate off a name-hash), shown NOT in a box but as the answer wrapped in `--accent-text` curly quotation marks on both sides (Maryam 21 Sep 2026). The quiz + talk-about show for EVERY upcoming interview (first and re); a "What {first} wants to talk about" heading is personalised (no desc line) and the note reads as the answer wrapped in `--accent-text` curly quotation marks (Maryam 21 Sep 2026). A **RE-interview** (a cohort finisher — the seed carries two upcoming re-interviews, `bk14` Grace Kim / `bk15` Diego Alvarez, with an explicit `stage:'booked'`) ALSO shows, via `reInterviewExtras(iv)`: the candidate's whole profile (the candidate-detail 8-fact `candProfile` band + `candCV()` Experience/Education/Licenses/Skills/Linked), a **Cohort journey** four-figure `stats()` band (Chapters done / Assessment average / Time invested / Tasks on time, authored §74 matching the candidate-portal course band), and a **Cohort leader evaluation** (`cohortLeaderEval` — the leader resolved from the candidate's cohort, photo via `agentFace`, a read paragraph and two `qzList` columns; authored §74). There is NO divider line above this section (Maryam 22 Sep 2026: "remove the line above") — the journey `.sec` carries `.sec-noline` (its §14 foot `::after` off); the eval keeps its normal top padding so it sits a proper gap below the band (no line); the aiHead carries NO desc line (Maryam 22 Sep 2026); the leader-row ★ is the rating gold `--star`; the read paragraph runs FULL WIDTH (`max-width:none`, off the reading measure); and "Recommends re-interview" is a GREEN CHIP (`.tag sm green`, the `verifyTag` pill), not `tag()`'s plain `.statw` text (Maryam 22 Sep 2026: "Recommends re-interview should be a chip"). ALL the content sections of this interview-detail page are wrapped in `.iv-detail-body` so a page-scoped rule gives every consecutive `.sec` an extra `--s06` gap (48→72, Maryam 22 Sep 2026: "give more gaps between each section") — the wrapper reaches the sections from shared helpers (`admQuiz`/`candCV`/`reInterviewExtras`) a per-section class could not, and it restates the top-block no-divider `::after` the first section had as `.modhead + .sec`. Admin-local. The candidate is resolved by `candByName`, synthesised (§74) when the booking's name is not in `CANDS`. A first interview shows only facts + quiz + talk-about (Maryam 21 Sep 2026: "show some re-interviews as well … for their detail page … quiz details / what {name} wants to talk about / candidate whole profile / cohort journey in 4 blocks / cohort leader evaluation"). The tab strip section carries `.iv-tabs` and an admin-local rule zeroes its bottom padding + `.cs` margin and pulls the content up to a single `--s05` gap (Maryam 17 Sep 2026: "remove the unnecessary gap between tabs and the content"; the DS `:has()` strip-only zeroing did not resolve at compute time, so a plain class handles it). (tn-admin-portal.html, Maryam 17 Sep 2026)
- THE SUPER ADMIN "TAL'S READ" ON THE INTERVIEW DETAIL MIRRORS THE TALENT AGENT PORTAL exactly (Maryam 17 Sep 2026: "i need the tal evaluation just like it is on the talent agent portal"). The old `aiHead('Tal's evaluation') + .iv-card` plain paragraph is replaced by the agent portal's `.ai-aura.tal-eval` component: the blob mark + `.ai-label.bare` "Tal's read", the read as highlighted `<b>` accent phrases (`.tal-eval .ai-body p b` — `--ai-hl-bg` background + `--accent-text`, both ship in the DS this portal links, so `iv.tal` is rich HTML and is NOT escaped), and a `.help mt5` "Proposed only. TalentNext sets the level after analysis, so this is not a decision you make here." line. Each `PAST_IVS[].tal` was rewritten as a four-paragraph read in the agent-portal structure (Tal suggests <level> · what earns it · what holds it below the next level · ask in the room), derived from the existing tal/strengths/growth so no new claims (§74); level phrasing is "Explorer – E<n>" (en dash, allowed), no em dashes (§5). (tn-admin-portal.html, Maryam 17 Sep 2026)
- THE SUPER ADMIN INTERVIEW TRANSCRIPT (`marketRecordingDetail`, `market/recording/<id>`, a hidden sub opened from a recordings row) MIRRORS the Talent Agent portal's transcript (Maryam 15 Sep 2026, with a reference): `detailHead` back arrow, `aiHead('Interview Transcript', …)` with the candidate·agent·kind·when·length sub and a Download Transcript action, then the DS `.tx-*` component — a "Find a word in the transcript" search (`data-srch="txq"`), round topic-filter chips (Everything / Marked scenes / Delegation / Hard conversations / Decisions, `data-filter="txf:<k>"`) and the line list (timestamp · speaker · text). The MARKED-SCENE lines (the `scene` tag, "📹 Marked scene" chip) ARE the interview scenes, so this one page is the "scenes and transcript" the row promised. The interviewer (agent) reads accent (`.tx-line.me`), the candidate grey, as the reference shows. `TX_LINES_ADM` is authored placeholder (§74); `recTranscript` (the .txt download) is derived from the same lines so the download matches the page. The `.tx-*` CSS ships in the design system this portal links — only markup + data are portal-local; filter/search reuse the existing `data-filter`/`data-srch` handlers (no new listeners). (tn-admin-portal.html, Maryam 15 Sep 2026) — THE VIEW PAGE NOW CARRIES THE RECORDING TOO, not the transcript alone (Maryam 21 Sep 2026: "on view we should not only see the transcript on the next page but the recording as well"): the `aiHead` reads "Interview recording", then `recPlayer(b)` — a 16:9 prototype player (streaming not wired) whose POSTER is a real captured call frame, `assets/agent/iv-recording-still.webp` served relative (the same still the Talent Agent portal embeds as `IV_REC_STILL`), with the `--s...` play disc + scrubber over it — then a `secHead('Transcript')` and the existing `.tx-*` search/filters/lines. Admin-local (`recPlayer`). (tn-admin-portal.html, Maryam 21 Sep 2026) — THE VIEW PAGE IS THEN SPLIT INTO **THREE TABS — Recording · Scenes · Transcript** (Maryam 21 Sep 2026: "there should be 3 tabs ... show scenes of the interview as well"): a `.sec.sec-cs.iv-tabs` strip (reusing `filterStrip('rectab', 'recording', …)` + `data-filter`, the interview-detail tab mechanism, no new handler) under the `aiHead`, then ONE pane — **Recording** = `recPlayer(b)`, **Scenes** = `admScenes([b])` (the shared `dsScenes` carousel of this interview's clips, `ADM_SCENE_SET` × the agent, authored placeholder §74), **Transcript** = the `.tx-*` search/filters/lines (rendered only inside its pane). The three panes are `.sec` siblings of the strip, so `.iv-tabs + .sec` gives the tight `--s05` gap. This SUPERSEDES the single-scroll "player then transcript" layout above. (tn-admin-portal.html, Maryam 21 Sep 2026)
- THE SUPER ADMIN COURSE DETAIL (`cohortsCourseDetail`, `cohorts/coursedetail/<id>`) shows a COURSE OUTLINE — the candidate portal's chapter accordion (`courseOutline`, `.acc.ol` / `.ol-t` / `.ol-m` / `.ol-cd` "Chapter details" hover label / `.lrn` / `.skl`), added after the Price/Length/Status facts (Maryam 15 Sep 2026: "show course outline ... like the one we are showing on the candidate portal side"). Rows are title + "Chapter N · X min"; each opens to "What you'll learn" (`.lrn` ticks) + "Skills you'll gain" (`.skl` chips). The `.acc.ol` component ships in the DS this portal links, so only markup + data are local; the accordion reuses the admin's `data-crsopen`/`S.crsOpen` open-index handler — compare with `String(S.crsOpen) === String(i)` (the dataset value is a string, like `candCourseRow`), not `===` on a number. `COURSE_CH`/`COURSE_SYL`/`COURSE_LEARN` are the reference E-track curriculum, AUTHORED PLACEHOLDER (§74) — every course shows this until real per-course chapters land. (tn-admin-portal.html, Maryam 15 Sep 2026)
- THE SUPER ADMIN COHORT DETAIL head (`cohortsDetail`): the "Led by <leader>" line in the `aiHead` description carries the leader's PHOTO as a small round 24px disc (`.cl-lead` inline-flex + `.cl-lead-ph` round `agentFace(leader)`), Maryam 15 Sep 2026 ("show the cohort leader image"). The date field's label is "Cohort starts on" (was "Cycle start") on the create/edit sheet, the extend sheet and the detail fact card; its trigger (`.fdate-t`) insets the calendar icon at `--s05` like the form dropdown's chevron (not `.inp`'s wide `--s08`) so the date reads like the other sheet fields (Maryam 15 Sep 2026: "why this field is not looking like other fields"). Admin-local. (tn-admin-portal.html, Maryam 15 Sep 2026)

Shape and rhythm
- Radius 0. Round things: marks/discs, `.agt-tag`, the `.tbl-flag` chip, the Tal dock (all a literal `999px`), a chat bubble's `16 16 4 16`, a Tal reply's stat card (`.tw-lines`, `16px`, Figma 882:6701) and its follow-up chips (`.tw-chips .chip-tal`, `999px`), the time-grid slot block `.calw-ev` (`8px`), the calendar now-line pill `.calw-now-t` (`999px`) (Maryam 6 Sep), and the auth-card consent boxes `.auth-card .cbx .box` (`4px`, Maryam 17 Sep 2026). (§56, §31.6, §114, §117, §115, §41, §39, §17)
- Sections are 48 apart; heading to content 12; heading to description 8. (§10, §01)
- A mark is a bare 18px glyph, no tinted chip — every page-body content/figure mark (`.stat-ic`, `.qa-ic`, `.prog-ic`, `.pulse-ic`, `.pf-facts` label mark, `.tg-mk`, `.pf-sr-ic`, `.cl-ic`, `.qzp-r-ic`, `.sump-fi`, `.cco-ic`, `.aw-ph`, `.sumdrop .sd-ic`, `.sig-ic`, `.note-mk`, `.ob-jr-ic`) is 18px, one uniform page-icon size, all four portals via the DS. This SUPERSEDES the earlier 20px (and the 16/17px per-mark variants). The 40px-chip list-row LEADING icons (`.ch-ic`/`.nrow-ic`/`.cardrow-ic`/`.ach-ic`, 22px) are a separate component and are NOT page marks; the Tal floating-field marks (22px, §117) and the side-nav rail icons (20px) are their own sizes. (§29, §72, Maryam 16 Sep 2026)
- A DECORATIVE category-hue mark is INK, not a hue: every bare glyph that only categorises reads `--icon-primary` **#111** (on a dark card, `--on-dark`), never a `--mk`/category colour. This blackens `.stat-ic`, `.qa-ic` (+ all `ic-*` variants), `.prog-ic`, `.pulse-ic`, `.pf-facts` mark, `.tg-mk`, `.pf-sr-ic`, `.cl-ic`, `.qzp-r-ic`, `.sump-fi`, `.cco-ic`, `.aw-ph`, `.sumdrop .sd-ic`. STATE/subject marks are NOT decorative and keep their colour: the green done/verified tick `#00a43c` (`.rec-v`/`.crow-v`/`.pnc-v`/`.pulse-tick`/`.sump-rm.on`/`.note.succ`), the rating star, the journey green/amber/red, the cert-banner brand mark, and `.row-lead > .btn`'s tinted icon-button. All four portals via the DS. (§29/§70 etc, Maryam 9 Sep 2026)
- A CLICKABLE block's decorative mark turns `--accent` **#bd2a2a** on hover — and NOTHING ELSE moves (Maryam 9 Sep 2026: "do not make the border black on hover"). The icon lights on the container's `:hover`, on the quick-action card (`.qa-c`), the cohort card (`.cco`) and the quiz-peek row (`.qzp-r`); armed in `HOVER_KEEP` (`qa-c`/`cco`/`qzp-r`). Each card's old border/background hover was DELETED at source so arming the class revives only the mark. Static figure cells (stat/pulse/facts/prog/sump/cl) get no hover. (§70/§92/§80, Maryam 9 Sep 2026)
- `.stat` and `.qa-c` are one shape: title `--t-h4`, content hangs from the top, the figure/arrow centred on the block's right. (§29, §70)
- The cohort leader's "Progress by Chapter" status mark sits on the RIGHT for EVERY row (`ldrChRow`): a green `checkFilled` for a finished chapter (no longer an inline `.ch-tick` beside the name), a **very light** `--gray-20` **#e1e0dc** ring for the rest (Maryam 9 Sep 2026, "they should be very light in grey" — was `--text-secondary` #828282, a mid grey that competed with the tick). The not-started `--icon-primary` and the started `--in-progress` orange both go to `--gray-20`, so the column reads as one green tick against very light rings; state is in the meta ("Started" / "Not started"). `.ch:not(.done):not(.locked) .ch-ic svg`, so the finished tick keeps its green and the candidate's padlock its ink. (§15, Maryam 9 Sep 2026)
- Every photograph of a person is a disc; artwork (covers, badges, `.crt-art`) stays square. (§106)
- The interview-detail scene cards' play mark (`.scene-play`, on `V.report`'s kept-scenes row AND the chooser) is the SAME disc as the profile-settings scenes' `.scv-play`: a round translucent-black wash (`border-radius:50%`, `rgba(0,0,0,.44)`) with a WHITE glyph — not the old white square with a dark glyph. Sizes stay context-tuned (44px / 32px desktop chooser); only the treatment matches. The `.scene-full` unavailable state dims the disc to a fainter dark wash. (§38, Maryam 17 Sep 2026)
- A person's active status is `.av-on` (§09): a green (`--support-success`) circle at the avatar's lower-right, ABOVE the photo (`z-index:2` — the `.av-ph img` is absolutely positioned, so a plain sibling paints behind it). Sized in per-cent so one class fits every avatar; its host is `position:relative` and non-clipping — a SIBLING of `.av-ph` (`.idphoto`, `.mhead-av`), never a child (`.av-ph` is `overflow:hidden`). On the agent + cohort-leader profile photos and the messages header. (§09, Maryam 7 Sep 2026)
- Icons are Material Symbols **Rounded, FILL 0**, pasted on the `0 -960 960 960` box; only four glyphs flip to FILL 1 to say a state (`star`, `checkFilled`, `stopFilled`, and their pairs). (trap 7)
- Cohort covers are one 9:5 rectangle, `object-fit:contain`. (§86)

Components
- THE FRONT DOOR'S LEFT PANEL IS A PHOTO SLIDER (hifi candidate portal, ≥900). §57's single `__AUTHART__` gradient stays as the pre-decode ground; three photographs cross-fade over it (`.auth-slide-1/2/3`, `__SLIDE1..3__`, Maryam's onboarding images resized to 1080w WebP), on a 15s pure-CSS `authSlide` cycle — NO JS/timer (a repaint would blow away the password being typed), so the slide is animation PHASE not `S`. POSITION BARS sit at the RIGHT END of the copyright row (`.auth-foot-row` is `space-between`: `©…` left, `.auth-bars` right): three 32×5 `999px` white-20% tracks, each with an `--accent` `::before` fill that WIPES — it moves in from the left, holds while its slide shows, then goes out to the right, and the exit of one bar overlaps the entry of the next (`authBarFill` clip-path keyframe + the slides' delays) so the accent reads as one fill travelling bar to bar. Motion-off leaves bar 1 filled. The welcome `<h2>` is SEMIBOLD (600, §63's second stated exception, scoped to the slider); the lede (`.auth-lede`) is a half-width 320px measure at 23px line-height. The panel wears the wordmark as a crisp VECTOR (`AUTH_ART` prints `LOGO_SVG` — the wordmark traced from `LOGO_K` with vtracer, `fill:currentColor`, filled white here — not the upscaled-blurry `LOGO_W` raster), at a reduced 150px width and a 64px top inset (§57's 291/133 stay for the DS's raster). `LOGO_SVG` lives in data.js and only `AUTH_ART` uses it, so `build-ds.py` does not extract it. Two BACKDROPS anchor the text on the photo: a SOFT, gradual dark gradient rising from the FOOT (`.auth-brand::after`, multi-stop from ~.78 up to transparent by mid-panel so the photo stays visible) under the copy + bars, and a gentle dark wash at the TOP (`.auth-brand::before`) seating the white logo on the brighter slides. §57 flips the welcome copy to `--on-dark` and the "Let's begin." line to `--accent`, scoped `:has(> .auth-slide)` so the design-system front door (no slides) keeps its dark-on-gradient copy + black logo. hifi-only — §132 is in `build-ds.py`'s `NOT_IN_DS` (only hifi's `AUTH_ART` emits the markup; the base panel still crosses via §57). (§132/§57, Maryam 17 Sep 2026)
- THE SIGN-UP EMAIL BANNER (`.auth-id`, the panel naming the address) is a TINTED ACCENT CHIP: ground `color-mix(in srgb, var(--accent) 12%, #fff)` (the lightest accent tone), ink `--accent-text` #c2332d for the label, the address AND the "Not you?" link (Maryam 17 Sep 2026: "the inner text to be in accent color and the bg color to be in lightest accent color tone"). This REVERSES the same-day solid `--accent` #f9423a bg + white text; `--accent-text` is the accent as INK (AA on the near-white tint, where plain `--accent` fails). §57.4, crosses to the DS. (§57, Maryam 17 Sep 2026)
- THE AUTH-CARD FIELD FOCUS IS A COLOUR CHANGE, NOT A THICKENING — an auth field must not change height when clicked (Maryam 17 Sep 2026, "the fields input behavior is broke, it changes the height everytime I click on the field"). §12's generic `.app .inp.fill:focus` swaps the 1px underline to 2px and yanks `padding-bottom` to 0 to hold ITS box height; the card pads 8/16 and carries no such compensation, so that rule shrank every auth field 7px on focus. §17's focus rule now matches `.app` + `.fill` (→ 0,5,0, beating the fill rule) and keeps `border-bottom:1px solid var(--text-primary)` + `padding-bottom:8px`, so the box holds 41px. Crosses to the DS. (§17, Maryam 17 Sep 2026)
- THE AUTH-CARD FORM PANEL (`.auth-card > .auth-col .page`, the bordered card the form sits in ≥900) is `max-width:588px` (was 608, "reduce the width a little(20px)") and inset **24 / 28 / 28** (was 33 / 41 / 41); the auth consent boxes (`.auth-card .cbx .box`) carry a soft **4px** corner — a stated radius-0 exception like the admin select boxes. All cross to the DS. (§57/§17, Maryam 17 Sep 2026)
- AUTH-CARD FIELDS PAY NO LEFT PADDING — the input text/dots align to the label's left edge, not 16px in (Maryam 17 Sep 2026, "remove the left side padding"); the label sits **6px** above its field (`.auth-card .f > label` margin-bottom); the password↔confirm rows are **32px** apart (`.f-row` gap). (§17, Maryam 17 Sep 2026)
- THE SET-PASSWORD AND LOG-IN CARDS SIT ON ONE EQUAL **32px** RHYTHM ≥900 (Maryam 17 Sep 2026, "give 32px space between all rows … each section of right side card", and "i need 32px equal gaps in this card as well" for log-in): every section's own vertical padding is zeroed and a single 32px top margin carries every join (desc→…→CTA), matching the password↔confirm 32; on log-in the two bare `.sec-rule > .f` fields (email/password) also take 32 so every gap is one value. Both cards opt in with **`.authtight`** on their `.page` (the marker only these two screens write); no dividers inside an authtight card (`.sec` `border-top:0`, killing §17.6's `.sec-rule` line that read as a divider under the log-in sub-text). The padding-zero carries `!important` **on purpose**: the platform section rhythm (1 Sep "48px" rule + §101 block gaps) sets `.app .page > .sec` padding through a ~30-class `:not()`/`:has()` chain that no plain selector can outweigh; reproducing it to add an exception is worse. Desktop card only. (§57, Maryam 17 Sep 2026)
- THE hifi `create` (SET-NEW-PASSWORD) SCREEN reads heading "Set New Password" / desc "Create a strong password for your account." (with the full stop), a black "Set Password" CTA that goes to the **Log In screen** (`data-go="login"` — this SUPERSEDES the same-day `stage:onboard` route to the Tal onboarding), and has NO "Already have an account? Log in" closing row (Maryam 17 Sep 2026). The three consent boxes START UNCHECKED; the CTA is DISABLED (`.btn[disabled]` look + `pointer-events:none`) until the TWO required consents (`.cbx-req`, Terms + interview-recording) are both ticked — a pure CSS `:has(.cbx-req input:not(:checked))` gate that re-reads live on each native toggle, no render (trap 9). The third (marketing) is optional. hifi-only markup (`AUTH.create` in views.js) + §17. (Maryam 17 Sep 2026)
- THE hifi `login` (LOG IN) SCREEN, reached from the Set Password CTA (Maryam 17 Sep 2026, supplied content): heading "Log In" (supplied casing, kept), sub "Enter your details to sign in to your account.", an **Email Address** field (placeholder "Enter your email address") and a **Password** field (placeholder "Enter your password") with a **"Forgot Password?"** link under it (`data-go="forgot"`, ink `--accent-text` — Maryam 18 Sep 2026), a **"Remember me"** consent box, and a black **"Log In →"** button. The Log In button goes to the **Tal onboarding prototype** (`data-go="stage:onboard"` — Maryam 18 Sep 2026, was `data-loginas="candidate"` into the candidate dashboard). It DROPS the older login's role picker (`loginRoles()`, now unused). hifi-only markup (`AUTH.login` in views.js). (Maryam 17–18 Sep 2026) — A **SIGN-UP ROW** sits below the Log In button (Maryam 22 Sep 2026, reversing the 18 Sep "drop the Sign up row"): a LEFT-ALIGNED `.auth-alt` line "New to TALENTnext? Sign up", the lead-in `--text-secondary` grey and **"Sign up"** the accent as ink (`--accent-text`), semibold (the platform's strong weight `--t-w-strong` 500) and UNDERLINED; 24px above it. Inline styles follow the "Forgot Password?" precedent (hifi-only markup). NAVIGATION (Maryam 22 Sep 2026): "Sign up" opens the external get-started page `https://web-puce-six-27.vercel.app/new/get-started` in a NEW TAB — a real `href` + `target="_blank"` (`rel="noopener"`), NOT a `data-go` route, because it leaves the prototype for another origin. (Maryam 22 Sep 2026)
- THE hifi FORGOT-PASSWORD FLOW is `forgot` → (Send Code, "Sending..." beat) → `fcode` (Enter Code) → `reset` (Maryam 18 Sep 2026). The `forgot` screen reads heading **"Reset Password"** / desc **"Enter the email for the verification code."**, the email field, and one CTA **"Send Code →"** — NO "Remembered it? Back to log in" line. The CTA carries `data-send="fcode"`: on click it swaps to **"Sending..."** and disables via inline `pointer-events:none` (NOT the `disabled` attribute — §02's `.btn[disabled]` greys it and the loading button stays dark), then advances to `fcode` after ~900ms; the button is mutated in place with no render() in the interval so the loading label survives (trap 9). `fcode` (Enter Code, content off Maryam's reference, built in our auth-card components) reads heading **"Enter Code"** / desc **"We sent a 6-digit code to your email."**, a **"Verification Code"** label with an `--s06` gap below it to the code row, a SIX-digit `.otp` row (matching `.otp`'s 6-cell grid), a full-width **"Verify Code →"** button (`data-go="reset"`) and a **"Didn't receive code? Resend"** line BELOW the button — plain block flow, not `.foot-row` (which sits the action and the way-out side by side on desktop). (Maryam 18 Sep 2026) It is separate from `verify` (the create-flow six-digit email confirm that lands on onboarding). The `reset` (SET-NEW-PASSWORD) landing reads heading **"Set New Password"** / desc **"Set a strong password for your account."**, the New password + Confirm new password fields with NO `.pw-rules` checklist, and one full-width **"Update Password →"** CTA that returns to the **Log In screen** (`data-go="login"` — Maryam 18 Sep 2026, was `stage:new` into the dashboard) — NO "Changed your mind? Back to log in" row (the create screen keeps the pre-filled/pre-ticked composition; `reset` is stripped to the two fields and the button). The old `sent` "Check your email" screen is off this path (kept defined, reachable by nothing). hifi-only markup (`AUTH.forgot`/`AUTH.fcode`/`AUTH.reset` in views.js) + the `data-send` handler. (Maryam 18 Sep 2026)
- A centred button with a LEADING icon (`<svg> Label`, i.e. `.btn.noic` with the mark before the words) gets +--s02 (4px) of right padding, so the icon's own left air does not make the group read right-tight. Universal, all portals, one rule in §02. Only the leading case: the GO arrow (`I.arrowRight`, tagged `.i-go`) TRAILS a button and is excluded — it is the one CSS-visible signal that separates leading from trailing (both are a lone `<svg>` child); `.ic-l` (box ends at its words) and `.ghost` opt out too. (§02, Maryam 7 Sep 2026)
- "Make this a black card" means the WHOLE §75 recipe — inset by `--pad-x`, `--s07` inside, haze as `background-image`, the section's hairlines off, `.dc-hd` heading rule, ink flipped by token. Never `.plate` / `.sec.on-dark` in the page body (they get hoisted into the head band). (§75)
- Every heading with a sentence under it is `aiHead({title, desc, act})`; the actions centre on the pair. One Tal star per page. (§73)
- The enrolment card's figure row (`.facts.eo-facts`): the fourth cell's heading is "Members" (not "Total Members"), with the "N seats left" chip (`.eo-chip`) on its RIGHT on the same line — `.eo-fl` is `space-between`, and the chip is `--t-caption` 11.5px at 1px vertical padding (smaller than the old `--t-label`) so it fits there. The "View Course" CTA joins the row as a trailing `.eo-cta` cell and draws NO divider before it (`> .eo-cta::before{content:none}` — it is the card's action, not a fifth figure), at the FULL `.btn` height (40px), never `btn-sm` (32px), so it matches the card's other buttons. Between 900 and 1300 the row with a CTA goes 2×2 with the button on its own full-width line below (`.eo-facts:has(> .eo-cta)`), because five columns crush the cells there; `V.enrol` (no button) keeps four-across. (§63/§108, Maryam 9 Sep 2026)
- The enrolment black card carries **"Tal recommends"** with the sparkle (`.ai-label bare rec-lab`) above the course name (`.eo-cname`), the same label the agent recommendation card wears (Maryam 9 Sep 2026). Its "View Course" CTA (`.eo-cta`) is DASHBOARD-ONLY — gated on `act === undefined`, not `!act`: `V.payment` passes `act:false` (a card with no head action), and `false` is falsy, so `!act` was still drawing "View Course" on the checkout page where the course is already chosen (Maryam 9 Sep 2026: "remove the view course button from black card on this page"). `undefined` = the dashboard's "no act"; `false` = payment's "no action at all". (views.js `enrolOffer`, Maryam 9 Sep 2026)
- Head band: Tal's summary left, the one dark card or the journey right; full-width summary when there is no second column. (§56, §70)
- The head band WASH (`--ai-wash`, on `.modhead::before`) is the Figma node 871:6425 gradient, whole: `linear-gradient(183.76deg, rgba(189,42,42,.05) 11.02%, rgba(255,255,255,.05) 52.27%)` — the new accent #bd2a2a at 5% along the TOP edge fading to white by the panel's middle, no green. Replaces the old green-through-warm horizontal ramp. All four portals via the DS. (§70, Maryam 9 Sep 2026)
- The head band label "Summary by Tal" is FLAT BLACK SEMIBOLD, no text gradient: `--text-primary` ink at `font-weight:600`, the `--ai-grad` clip removed. 600 is the ONE stated exception to the two-weight rule (§11's SemiBold face is loaded); the Tal sparkle mark beside it keeps its ramp. All four portals via the DS. (§70 §2 + §63 §10, Maryam 9 Sep 2026, off 871:6425)
- The Tal summary DESCRIPTION (the body paragraph under the label, `.ai-aura.talsum > .ai-body` and its `.tsum` p) is `--tal-sum-desc` **#414141** at `--t-w-book` (400) — a dark grey well ahead of a plain description, platform-wide (Maryam 15 Sep 2026: "the tal summary desc needs to be a little darker", then set to #414141). This SUPERSEDES the 9 Sep `--text-secondary` **#828282** (off Figma 871:6425), which in turn reversed an earlier "TRUE BLACK" (`#000`); the `<b>` accent phrases still carry the emphasis and keep `--accent-text`. It reads a dedicated token (`--tal-sum-desc`, §01), so ONLY the summary body moves — every other description stays #828282. All four portals via the DS. (§33/§01, Maryam 15 Sep 2026)
- The Tal summary MARK (the "star" beside "Summary by Tal", `.ai-aura.talsum .ai-label::before`) is the BLOB artwork `--tal-mark`, not the sparkle/sphere it was: **28px**, `background-size:contain`, radius 0, no gradient/glow/wordmark, no `mask`, and the `::after` spin off. The WINNING layer is **§70.2** (which had reduced §33.6's sphere to a 12px `--ai-star` sparkle shared with `.rec-lab`); that shared rule is SPLIT — the "Tal recommends" `.rec-lab` mark KEEPS the 12px sparkle, only the talsum star becomes the blob. §33 §7 is also repointed to the blob for consistency but §70 wins. Every head-summary Tal mark is now the one blob; the black card's "Tal recommends" sparkle is untouched. All four portals via the DS. (§70/§33, Maryam 9 Sep 2026)
- THE ORB'S STATE IS PER-SLOT, AND ONLY THE ORB YOU ARE TALKING TO REACTS (Maryam 17 Sep 2026: "use isolated states of orb where ever they are meant to be used ... in the header there should always be idle state"). An orb is either **live** — it reports what Tal is doing — or an **identity mark** that says "Tal" and holds `idle` for ever. Live: the chat hero (`.tal-hero`, candidate + leader + onboarding) and the byline on the reply that is STREAMING. Idle always: the chat header (`.ask-top-id`), the floating dock and ask bar (`.askline-mark`), the Tal panel header, the composer (`.composer-mk`), the head-band summary mark, and every settled byline. `borbMark(cls, live)` (icons.js) stamps `data-borb-live` for the first kind; `orbSync` (orb-wire.js) drives `.borb[data-borb-live]` and PINS every other `.borb` to idle on each render, so "chrome is idle" is an invariant restored by the renderer rather than a convention nine call sites have to keep. This REVERSES orb-wire.js's first model, one `querySelectorAll('.borb')` taking every orb at once — asking Tal a question set six orbs thinking across the frame, which read as the page glitching. (§132/§133, icons.js + orb-wire.js, Maryam 17 Sep 2026)
- The head band's Tal MARK is the ORB COMPONENT in its idle state, not a picture of it (Maryam 17 Sep 2026: "in summary section you should use the idle state of the orb"). `placePageSummary` (ai6.js) emits `borbMark('tal-mk')` into `.ai-label` — without `live`, so it never thinks or speaks — and §133 switches off §70.632's `--tal-mark` `background-image` on `.ai-label::before`, gated on `:has(> .borb)` so a portal that has not emitted one keeps the artwork. Same **28px** as the artwork it replaces, so the band's geometry does not move. This SUPERSEDES the 9 Sep "the summary mark is the BLOB artwork `--tal-mark`" rule, which stands only for the hand-written portals until they emit an orb. (§133/§70/§33, ai6.js, Maryam 17 Sep 2026)
- THE SMALL / CHROME TAL MARK IS THE FLAT BLOB, NOT THE ANIMATED ORB (Maryam 21 Sep 2026: "use this blob in all the short blobs places like the ones attached", supplied as `tal-blob.html`, with the Summary-by-Tal band mark, the Ask-Tal dock mark and the chat-header mark as the three examples). Every small Tal mark that is CHROME — the head-band summary mark, the ask bar / field / dock (`.askline-mark`), the chat header (`.ask-top-id`) and panel header, the composer, the standalone mark and every SETTLED thread byline — renders as the clean STILL blob: a flat **145° `#ff6e60` → near-white** body (`--tblob-rose`, a stated exception to §132's "the orb is shades of --accent") with the three brand chevrons in flat white at **half the disc**, no glass, no moving pools, no cast shadow, no motion. The LIVE orb is untouched and keeps the full §132 animated component: the chat hero and the byline of the reply that is STREAMING (a byline flattens the moment its reply settles). Implemented CSS-only in §135 on `.borb:not([data-borb-live])` — the exact seam `borbMark` already stamps (see [[the orb's state is per-slot]] rule above) — so no call site, no JS and no §133 box-sizing changes and every slot flattens at once. The three blade clip-paths are byte-for-byte §132's / `tal-blob.html`'s (same brand mark). Crosses to the DS: the agent portal's borb marks flatten with the candidate portal's; the admin portal emits no borb (Tal summary hidden) so it is untouched. Reversible: drop `135-tblob.css` and its two LAYERS entries. This SUPERSEDES, for the chrome marks, the 17 Sep "the head band mark is the orb component in its idle state" — the chrome orb no longer draws the glass/motion, only the live orb does. (§135/§132/§133, Maryam 21 Sep 2026)
- THE ORB'S TINT DEPTH HAS A HARD CEILING AT `--borb-int: 1.08` and passing it costs detail rather than buying colour. Each of the four tint clouds ramps from `rgba(t, 0.92 * int)` to transparent; CSS clamps alpha at 1, so a peak above 1 paints a FLAT OPAQUE disc in the middle of that pool — at 1.86 the inner 42% of every cloud was flat, four overlapped into a solid red ball, and a pool with no gradation cannot show that it is MOVING, so the background motion disappeared as a side-effect of a colour setting. Depth comes from the palette, from the four pools overlapping, and from `--borb-core` (the density of the middle, its own control) — never from clipping. Ships at 0.52 with the core carrying the colour. (§132, Maryam 17 Sep 2026)
- THE ORB'S DENSE MIDDLE IS DRAWN UNDER THE MOVING POOLS, never over them. `.borb__field::after` is the core at `z-index:0`; the four `.borb__cloud` tints sit at `z-index:1` above it and the grain, sheen and cast shadow above those. Anything painted OVER the clouds hides the motion — the layer shipped first as a white lid and then as a red one, both of which read as a flat disc with no movement in it. The core's ramp runs DOWN the palette as it travels out (t1 at dead centre through to t4 at the rim), landing the middle near `rgb(226,116,111)` against a near-white rim, off Maryam's reference of 17 Sep 2026. (§132, Maryam 17 Sep 2026)
- The highlighted-phrase POPOVER (`.sumdrop`, opened from a `data-sum` accent phrase) takes the platform's own edge and corner: a `--rule` **#efefef** 1px border (was the `--ai-1` accent edge) and radius **0** / `--radius` (was 8px). Its prose (`.sd-lead` / `.sd-read` / `.sd-nb`) is `--text-secondary` **#828282** at 400 (was `--text-primary`); the blue lead-in `.sd-read b` (#0488c5) and the black "Your Next Step" label `.sd-nt` keep their colours. All four portals via the DS. (§70 + §63, Maryam 9 Sep 2026, off 875:6570)
- A `.crow-a` action button is `min-width:185px` (a floor, equal pair) + `white-space:nowrap`, so a long label ("View candidate profile") grows to fit on ONE line rather than wrapping; below 900 it goes full-width. A button label on two lines is a bug. (§71, 6 Sep)
- The candidate General edit form (`pfFormGeneral`) holds Name, Nickname (a handle), Email (read-only), **Current role, Industry, Years of experience, Intent aspiration** and About — Name / Nickname / Email on row 1, Current role / Industry / Years of experience on row 2 (`.pfe-g3`, three columns ≥700, stacking below), Intent aspiration a 3-option select (`INTENTS` = Develop in my current role / for another role / for ownership), then About full width (`w:1`). Intent aspiration is the SINGLE intent field cohort formation reads (it is the "intent aspiration" of the five onboarding fields — not duplicated). The **student flag is derived from Current role = "student"**; the degree program lives in Education (its `degree`/`field`). `pfMiss('general')` counts name/nickname/role/industry/years/intent/email/about; the read view (`pfSecView.general`) appends Current role / Industry / Years / Aspiration to the `.facts pf-facts` band (icons `user`/`skill`/`time`/`flag`). This SUPERSEDES the 7 Sep four-field freeze — the client's five-field onboarding requirement reopens it. (§111, Maryam 9 Sep 2026)
- The Intent aspiration field is OUR dropdown (`dd()`), never a native `<select>` whose open list is the OS's (Maryam, 9 Sep 2026: "this dropdown should not be system dropdown but of our ui"). §123's `.dd*` CSS already ships in this build; the candidate portal now has the `dd()` builder + `S.dd`/`S.ddVal` state + `data-ddtoggle`/`data-ddset`/click-away handlers, ported from the agent portal — `pfField`'s `t:'dd'` emits it. The form is a prototype that does not persist on Save (`data-pfedit` only closes), so the pick lives in `S.ddVal[fid]` for the session. (§123, views.js, Maryam 9 Sep 2026)
- The profile fact band (`.facts.pf-facts`, read view) is a FIXED FOUR COLUMNS at ≥900 (`§105.1a`, inside the container query), so the candidate's eight facts read as two attached rows of four rather than one row of eight (Maryam, 9 Sep 2026: "divide these 8 cards in 2 rows ... 4 cards in each row, all attached, just like a grid"). The band's own 1px grid gap paints the row hairline too, so it reads as one grid. A four-fact band (leader/agent public profiles) stays one row of four; below 900 the base `.facts` auto-fit stands (two columns on a phone). (§105, Maryam 9 Sep 2026)
- The COHORT LEADER profile module mirrors the candidate's: FOUR tabs — General Profile / Public Profile / Notifications / Privacy Settings (`S.ldrPfTab`). General is the candidate's `pfSecView.general` shape — a 72px `.idhead` (name, "Volunteer cohort leader · leading since …", the "Assesses E1–E3" `.tag`, the `.av-on` dot, Edit details), a four-fact `.facts pf-facts` band (Candidate rating 4.9 · Cohorts led 8 · Completion rate 84% · Level movement +0.8 — the leader's standing figures, moved off the old Public "Your standing"), then an About paragraph (`LEADER.bio`). Notifications is its own tab (the four `.tg` toggles). Privacy Settings is the candidate's `pfPrivacy` composition — Sign in and security (two `.pf-sr` rows, `LEADER.email` + password with Reset via `S.pfPw`/`data-pfpw`, the inline `pfFields('pw', …)` form) over Closing your account (`.close-b`, `data-del`). `LEADER` gained `email` and `bio` in lead.js for this. (Maryam 7 Sep 2026, `V.leadProfile` in lead4.js)
- The cohort leader's PUBLIC Profile is built on the Talent Agent's Public Profile (Maryam 9 Sep 2026, "improve the cohort leader public profile just like talent agent public profile"): a 72px `.idhead` with rating + assess range on the `.idmeta` line (`★ 4.9 · Assesses E1–E3`), a four-cell `.facts pf-facts` band, an About paragraph and a Specialities `.skl` chip cloud — NO "public profile" heading/description/edit button (editing is on General's "Edit details"), first two sections `.sec-noline` so it reads as one band, no `.av-on` dot (§09). The four cells are the leader's, not the agent's — Cohorts led / Call length / Cadence / Leading since (a leader is unpaid, so no fee cell; rating and range stay in the idmeta, not repeated). The old "Your listing" kv folds in: range → idmeta, call length → a cell, Specialism → the chip cloud (`LEADER.specs`, authored placeholder). This SUPERSEDES the 7 Sep identity (stars line + "8 cohorts led" helper + bio quote). (`V.leadProfile` in lead4.js, Maryam 9 Sep 2026)
- The Talent Agent's PUBLIC Profile identity uses the SAME large 72px `.idhead` as My Profile (not the old 40px `.cardrow`); the sub-line is the rating + assess range, no `.av-on` dot (that green marker is the owner's own view, not the listing). (Maryam 7 Sep 2026)
- Both idhead sub-lines under the candidate's name — the read view (`pfSecView.general`) and the edit form (`pfFormGeneral`) — show the NICKNAME, not the email or the headline. (§111, Maryam 7 Sep 2026)
- A read-only field is `[readonly]` + `.pfe-ro`: §12 draws `[readonly]` with a DASHED underline (the lock), and `.pfe-ro:focus` holds that dash instead of arming the 2px accent — never `disabled` (the value stays selectable), and no "Needed" flag. `pfField`'s `{ro:true}`. (§111/§12, Maryam 7 Sep 2026)
- A profile photo's edit control in EDIT view is the `.idphoto-edit` pencil badge on the disc's bottom-right — a ROUND (`999px`) chip in `--link` blue (deepening to `--link-hover` on hover), `--background` ring, NOT a "Change photo" button beside it. This REVERSES §105, whose reason (two controls 40px apart) the button's removal undoes; the badge is the one place a mark is not radius-0. Read view keeps a plain disc, no badge. (§11/§111, Maryam 7 Sep 2026)
- The PHOTO PICKER modal (`photoSheet`, opened by the `.idphoto-edit` badge) has TWO tabs — **Profile Image** and **Avatar** (`S.photoTab`, a `.cs` strip filling the width, the sheet header divider dropped so the strip is the one line under the title). PROFILE IMAGE: the current photo large and centred (160px disc — enlarged from 120 to use the modal's space, Maryam 14 Sep 2026), with a centred Upload / Remove row below — each mark LEADS its label (`.btn.noic`, `I.upload` = the upload arrow NOT `I.add`, `I.close` for Remove). Upload is a `<label for>` over a hidden `<input type=file accept=image/*>` so the click opens the OS file dialog; `pfPhotoUpload` reads it into `S.photoPreview` and re-renders. Remove sets `S.photoPreview='removed'`, swapping the disc for the `image` placeholder glyph in a light `--surface-2` disc. AVATAR: the ten supplied discs (`AVATARS` — av1 + b1..b4 + g1..g5) at FIVE per row (two rows), the current one carrying `.photopick.on`; selection is `S.avatarPick` (default `av1`). The black footer button reads **"Update Photo"** on the Profile Image tab and **"Update Avatar"** on Avatar; a prototype, so it and Cancel just close (opening resets to the current photo). All state in `S` (trap 9). BOTH tabs are the SAME height — `.photo-sheet .sheet-b` holds a `min-height` and vertically centres each pane — so the modal does not resize when you switch (Maryam 14 Sep 2026). (§11, `photoSheet`/`pfPhotoUpload` in views.js, Maryam 14 Sep 2026)
- The candidate General-details EDIT form (`pfFormGeneral`) has NO "General details" heading — it opens straight on the photo row; the `.sec-h` stays only as the carrier for the Save / Discard pair, which `.pfe-acts{margin-left:auto}` floats to the far right of the label-column-free profile page. The READ view never had that heading (it opens on the `.idhead`). (§111, views.js, Maryam 14 Sep 2026)
- Module pages have no `<h1>` and no fact row; the page name is in the top bar; back is "← Back". (§78)
- A sub-page's back arrow returns to where you came FROM (`S.hist`'s last entry), never a hardcoded view — same target the breadcrumb's penultimate crumb links; fall back to the module root only for a deep link with an empty stack. (§78)
- A STRIP-ONLY tab section draws NO closing hairline under it: `.cs`'s own `border-bottom` is the boundary, so a `.sec-cs` holding only the strip has its `::after` off. A `.sec-cs` that ALSO holds its content (leader Cohort/Reports/Profile) keeps `::after` — that is a real section-foot separator, not the after-tabs line. Gated `:not(:has(> .cs + *))`. (§98.3)
- A strip-only tab section sits ONE `--s06` (24) above its content, not two: its own `padding-bottom` and the `.cs` `margin-bottom` are zeroed so only the content section's `padding-top` remains — else the strip pays the gap twice (~64px). Same `:not(:has(> .cs + *))` gate. (§98.4, agent portal, 6 Sep)
- Quick action cards may take a short one-line description, but it is a STATIC descriptor of the destination — never the queue's data (a name, time or count), which lives on the page the card opens. (§98.3, agent portal)
- A dashboard's quick actions are a ROW of `.qa-c` cards laid out by `.qa` — 2/3 up on the candidate and agent dashboards (the base grid, `max-width:784px`), and 4 up on the admin dashboard via `.qa.qa-4` (§70), which lifts the cap and steps 4 → 2 → 1 across 900/600 like the `.stats` rows beside it. At the narrower 4-up width the title and description must each hold ONE line, so the copy is kept short (§98.3 makes the descriptor static, which keeps it short too). `.qa` is a §10.15 label-column opt-out (its heading sits above the card row). (§70/§10.15/§98.3, Maryam 8 Sep 2026)
- A dashboard puts related SHORT key/value blocks SIDE BY SIDE, not each in its own full-width band — the frozen file's `g-fin`/`g-3`/`g-2` rows. `.dgrid` (`.dgrid-2` / `.dgrid-3`) is the grid, each `.dcol` a heading + its `.kv`, no card chrome (the linear-band aesthetic holds), stacking to one column below 900. Inside a `.dcol` the `.kv` drops the 184px label spine §10.5 gives it (a narrow cell has no room) and reads label-left / value-right; the per-block description and action are dropped (the rail is the navigator). Wide stat rows and tables stay full-width. (§120.1e, Maryam 8 Sep 2026)
- The Interviews view keeps its FOUR tabs (Requests / Upcoming / Evaluations / Past Interviews) even with nothing booked; each tab draws its OWN empty section, never one page-wide empty state that hides the strip. The lists are gated on `v.busy` (IVS is a fixed array, so `reqOpen()`/`diary()` are non-empty whatever the stage). (`V.interviews`, agent portal, 6 Sep)
- No timezone is shown anywhere in the agent portal — no "ET"/"Eastern time" label, and times are bare ("3:00 PM"). (Maryam 6 Sep)
- The time-grid day column head is Google-style: day abbreviation ABOVE the date, centred; today's date is a filled accent disc (same marker as the month grid). (§42, 6 Sep)
- The calendar has three views behind a Daily/Weekly/Monthly switch (`data-calview`), Weekly the default: Weekly/Daily are a time-grid (`.calw` — hour axis, a column per day, bookings placed and sized by time, a now-line at `CAL_NOW_MIN` on today), Monthly is the month grid (`.cal-grid`). One header (`calHead`), one popover, one set of block states across all three; nav means ±1 month / ±7 / ±1 days by `S.calView`. (§41/§42, agent portal, 6 Sep)
- Calendar slots are FILLED by type in both views — first interview `--cal-iv`, re-interview `--cal-reiv`, open `--cal-open`, no border: the weekly/daily time-grid is a block sized to its duration (`.calw-ev`, 8px, 3px off the hour line); the MONTHLY view is a compact one-line row (`.cal-slot`, 6px) carrying the same fill (no dot) + time + name. Today's date is a filled accent disc, white text, no border. A cell lists every slot on its day up to 3, then `+N more` (`.cal-more`) opens the day view. No Join or any button on the slot; Join is in the card it opens. (§41/§42, agent portal, 6 Sep)
- A Join is shut (`disabled`, grey) until the call opens; the gate is on by default. (§81, `joinLive`)
- THE JOIN BUTTON IS DISABLED WHENEVER THE CALL IS MORE THAN 1 MINUTE AWAY — it enables only inside the final minute before the start time. A countdown/`crow-due` timer reading more than `1:00` always means a disabled Join; the button goes live when the timer reaches ≤ 1 minute (and stays live once the call is open). (§81/§119, `joinLive`, 6 Sep)
- The "Add a slot" control leads with a plus (`I.add`), never a trailing arrow — adding is not going somewhere. (§64, agent portal, 6 Sep)
- A dialog footer action (a `.sheet-f` / `.conf-f` button) grows NO trailing arrow — it is one of a pair of answers next to Save, not a link. §64's arrow is for a quiet button that takes you to a page. (§64, 6 Sep)
- A MODAL SIZES TO ITS CONTENT and never reserves blank space: `.sheet-b{flex:0 1 auto}` (was `flex:1` = `1 1 0%`, whose flex-basis:0 + `overflow:auto` let some engines make the body taller than its content inside the auto-height sheet — a reserved gap, e.g. below the Origin dropdown on Add User). The sheet is header + content + footer, capped at `max-height`; the body scrolls only when capped; a dropdown opens downward over the body, no space kept for it. All portals via the DS §02. (Maryam 13 Sep 2026)
- A MODAL OVERLAY COVERS THE WHOLE FRAME, app bar included: on the Super Admin portal the `.modal` overlays (`confirmSheet`/`sheetView`/`dsPayForm`) are direct children of `.app` (positioned), not `.shell-body` (which begins below the 68px bar) — so `inset:0` dims the header too; `.modal` z-index (70) beats the bar's (30). The notif panel stays in `.shell-body` (a bar dropdown, not a full overlay). (tn-admin-portal.html render, Maryam 13 Sep 2026)
- MODAL FRAME (all `.sheet` modals, all portals): one uniform `--s06` (24) inset on the header, body and footer — the title, fields and buttons all sit the same distance from the walls (header keeps `--s05` right for the `.x`). The header divider is INSET by `--s06` (a `.sheet-h::after` rule, not a wall-to-wall border); the footer has NO top divider. (§02, §10, 6 Sep)
- The add-slot sheet is OUR dropdown, never a native `<select>` whose open list is the OS's: `dd(key, opts, val, {ic})` draws an `.inp.dd-btn` trigger (rotating `chevDown` flush right, a leading `time` clock on the time variant) and a `.dd-menu` popup on `.acct-menu`'s recipe; the open one is `S.dd`, and its `.dd-opt.on` row is the accent — tick fill `--accent-text` (§123) and label `--accent-text` (§63 §52) — over a `--overlay-selected` ground. `dd-opt` is the one armed hover. (§123 + §63 §52, agent portal, 6 Sep)
- An open dropdown OVERLAYS everything and is not clipped by the sheet's scroll: `editSheet` puts `.dd-open` on `.sheet-b` while `S.dd` is set, which makes the body `overflow:visible` and lifts it above the footer (§123.4). Time fields open upward, the day field downward. (§123, agent portal, 6 Sep)
- The add-slot Day is a WEEKDAY name only ("Monday"), not a date, with NO "Day"/"Time" field labels (the controls say what they are); the Time is two side-by-side dropdowns in a `.tm-row` grid — start "to" end, each 12-hour with AM/PM (`SLOT_TIMES`), after the reference; no "45 minutes…" helper; the save button is "Save Slot" (a third `SHEETS` tuple slot). (§123, agent portal, 6 Sep)
- ADDING A PAYMENT METHOD OPENS ONE SHARED MODAL ON EVERY PORTAL (Maryam, 6 Sep): Stripe's own "Add Payment Method" form, EMBEDDED as a picture (the Calendly third-party rule), never redrawn. `payForm(tab, open)` in views.js draws it — a `.pay-modal` over the `PAY_ART` picture (Card / US-bank), three transparent buttons over the form's own painted tabs and Save/Cancel row (`data-paytab` swaps the state, `data-payclose` + backdrop close). It crosses to the design system as `dsPayForm` (DS_RENAME/DS_BUILDERS) with `PAY_ART` re-embedded by `build-ds.py` and named in DS_HAVE. `S.payTab` is the state; each portal has its own open flag (`S.addCard` candidate, `S.payModal` agent/admin) toggled by an "Add card"/"Add payout method" button. No "How a fee splits" on the agent Earnings page.
- A candidate PAYS from a saved card, never a re-typed one: `cardPicker()` lists `S.cards` (`BMK` brand marks, the `.cardrow` the Payments page draws minus Remove), the default selected (`payWithIdx()` → the `def` card), `data-paypick` chooses (`S.payWith`). "Add a card" (`addCardAct`) rides the RIGHT of the "Pay with" heading row (`.sec-h-act`), not the card list, and opens the shared modal. Used on the course payment (`V.payment`) and the booking checkout (`V.checkout`). Both screens are the picker + the pay button and nothing else: no Tal summary band (no `PAGESUM` entry), no processor note, no fee/total or order-breakdown tile, no refund line — the price is on the pay button. (candidate, 6–7 Sep)
- An `ENROL_COURSE` (course card) `desc` is a DEFINITION of the course — what it teaches and what it is for, in `E3`'s catalog register — NOT the journey/format ("take the same chapters further, close the 90 days"). The format lives in the stat cells, the crumb and `ENROL_DESC`. (candidate, 7 Sep)
- The ALL-COURSES list (`allCourses`) FOLLOWS THE ALL-AGENTS LIST exactly (Maryam, 9 Sep 2026: "follow the organized ui we have on the all agents screen"): the `.agt` table is a BARE sibling of the heading `.sec`, like `agentsTable()` — never nested inside the `.sec` (that pays a 32px inset each side, narrowing the table 64px and squeezing "What it covers" to ~173px so every topic pill wraps to its own line). And EVERY course carries a rating, so the identity cell is the same three lines on every row (name+`ag-rec` / stars+rating / "N reviews"), as the agent list always is — a 2-line cell beside a 3-line one is the ragged, uneven-row "broken structure" this fixed. E3/E4 read their figures off `ENROL_COURSE`; E1/E2 are `COURSE_RATE_PH`, an authored placeholder flagged like `COURSE_TOPICS`. (candidate, §114, Maryam 9 Sep 2026)
- BOOKING OPENS WITH PAYMENT, THEN THE CALENDAR (Maryam, 19 Sep 2026, two scenarios): a candidate books an open slot only after a payment method is on file, so the order is **Book → payment → payment-success confirmation → calendar → pick slot → booking confirmed**, REVERSING the old "calendar first, pay second" order and the 18 Sep "complimentary skips checkout" shortcut. The `agent:<k>` route (the marketplace card, the list "Book" button, the dashboard recommendation — all still emit `agent:<k>`, the DS builder markup is untouched) now lands on `V.checkout` (the PAYMENT step) FIRST; a NEW `cal:<k>` route reaches the calendar directly and is used by "Continue" after payment and by Reschedule (no re-charge). FIRST interview (complimentary): NO card is on file, so the payment screen shows an EMPTY-STATE placeholder (`.empty pm-empty`, the `creditCard` glyph + "No payment method added yet") — never a saved card; "Add a card" opens the Stripe modal (it does NOT auto-open, so the empty state is what the reader lands on), the page states "$0 charged today, card saved for next time". **While NO card is on file there is NO CTA** (Maryam 19 Sep 2026: "remove button when no payment method is added") — closing the add-card modal on the checkout view flips `S.pmAdded`, which replaces the empty state with the saved card (`cardPicker`) AND reveals the **"Continue to Pay"** CTA (was "Confirm ($0 today)"); `S.pmAdded` resets on each booking start. The button is gated `charged || S.pmAdded`. RE-interview (charged): the saved-cards picker (`cardPicker`), the button reads "Pay $X". Both carry `data-payok` → `S.paySuccess` → `paySuccessModal()`: the `.conf conf-ok` shell with a dedicated **Interview Fee row** (`.bkc-fee`/`.bkc-fl`/`.bkc-fv` — the amount is its OWN row, not buried in the copy; complimentary shows the standard fee STRUCK with "Free" via `ivFeeLabel(false, a.price)`, a re-interview shows the charged amount), the copy explaining without repeating the figure ("complimentary, no payment charged, method saved" / "payment successfully processed, continue to select a time"), and one **"Pick a slot"** `data-paygo` control → `cal:<k>` (Maryam 19 Sep 2026, was "Continue to scheduling"). `data-payok` does NOT book. `V.agent` (the calendar) is now the LAST step: no fee row, its foot button is "Confirm booking" (`data-book`) → booked dashboard + `ivBookedModal` ("Interview booked"). `data-book` clears `S.paySuccess`. (candidate, views.js `go`/`V.checkout`/`paySuccessModal`/`V.agent`/handlers, Maryam 19 Sep 2026)
- Booking's "Proceed to pay" no longer books; it goes to `V.checkout` — the card-picker + a "Pay $X and book" button that carries `stage:booked` (the old Proceed target). A card choice is inserted between the click and the commit; the destination is unchanged. (candidate, 6 Sep) — SUPERSEDED 19 Sep 2026 by the payment-first order above (`V.checkout` is now the first step, not reached from the calendar).
- A COMPLIMENTARY INTERVIEW BOOKING SKIPS THE CHECKOUT SCREEN and shows a SUCCESS MODAL on the booked dashboard (Maryam, 18 Sep 2026: "no need to show this screen, just show a modal of successful interview booking ... the screen the modal will open on should be the booked interview prototype screen"). `V.agent`'s Proceed button carries `data-book` when the interview is complimentary (`!ivCharged`) — it sets `S.ivBooked` and `setStage('booked')` (skipping `V.checkout`), and `ivBookedModal()` (the `enrolSheet` `.conf conf-ok` shell — success mark, "Interview booked", one dismiss control) renders on the dashboard, gated `S.ivBooked && (S.stage==='booked'||resched)`; the button/backdrop clear the flag onto the dashboard behind. A CHARGED re-interview still routes through `V.checkout` for the card picker, whose confirm now also carries `data-book` (same modal). (candidate, `data-book`/`ivBookedModal`, Maryam 18 Sep 2026) — SUPERSEDED 19 Sep 2026: payment now comes FIRST for every booking (complimentary too — the card is captured at $0), so nothing "skips checkout". `ivBookedModal` stays as the final "Interview booked" dialog raised from the calendar's `data-book`; `data-payok` on the payment step raises `paySuccessModal` before it.
- The RESCHEDULE stage (`resched`) draws the booked dashboard but the interview is TWO DAYS OUT and the action MOVES it (Maryam, 18 Sep 2026): `CALL_ROW.iv` returns `when:'in 2 days'` and a `resched` flag + agent `key` on `S.stage===RESCHED`; the black card's `.dc-when` reads **"In 2 days"**, and `crow` swaps the accent Join for a WHITE **"Reschedule"** button (`.crow-resched` — white ground §77, dark ink §63 at (0,5,0) to beat §63's dark-card `.btn:not(.btn-p)` white) that reopens the agent's booking calendar (`data-go="agent:<key>"` → `V.agent`). (candidate/`crow`/`CALL_ROW.iv`, Maryam 18 Sep 2026)
- The COMPLIMENTARY interview fee shows the agent's price STRUCK THROUGH with "Free" beside it, not "Complimentary" (Maryam, 18 Sep 2026: "show the price but cut it and write Free next to it"). `ivFeeLabel` returns `<s class="fee-was">$price</s> <span class="fee-free">Free</span>`; §63 mutes/lines-through `.fee-was` while "Free" keeps `.bkc-fv`'s accent. Booking-page fee value only (`.bkc-fv`); the rec cards and receipt still read "Complimentary". (candidate/`ivFeeLabel`/§63, Maryam 18 Sep 2026)
- THE DASHBOARD REC BLACK CARD SHOWS THE STRUCK PRICE, NOT "Next slot" (Maryam 21 Sep 2026: "instead of the next slot show the price that is $120 Free for the first interview, cut the $120 so the user knows it is free"). The `.rec-f` meta for a complimentary first interview drops both the "Complimentary" word and the "Next slot: …" chip and shows `ivFeeLabel(false, a.price)` — the agent's price STRUCK with "Free" — beside the length (`[💰 $95̶ Free][🎥 45 mins call]`). The price is `a.price` (the agent record — $95 for Priya, one price across every surface; it reads "$120" only if the record does). A charged re-interview is unchanged (fee · length · next slot). This UPDATES the 18 Sep "the rec cards still read Complimentary". (candidate/`talRec`/`ivFeeLabel`, Maryam 21 Sep 2026)
- The full-bleed CALL screen (`.call`) is in the rail-divider suppression list (§14, `.app:has(> .call)::after{content:none}`) with the other full-bleed scenes — else the stray left line draws down the video. (§14, Maryam 18 Sep 2026)
- The booking CHECKOUT (`V.checkout`) MIRRORS the booking page (`V.agent`) so moving from the Calendly card to payment only swaps the card, not the width: the SAME `.bkp` agent profile head on top (full width, its hairline below), then "Pay with" + `cardPicker()` + the pay button in the SAME 830px centred column the Calendly card sat in (`.bks-w`, §76.1b) — not rail-to-rail. The pay button sits inside `.bks-w` under the picker (`.bkpay-go`, left-aligned), not in `.bkc` (whose `margin-left:auto` right-aligns it). (candidate, §76, Maryam 9 Sep 2026)
- A disclosure's state is `S.disc` AND a DOM class; opening one must not reset the scroller. (§65)
- A row's arrow may carry a verb label (`.row-cta`) only when it is the one thing the page behind it is for. (§112)
- A tab row is `.sec.sec-cs > .cs`, never `.tabs` inside a padded `.sec`; a queue count rides the tab in `.lf-n`. (§15/§16, admin 4 Sep)
- A list page is: heading row (title + description, its buttons on the RIGHT end — NO search here), then the `.cs.lst-filt` tab strip, then a toolbar (`.lst-tools`) with the search on the LEFT and the table's own filters on the RIGHT, then the table. The heading row gets equal air above and below (`--s07` both). (§128, Maryam 7 Sep 2026)
- The tab strip keeps its TOP border on admin list pages: `.modhead + .sec > .cs` (§15) drops it under the Tal band, but the heading sits between them here, so it is restored. (§128, Maryam 7 Sep 2026)
- On the SUPER ADMIN portal a PAGE-HEADING DESCRIPTION (`aiHead`'s `.aih-d` lede) is the description grey `--text-secondary` **#828282** — the colour the Tal summary body carried before it was darkened one step platform-wide the same day (Maryam 15 Sep 2026: "the desc with the page heading needs to be in the font colour what tal summary desc colour is in right now"). The DS only colours `.aih-d` through `.sec > .sec-h.aih` (a DIRECT child, §63 §13), but admin list heads wrap it in `searchHead` (`.sec > .hd-srch > .hd-srch-t > .sec-h.aih`), so that rule never matched and the desc fell back to the inherited primary #111 (black). An admin-local rule (`.app .page .sec .sec-h.aih > .aih-b > .aih-d`, descendant so it covers wrapped and direct heads, (0,7,0) later than the DS base) pins every admin heading desc to #828282; the dark-card override (0,8,0, §63) still wins on a black-card heading. Admin-only, in `tn-admin-portal.html`. (§63, Maryam 15 Sep 2026)
- Admin tables follow the cohort leader's table whole (§31): header divider the light `--rule` (not `--rule-ink`); NO hairlines between rows (rows separated by air, `td border-bottom:0`); the first data row stands off the header by `--s06` (`tr:first-child + tr td`); cells `vertical-align:middle`; the first column is `--t-w-strong` (500) so the name is the anchor, its `idCell` caption sub-line staying book. (§128 + §63 §56, Maryam 7 Sep 2026)
- EVERY `.tbl` header row (all portals) is the SAME SIZE AS ITS CELLS, in BLACK STRONG: `.tbl th` moves off the eyebrow role (11.5px) to `--t-compact` (13.5px, `.tbl td`'s own size/lh/ls) at `--text-primary` and `--t-w-strong` (500 — "semibold" is 500, never 600; two weights only). `.chart-table th` stays the eyebrow (a chart's data readout, not a listing). (§63 §56b, Maryam 13 Sep 2026)
- EVERY `.tbl` data cell (all portals) is the DESCRIPTION GREY `--text-secondary` #828282 by default, EXCEPT the first column (the name anchor, pinned back to `--text-primary`) and any cell whose colour lives on its own element — `.statw` status green/amber/red, the `.t-caption` identity sub-line's floor grey, a `.tag` chip's own ink — which beat `.tbl td` (0,2,1) and are children, so none is named in the rule. (§63 §56b, Maryam 13 Sep 2026)
- A table's filters are OUR dropdowns (`.fdd`, never a native select), one per sensible categorical column NOT already covered by the tabs (e.g. Users/Candidates: Status); they write the page's filter state via `data-filter` and open downward. `S.fdOpen` is the open one; a choice or an outside-click closes it. Where the tabs already are the table's filter, the toolbar is search-only. A filter's option labels MIRROR the words the table's own column shows (Users say "Inactive", so the filter says "Inactive", not "Deactivated"). (§128, Maryam 7 Sep 2026)
- EVERY DROPDOWN ON THE SUPER ADMIN PORTAL OPENS DOWNWARD, never above (Maryam 12 Sep 2026: "dropdowns should always open below not above"). The filter (`.fdd`), form (`.fdd-form`) and rows-per-page (`.pgn-dd`) menus all use `top:calc(100% + --s02)` — the pagination menu was the one exception (upward, because it sits at the table foot) and is now downward too. The row-action kebab (`placeRowMenu`) is the one adaptive case that may still flip up as a last resort, only when opening down would hide options behind the floating Tal dock (§125). (127-pagination.css, Maryam 12 Sep 2026)
- A LIST TABLE's SEARCH FIELD carries a clear (X) button at its right end while it holds text (`.srch-x`, shown on `.srch-has`): pressing it empties the query and returns the full table, caret back in the now-empty field (`data-srchclear` → `setFilter(key,'')` + `SRCH_FOCUS`). The input reserves right padding so text never runs under the X; the X is one of the armed hovers (`HOVER_KEEP`). (128-adminlist.css + lstTools, Maryam 12 Sep 2026)
- The toolbar (search) row's air ABOVE and BELOW is equal — `--s07` (32) both sides. On a list page the operative rule for the gap ABOVE is `.app .cs.lst-filt + *{margin-top:var(--s07)}` (§120.4): the tab strip sits between the heading and the search, and that `+ *` rule (0,3,0) beats `.lst-tools`'s own `margin-top` (0,2,0), so the toolbar's own margin alone was not enough — the gap above read 16 against 32 below. Both are `--s07` now. `.lst-tools{margin-top:var(--s07); margin-bottom:var(--s07)}` carries the below gap and covers the no-tab case (heading→toolbar collapses to 32, unchanged). Across every admin list table. (120-admin.css §120.4 + 128-adminlist.css, Maryam 12–13 Sep 2026)
- NO NATIVE `<select>` ANYWHERE ON THE SUPER ADMIN PORTAL (Maryam 11 Sep 2026: "i do not want any system dropdowns anywhere ... update it everywhere"). Every `t:'select'` form field — sheet forms via `fieldHtml`/`recSheet` AND settings pages via `settingsForm` — renders through `formDd(f, val)`, our dropdown (reuses `.fdd*`, like `impRoleCell`, so no new CSS). It is DOM-only: a hidden `[data-f]` input that `readForm`/`readPageForm` read unchanged, and `data-formddtoggle`/`data-formddset` handled in the NON-rendering click listener — a pick must never `render()`, or the form (read from the DOM on save) is rebuilt and every other in-progress field is lost. Opening one adds `.dd-open` to its `.sheet-b` (overflow:visible) so the menu is not clipped. (tn-admin-portal.html, Maryam 11 Sep 2026)
- On the Super Admin portal a SUB-MODULE heading row has EQUAL air above and below (§128.1b/1c): the first content section after `.modhead` that LEADS WITH A HEADING (`> .sec-h`) takes `padding-top:var(--s07)` (32, same as a module's list page — overriding §16's `.sec:has(> .cs){padding-top:0}` that was pulling a heading tight against the band, e.g. Roles & Permissions), AND that heading's own bottom gap to its content is `--s07` too (`> .sec-h{margin-bottom:var(--s07)}`, Maryam 12 Sep 2026: "bottom space equal to the above space"), not the default `--sec-h-gap` (12). A strip-only section (no `.sec-h`) keeps hugging the band, and section headings deeper down keep the tight heading→content gap. (128-adminlist.css, Maryam 11–12 Sep 2026)
- The Users & Roles list filters by ROLE through a TAB STRIP (`filterStrip('role', …, ROLE_TABS())` with live counts), NOT a dropdown (Maryam 13 Sep 2026, reversing the 11 Sep dropdown — "take the role tabs as we had before ... remove the roles filter"). The tabs are the same role set (`ROLE_OPTS` = All / Candidate / Talent Agent / Cohort Leader / Admin / Employer — Admin is the `u.internal` bucket, Deactivated is the Status filter's job not a role); the toolbar keeps only the Status `.fdd`. When the Role tab is Cohort Leader, the table gains an "Added by" column: a cohort leader an admin added is a MODERATOR, `u.addedBy` is the adding admin's id, and the cell reads "You" for the signed-in Super Admin (ME) or the admin's name for any other (the "Added by" lives in the header, not repeated per cell; authored placeholder on the two leader accounts). (tn-admin-portal.html, Maryam 11 Sep 2026)
- EMPLOYERS ARE NOT PART OF THE SYSTEM FOR NOW, so the All Users screen has NO Employer tab AND NO employer rows (Maryam 19 Sep 2026: "remove the Employers tab entirely, as employers are not currently part of our system"): `usersAll` filters `ext` to `!u.internal && !u.roles.includes('employer')`, drops `'employer'` from `extTabs` (so the strip is All / Candidate / Talent Agent / Cohort Leader), and the head copy loses "and employer". So the "All" count and every tab exclude employers, not just the tab. The Employer role, its seed accounts and the (hidden) Employers/Finance modules are untouched — this is scoped to the All Users list. (tn-admin-portal.html, Maryam 19 Sep 2026)
- THE ROLES SUBMODULE IS A SINGLE INTERNAL-ROLES VIEW — no External category, no All/Internal kind tabs (Maryam 19 Sep 2026: "remove the External Roles category ... no need to maintain separate All and Internal tabs ... a single Roles view containing the roles added by the Super Admin ... treated as the system's internal roles"). `usersRoles` shows `ROLES.filter(r => r.kind === 'internal')` (Reviewer / Manager / Supervisor / Admin / Super Admin), drops the `.cs.lst-filt` kind strip and the conditional "Type" column, and the Permissions matrix columns follow the same internal set. External roles (candidate / agent / cohort leader) are self-serve product memberships, not admin-managed roles, so they are not listed. Every role added here is INTERNAL: `KINDS.role` drops its "Kind" field and create/update fix `kind:'internal'`, so a new role appears in the view. The Roles table carries NO row-select checkboxes (`table(…, {select:false})` — Maryam 20 Sep 2026: "remove the table checks from the roles table") even though it has a kebab column that would otherwise auto-draw them. The Permissions matrix edit button reads **"Update Permissions"** in edit mode (was "Done") and is DISABLED until a capability is toggled (Maryam 20 Sep 2026): `S.permsDirty` starts false on entering edit mode (`editperms`), is set true by `toggleperm`/`doperm`, and gates the button's `disabled`; leaving saves and clears it. (tn-admin-portal.html, Maryam 19–20 Sep 2026)
- NO LABEL COLUMN ANYWHERE ON THE SUPER ADMIN PORTAL — every section stacks its heading ABOVE its content, never §10.15's 184px spine beside it (Maryam 14 Sep 2026: "none of our sections are side by side heading and content, everything is vertically aligned"). Every existing admin section already opts out of the label column by its content (kv / tbl-wrap / tile-stack / stats / …); this is now UNIVERSAL so a section built on any other wrapper stays vertical too — an admin-local rule in `tn-admin-portal.html` restates §10.15's opt-out for `.app .main .sec:has(> .sec-h)` (display:block + the `> .sec-h` reset) at (0,4,0), INSIDE the `@container app (min-width:900px)` tier (trap 3). It caught the candidate-detail Interview scenes (`.scv-row`) and Quiz results (`.adm-quiz`), the only two sections whose wrapper was not on §10.15's list. When reusing a candidate-portal component here, do NOT re-derive its label-column opt-out — the admin has none. (tn-admin-portal.html, Maryam 14 Sep 2026)
- A NO-BACKGROUND heading-action button carries NO side padding, so the group's `--s05` gap alone sets the space between it and its neighbour (Maryam 13 Sep 2026: "remove the paddings of the buttons with no bg and then give them space the way it looks like ... between export and bulk correction"). A quiet `.btn-g`/`.btn-s`/`.btn-t` has the same 16px side padding as the filled `.btn-p` beside it but an invisible box, so the whitespace between two ghost labels (Export | Bulk Correction) was gap + both paddings (40px), reading wider than the ghost-label-to-filled-box-edge gap (24px). Zeroing the ghost padding equalises every visual gap in the row at the one 24px value. Scoped to the admin heading-action `.noic` ghosts only (`.aih-a`/`.sec-h-act`, §120) — §64's directional `:not(.noic)` go-button keeps its shaped padding. (§120, Maryam 13 Sep 2026)
- EVERY admin table (`table()`) grows a LEADING SELECT COLUMN — a checkbox on each row and a select-all in the header (Maryam 15 Sep 2026: "on all super admin portal tables I need a checkbox on each row and ... the header row for select all"). It is a `role="checkbox"` BUTTON (`selBtn` → `.tbl-cb` > `.tbl-cb-b`), never a native `<input>`: one click target that the router matches (`data-sel`/`data-selall`) BEFORE the row's `data-go`, so ticking a box never opens the record, and a pure function of `S.sel[key]` redrawn each render (no stale DOM checked-state). Selection is keyed like `S.pg` (`pageKey()#TBL_SEQ`); a row's key is its id / `go` / first-cell text (content-stable across paginate + re-sort); `TBL_ROWKEYS[key]` holds the full ordered key list the SELECT-ALL handler reads (it toggles the WHOLE dataset, every page). The box is a QUIET LIGHT-GREY control square with SOFTLY ROUNDED corners (`border-radius:5px` — a stated radius-0 exception, like the admin `.tag` chips; Maryam 15 Sep 2026 "give corner radius ... it does not look this edgy"): a light `--gray-30` edge on white AT REST (Maryam 15 Sep 2026 "the color of the checkboxes should be very light grey"), and `--accent` fill + white check when ACTIVE (Maryam 15 Sep 2026 "the active state of these icons will be in accent color like before"). Box and tick are a step small: 16px / 10px (Maryam "reduce the icon size"). A SELECTED ROW gets NO background — its first-column NAME turns `--accent-text`, exactly the hover affordance (Maryam 15 Sep 2026 "on selecting a row, do not give bg to the row just make the first column text accent colored just like ... the hover state"): `.tbl tr.sel td.tbl-sel + td` (0,4,3) beats the name re-pin. The select cell hugs the LEFT (no inset, 34px wide) so the box lines up flush UNDER THE SECTION HEADING, not centred ~19px in (Maryam 15 Sep 2026 "reduce the padding of the rows from the left ... off the alignment"). Because the NAME anchor moved to the second cell, the DS's `td:first-child` ink/weight and the admin row-hover accent are re-pinned to `td.tbl-sel + td`. `opts.select === false` opts a table out (none does). Admin-local CSS (`#admin-tbl-sel`) + `table()`/`selBtn`/router in `tn-admin-portal.html`. (Maryam 15 Sep 2026)
- WHILE ROWS ARE SELECTED, the floating Tal dock is REPLACED by a BULK ACTION BAR (Maryam 15 Sep 2026: "change the tal field into the action bar ... the actions that could be done with those selected rows"). It shows the actions that apply, icon + text, DYNAMIC BY COUNT: ONE row shows the per-record set (a cohort → Edit / Archive / Delete); TWO OR MORE show only what applies to many at once (Archive All / Delete All — no Edit, you cannot edit two at once). A table opts in with `opts.selectKind` (a `KINDS` key) and rows carrying `id`; `SEL_TABLES` registers each such table per render, `bulkCtx()` finds the first with a live selection, and `bulkActions(kind, ids)` DERIVES the buttons from the KIND — always Edit (`rec:`) and Delete (`askdel:`), plus a status toggle for a KIND listed in `BULK_STATUS` (cohort → Archive, agent → Pause, cand → Deactivate). Single Edit/status/Delete reuse the row handlers; the multi actions are `bulkstatus`/`bulkdel` → one `.conf` confirm → apply to every selected record via the KIND, then clear `S.sel[key]`. The bar is a content-width pill centred in the `.askdock`, with the Tal field's own three-layer shadow and a height that fits its content (not the dock's tall pill); a trailing × (`data-selclearall`) clears the selection. Wired on the **cohorts, agents and candidates** tables (`selectKind`); add `selectKind` + row `id` (and a `BULK_STATUS` entry if the kind has a status) to extend — Users/Staff/Roles are not wired because their delete/deactivate use custom (non-KIND) flows. Admin-local (`.actbar`, `#admin-tbl-sel`), `bulkBar`/`bulkActions`/`BULK_STATUS`/`bulkCtx` in `tn-admin-portal.html`. (Maryam 15 Sep 2026)
- A NON-KIND TABLE MAY DRIVE ITS OWN BULK BAR via `opts.bulk` — a `(ids) => [[icon, label, attr, cls?], …]` function returning its own buttons, for actions that act on something other than a record's KIND handlers (Maryam 15 Sep 2026). The **Transactions** table uses it for **"Download All"** (the `download` glyph) and **"Download as zip"** (a local `ADM_ZIP` = Material Symbols `folder_zip`, since the shared set ships no zip mark) — a prototype `bulktxdl` that toasts the count and clears the selection (no file leaves the browser, like the per-row "Download receipt"). `table()` registers a `bulk`-carrying table in `SEL_TABLES`; `bulkBar` renders a button's icon as a raw `<svg>` if the tuple's icon starts with `<`, else resolves it with `ic()` FIRST (the shared `I` proxy returns a truthy EMPTY svg for an unknown key, which blanks the mark — so `I[key]` can only be the fallback). (tn-admin-portal.html, Maryam 15 Sep 2026)
- THE SUPER ADMIN TRANSACTIONS TABLE HAS NO EMPLOYER ROWS (Maryam 20 Sep 2026, extending the 19 Sep employer removal from All Users into Finance): `financeTransactions` builds a `base = TXNS.filter(t => t.party !== 'Employer')` and every tab count + the row list read `base`, so employer-party transactions are dropped from the table. That empties the **Unlocks** and **Placements** tabs (both were all-employer), so those two tabs are REMOVED — the strip is now All / Courses / Interviews / Scenes / Payouts / Refunds. The "Candidate" column therefore only ever holds a real person, and the cell is unconditionally `personCell(t.who, '')` (the earlier `t.party === 'Employer' ? idCell(...)` branch is dead and gone); `t.party` is also dropped from the search `hit`. This SUPERSEDES the 20 Sep "an EMPLOYER row stays the plain `idCell(t.who, t.party)`" handling. The employer seed rows stay in `TXNS` unread by this table (the hidden Employers module is untouched, per 19 Sep). (tn-admin-portal.html, Maryam 20 Sep 2026)
- THE SUPER ADMIN TRANSACTIONS TABLE HAS A **"Scenes"** TAB (Maryam 20 Sep 2026: "add a dedicated Scenes tab ... whenever a candidate purchases additional interview scenes, record and display it here, separately from other transactions"): a `cat:'scene'` category between Interviews and Payouts — a candidate buying additional saved scenes from their own interview, beyond the free set. Same row structure and `methodMark` treatment as every other category (person cell + real photo, brand-icon method, `tag` status); the "Course" column shows the description as written ("Interview scenes ×N"). Priced by a new `FEES` line (`k:'scene'`, $15 per scene, set). Seed: three candidate scene charges (Sophia Ali ×2 / Liam Chen ×1 / Kai Andersen ×3) — authored placeholder (§74, the seed has no real scene-purchase telemetry). (tn-admin-portal.html, Maryam 20 Sep 2026)
- A PAYOUT HAS NO "PENDING" STATUS — its in-flight state reads **"In progress"** (Maryam 21 Sep 2026: "payouts will not have a pending status, it could be 'In Progress'"). The money is on its way to the agent's bank, so the payouts set is **In progress · Paid · Failed**. Scoped to the payout view by a `tag()` LABEL override, not a new status key — the money-in charges keep "Pending": the Transactions table passes `t.cat === 'payout' && t.status === 'pending' ? 'In progress' : null`, and the Payouts *module* passes `p.status === 'held' ? 'In progress' : null` (its pending bucket keeps the `held` key). The amber `warn` hue is unchanged (both `pending` and `in progress` map to it via `TAGV`). (tn-admin-portal.html, Maryam 21 Sep 2026)
- THE SUPER ADMIN TRANSACTIONS STATUS SETS ARE DEFINED PER CATEGORY (Maryam 20 Sep 2026): money-in charges (**Courses / Interviews / Scenes**) are `Received` · `Pending` · `Failed`; **Payouts** are `Pending` · `Paid` · `Failed`; **Refunds** are `Pending` · `Refunded` · `Failed`. Colours via `TAGV`/`tag()`: `received`/`paid`/`refunded` green (good/done), `pending` amber (was `held`, which is retained only for the separate Payouts *module*), `failed` red. `refunded` now carries green (it used to fall through to plain uncoloured text). The seed TXNS rows were re-mapped to these keys (no more `settled`/`held`/`blocked` on the money-in/payout/refund rows shown in the table); the two figure helpers that read a TXN's escrow state (`inProgress`, and the deactivation "mid-flight payment" check) were switched from `'held'` to `'pending'` to match. (tn-admin-portal.html, Maryam 20 Sep 2026)
- THE SUPER ADMIN TRANSACTIONS TABLE renames two columns (Maryam 20 Sep 2026): "Who" → **"Candidate"** (a person cell carries the real photo via `personCell(t.who, '')` — see the no-employer-rows rule above), and "Description" → **"Course"** with the leading "Course — " label stripped (`t.kind.replace(/^Course\s*[—–-]\s*/,'')`) so a course row reads as the course name alone. A non-course description (payout) is left as written. (tn-admin-portal.html, Maryam 20 Sep 2026)
- A FAILED TRANSACTION CAN ONLY BE DELETED (Maryam 20 Sep 2026: "a failed payment means the payment didn't receive ... allow only delete action on such transactions"). A `status === 'failed'` row moved no money (a charge that never cleared, or a payout that never sent), so it has nothing to receipt or refund: its kebab shows a single **Delete** action (`asktxndel:<id>` → a `.conf` confirm "Delete <ref>?" → `dotxndel` splices it from `TXNS` + a Medium audit line; no step-up). Every non-failed row keeps Download receipt + (money-in, not already refunded) Refund. A failed charge is also removed from the **Issue-a-Refund** form's selectable list (`opts` filters `x.status !== 'failed'`), so it can't be refunded there either. Applies across all tabs (money-in charges and payouts). (tn-admin-portal.html, Maryam 20 Sep 2026)
- AN INTERVIEW'S "KIND" IS CALLED **"Type"** EVERYWHERE ON THE SUPER ADMIN PORTAL (Maryam 21 Sep 2026: "interview didn't have a kind but a 'Type', so replace the interview kind with type"). Every interview-facing LABEL that read "Kind" now reads "Type" — the Interviews register filter dropdown (`filterDd('bkkind', …, 'Type')`, "Any type"), the `KINDS.booking` create/edit field (`{k:'kind', l:'Type', …}`), the agent-detail Upcoming/Past interview tables' column header, the upcoming interview detail's `admFact` fact, and the recording-detail kv. The underlying data key stays `b.kind` and the value strings ("Interview" / "Re-interview" / "First (discounted)") are unchanged — only the visible label moved. NON-interview "Kind" labels are untouched: the Rulings Register decisions filter (blocked/open) and the grant-revoke revocation-kind field. (tn-admin-portal.html, Maryam 21 Sep 2026)
- THE INTERVIEWS MODULE (`market/bookings`, `marketBookings`) HAS TWO TABS — **Upcoming** and **Awaiting Approval** (Maryam 21 Sep 2026: "i need 2 tabs here, Upcoming and Awaiting Approval ... take back the upcoming interviews ui to the table ... the awaiting approval are the ones where the talent agent has sent the recording, level recommendation, and their notes, and super admin could see the tal's read as well, and super admin has to approve or override this decision"). A `filterStrip('bktab', 'upcoming', …)` sits under the heading; the search + the **Type** (`bkkind`) and **Talent agent** (`bkagent`) `filterDd`s serve both tabs. **UPCOMING** is the read-only register TABLE of booked interviews (`bkStage === 'booked'`): Candidate · Agent · Type · Scheduled · Fee · Escrow, `{select:false}`, no navigation — the same table that predated the 21 Sep diary list, which is REVERTED (the `.bk-row` diary, `ivChip`/`ivEscrowNote` are gone). **AWAITING APPROVAL** is the queue of submitted interviews (`pendingApprovals()` = `PAST_IVS` with `decision:null`): a table Candidate · Agent · Recommended level (`lvlChip(agentLevel)`) · Tal's suggestion (`lvlChip(talLevel)`) · Submitted · Actions, each row → `market/approval/<id>`. **THE APPROVAL DETAIL** (`marketApproval`, hidden sub `approval` in MODULES) shows: a "Recommended level" band (agent's recommendation, Tal's suggestion, submitted/final, candidate rating) over the 15-rung `admLadder`, with **Approve {level}** (`askapprovelvl` → `.conf` `mk:'check'` → `doapprovelvl`) and **Override Level** (`SHEETS.overridelvl` → a `formDd` level picker + reason → `dooverridelvl`); then `recPlayer(iv)` (the recording), Tal's read (`.tal-eval`, `iv.tal`) and the agent's notes (`ivEvalBlock` — strengths / growth / note). A decision sets the candidate's level (`candByName(iv.cand).level`), writes a High audit line, and drops the interview from the queue; a decided interview shows the outcome instead of the buttons. `agentLevel` / `talLevel` / `submitted` / `decision` / `finalLevel` are authored placeholder on `PAST_IVS` (§74; piv4 is the agent-vs-Tal-differ case). This aligns with the 15 Sep client ruling — the agent recommends, TalentNext confirms admin-side. (tn-admin-portal.html, Maryam 21 Sep 2026)
- ~~THE INTERVIEWS MODULE IS A LIST OF BOOKED INTERVIEWS, NOT A TABBED REGISTER TABLE~~ (SUPERSEDED 21 Sep 2026 by the two-tab rule above — the diary list was reverted to the Upcoming table). Original ask (Maryam 21 Sep 2026: "show only the booked interviews by different talent agents ... no tabs, no tables, just keep the search and filter should be by type and by talent agent name"; off her reference — the agent portal's booked-diary row). It is scoped to `bkStage === 'booked'` (held / reported / not-held / resolved no longer appear), and the whole page is: the `searchHead` heading + search, a toolbar with **two `filterDd`s only — Type** (`bkkind`: Any type / First interview / Re-interview) **and Talent agent** (`bkagent`: Any agent + each `agentNames()`), and a `tile-stack` of **`.bk-row`** rows (the DS diary row, §31.5) — a left day/time column against a hairline (`ivChip` parses `b.slot`), a round candidate photo (`agentFace(b.cand)`), a "**{Type} · {Candidate}**" title (First interview / Re-interview via `agIvIsRe`), a "**{agent} · 45 minutes, recorded · {escrow note}**" sub-line (`ivEscrowNote`: complimentary / "$X already in escrow" / released / refunded), and a trailing arrow → the interview detail (`agents/interview/<id>`, which resolves a BOOKINGS id). The status tab strip (`filterStrip('bkstage', …)`), the stage/escrow **table**, the **Needs-attention** toggle (`attnToggle`) and the per-row Resolve action are all REMOVED from this module (Resolve/held handling lives off this list). This SUPERSEDES the 20 Sep register-table columns below (Candidate · Agent · Type · Scheduled · Fee · Escrow · Actions) — that table no longer exists; the naming rule below still holds. (tn-admin-portal.html, Maryam 21 Sep 2026)
- THE MARKETPLACE "BOOKINGS" SUB-PAGE IS NAMED **"Interviews"** (Maryam 20 Sep 2026: "instead of bookings name this module Interviews"). Display label only — the nav sub-item title, the `searchHead` heading, the export label, the empty state, the dashboard quick-link and the portal-switcher summary all read "Interviews"; the ROUTE KEY stays `bookings` (`market/bookings`, `PAGESUM['market/bookings']`, `V.market`'s `bookings:` view, `KINDS.booking`) so no hash link or state key breaks. ~~The parent module stays "Marketplace & Interviews".~~ **REVISED 21 Sep 2026 (Maryam: "the top header ... should be 'Interviews' and the page heading should be 'Upcoming Interviews'"):** the parent MODULE title is now **"Interviews"** (`MODULES` market `t`, the app-bar crumb), the bookings SUB-NAV label stays **"Interviews"** (Maryam 21 Sep 2026: the rail flyout should read "Interviews", not "Upcoming Interviews" — so the rail is Interviews › Interviews / Recordings & Transcripts), and only the bookings PAGE HEADING (`searchHead`) is **"Upcoming Interviews"**. The type/agent toolbar filters default to **"All"** (was "Any type" / "Any agent"). Route key still `bookings`. The register table carries NO row-select checkboxes (`table(…, {select:false})`) — it is read-only with no bulk actions, so selection serves no purpose (Maryam 20 Sep 2026: "none of the tables on the interviews sub module should have the checkboxes"). Its columns are trimmed (Maryam 20 Sep 2026): person cells carry NO email sub-line (`personCell(b.cand, '')` / `personCell(b.agent, '')`), the **Kind** column is renamed **"Type"** with the "(discounted)" parenthetical stripped (`b.kind.replace(/\s*\([^)]*\)/,'')` → "First" / "Interview" / "Re-interview"), and the **Stage** and **Report due** columns are removed — leaving Candidate · Agent · Type · Scheduled · Fee · Escrow · Actions. (tn-admin-portal.html, Maryam 20 Sep 2026)
- THE INTERVIEWS TAB HAS ITS OWN COLUMNS (Maryam 20 Sep 2026): `financeTransactions` builds `cols`/cells conditionally on `cf === 'interview'`. On that tab the "Course" column becomes **"Interview"** and shows the interview TYPE only ("Interview" / "Re-interview"), and a new **"Talent Agent"** column sits next to it with the agent's name AND photo (`personCell`). The `kind` is "Interview — <agent>"; split on the SPACED dash (`/\s[—–-]\s/`) so the agent name comes off while "Re-interview" (its own hyphen has no surrounding spaces) stays whole. Every other tab keeps the 7-column Course layout; `table()` sorting is off so the differing column count needs no `sv` realignment. The agent name stays searchable via `t.kind`. (tn-admin-portal.html, Maryam 20 Sep 2026)
- THE SUPER ADMIN TRANSACTIONS METHOD COLUMN SHOWS THE BRAND ICON ONLY, never the card number (Maryam 20 Sep 2026: "represented only by its payment provider/card icon ... Do not display the card number, last four digits ... use real coloured icons"). `methodMark(t.method)` renders the provider's real brand logo from `METHOD_ART` (SVG recreations of Maryam's reference logos, 20 Sep 2026 — a pasted image can't be embedded, so the marks are drawn in brand colours, no surrounding card box): **VISA** the classic navy wordmark with the gold flag; **Mastercard** the bare red/amber interlocking discs; **Amex** the blue box (not in the reference set, kept for Amex rows); **Stripe** the `#635BFF` wordmark, shown for an ACH / bank transfer (`METHOD_ART.ach`/`.bank` = the Stripe mark — a bank transfer carries no card brand, so the processor's mark stands in). Keyed by `methodBrand()` (the first word of `t.method`, before the bullet); an unknown provider falls back to the brand word, none to a dash. The last-four in `t.method` is never rendered AND never searchable (the search `hit` reads `methodBrand(t.method)`, not the full string). Scoped to the Transactions module only; payouts/invoices/statements keep their own columns. Admin-local (`METHOD_ART`/`methodMark`, `.pay-mk` CSS). (tn-admin-portal.html, Maryam 20 Sep 2026)
- THE SUPER ADMIN PAYOUTS PAGE (`financePayouts`) drops the manual REVIEW-HOLD (Maryam 15 Sep 2026: "remove hold option ... the hold state will be removed from wherever it is mentioned"): the `review` status, the "Hold For Review" / "Clear Hold" buttons and their handlers (`askhold`/`dohold`/`askclearhold`/`doclearhold`), and the "Under review" tab are all gone; a payout is either pending settlement or settled. The `held` STATUS KEY stays (the pending-settlement bucket the Release action operates on) but DISPLAYS as **"Pending"** everywhere via `TAGLABEL.held='Pending'` (payouts + a transaction in escrow), keeping the amber hue — so the word "Held"/"hold" is off the UI. The tabs are just **"Payments"** (the `held` bucket, renamed from "Held" and the default) and **"Settled"** — NO "All" tab (Maryam 15 Sep 2026). The per-row **Release** and the header **"Release All"** (renamed from "Release All Held") are NO-BACKGROUND ACCENT LINKS — `.acc-link` (transparent, `--accent-text` label + accent arrow, flush to the edge), not the black `.btn-p` (Maryam 15 Sep 2026: "remove the black bg of the release button and give the text and arrow the accent color"). Seed: the two `review` payouts became `settled` (a $0 first-interview discount) and `held` (a net still owed). (tn-admin-portal.html, Maryam 15 Sep 2026)
- On the SUPER ADMIN portal a headed section's HEADING SITS FLUSH WITH ITS CONTENT on BOTH sides (Maryam 15 Sep 2026: "the roles heading row has extra padding from both left and right sides, please align them with other content"). The universal label-column opt-out rule (`.app .main .sec:has(> .sec-h) > .sec-h`, (0,5,0), later than the DS link so it decides the heading's padding) had copied the DS opt-out's `padding:0 var(--pad-x)`, which DOUBLE-inset the heading — the section already pads `--pad-x` and the table/kv content sits flush at that edge — so the title read 32px right of its table and a heading action (New Role, Add User…) 32px left of the table's right edge. It is `padding:0` now: title on the content's left edge, the action button on the content's right edge; the tab strip (`.cs.lst-filt`) still bleeds past both by design. (tn-admin-portal.html, Maryam 15 Sep 2026)
- EVERY SUPER ADMIN DETAIL PAGE PUTS ITS HEADER OPTION ACTIONS IN A KEBAB MENU, never a strip of inline buttons at the top-right (Maryam 18 Sep 2026: "i told you millions of times that i do not want option actions on detail pages on the top right, use the menu icon and show these actions in a menu dropdown … do this on the whole admin portal"). The head block's actions (Edit / Delete / Archive / Deactivate / Verify / Approve / Publish / Reset Password / View As / Grant SA / Change Role / Download Transcript / etc.) are built as `rowActs([[icon,label,attr], …])` and passed into the `aiHead` act slot (or `.idhead-act`), exactly like the candidate detail's kebab. A STATUS/STATE tag is NOT an option — a `tag()`/`verifyTag()`/`leadStageTag()` state chip, an "Active · week N"/"Archived" tag, a "Deactivated" tag, or a "protected" badge stays VISIBLE, placed before the kebab in a `display:flex;gap:var(--s03)` span. This is portal-wide: `candidatesDetail` (the reference), `usersStaffDetail`/`staffDetailView`, `agentsDetail`, `cohortsDetail`, `cohortsCourseDetail`, `marketRecordingDetail`, `employersDetail`, `vettingAppDetail`, `vettingEmpDetail`, `crmLeadDetail`, `crmCampaignDetail`. (tn-admin-portal.html, Maryam 18 Sep 2026)
- ON THE SUPER ADMIN PORTAL A DETAIL PAGE'S BACK ARROW RETURNS TO THE SCREEN YOU CAME FROM, not a hardcoded list (Maryam 21 Sep 2026: an interview opened from the Interviews list sent Back to the agent detail; "walk through the whole portal and check back arrows"). A `S.hist` stack (added to state) mirrors the candidate portal's §78: `go()` pushes the route it is leaving (skipping no-ops and consecutive duplicates, capped at 30); `goBack(fallback)` pops it and navigates WITHOUT pushing (a back that pushed would ping-pong). `detailHead`'s `.ph-back` button is now `data-back="<fallback>"` (the fixed list it always carried), and the click router calls `goBack(d.back)` — so the arrow returns to the previous screen and only falls back to the fixed list for a deep link with an empty stack. One fix covers every `detailHead` page portal-wide. (tn-admin-portal.html, Maryam 21 Sep 2026)
- A row's actions are ONE kebab (`overflow` glyph) under an "Actions" column, opening a menu of labelled rows (`.rowmenu` → `.rowmenu-list` of `.rowmenu-i` icon+label), never a strip of separate icon buttons. `S.rowMenu` is the open id; the item keeps its own `data-*`, so a press still runs the action and closes the menu; outside-click closes it. `placeRowMenu` lifts the open list out of the sideways-clipping `.tbl-wrap` into `.main`, opens it up or down by the room below the kebab (the Tal dock's top is the usable foot), and clamps it inside the frame — never `position:fixed` (the prototype frame's fit-to-pane transform flips its containing block). A same-page `render()` preserves `.main` scroll and every `.tbl-wrap` scrollLeft, so opening a menu never jumps the table. (§125, Maryam 7 Sep 2026)
- A table shows ONE page at a time — 5 rows by default, resizable (5/10/25/50) — never the whole list as a block. `table()` slices after sorting; the `.pgn` bar (rows-per-page · range · prev/numbers/next) sits under the table, outside `.tbl-wrap`. State is `S.pg[key]`, keyed by page AND a per-render table index (`TBL_SEQ`) so two tables on one page paginate independently; a size change resets to page 1; the page clamps when a filter shrinks the list; no bar when the list fits the smallest page (≤5). Rows-per-page is OUR dropdown (`.pgn-dd`, never a native select) — the trigger + a menu that opens DOWNWARD like every other dropdown (Maryam 12 Sep 2026, was upward); `S.pgOpen` is the open one, a choice or an outside-click closes it. Page numbers are `.btn`, small ROUND, the active one a red circle (`.pgn .btn-p{background:--accent;border-radius:999px}` — like the count badges); §63 owns the type, §64's trailing arrow is suppressed. (§127, Maryam 7 Sep 2026)
- A table's ROW-SELECT CHECKBOXES appear ONLY on tables that have a row-action (kebab) column (Maryam 15 Sep 2026): `table()` derives `doSel` from `cols.some(c => c.act)` — a read-only table (no actions) draws no select column, header or per-row. `opts.select` still forces it either way. This reverses the 15 Sep "a checkbox on every table". (`table()` in tn-admin-portal.html)
- A table's COLUMNS DIVIDE THE FULL WIDTH — the `<colgroup>` is PROPORTIONAL (percentages of the content-sized total, sel column included), so `width:100%` fills the container instead of leaving space on the right, while the columns keep their content-proportional ratios stable page to page; `min-width` (the px sum) still lets `.tbl-wrap` scroll a genuinely-too-narrow table sideways. (Maryam 15 Sep 2026, `table()` in tn-admin-portal.html)
- THE SUPER ADMIN INTEGRATIONS LIST BREATHES — its `.cardrow` service rows carry a symmetric `--s06` (24) top and bottom (up from the shared 12) with `min-height:0`, so every row has equal air above and below its content and the divider (Maryam 21 Sep 2026: "add some gaps between all rows and dividers equally"). Scoped to that one list via `.intg-stack` on the `.tile-stack`, admin-local (`#admin-row-hover` block), so no other `.cardrow` list moves. (tn-admin-portal.html, Maryam 21 Sep 2026) — THE SERVICE-ICON CHIP CARRIES A LIGHT-GREY GROUND, `--layer-02` #f7f7f7 (Maryam 21 Sep 2026: "the bg of these icons should be light grey"): `.cardrow-ic` ships transparent, and the DS zeroes it for any `.sec:has(> .tile-stack)` list at (0,4,0), so an admin-local `.app .main .sec:has(> .intg-stack) .cardrow-ic` restores it at (0,5,0) — a deterministic win by weight, no `!important`. Scoped to this list, so the dashboard's attention/decision `.cardrow-ic` marks stay bare §29 glyphs. (tn-admin-portal.html, Maryam 21 Sep 2026)
- THE INTEGRATIONS "Connect a service" FORM OPENS AS THE RIGHT SLIDER, not a centred modal (Maryam 21 Sep 2026: "this form should also come as the right slider"). `'connect'` is added to `SIDE_SHEET_KEYS`, so `sheetView`/`sidePeek` render it as the in-flow `.peek` column like Add User / Add Staff. (tn-admin-portal.html, Maryam 21 Sep 2026)
- NO DIVIDER AFTER THE TOP SECTION BLOCK on any Super Admin detail screen — the identity/facts block right after the head band (`.modhead + .sec`). §14 draws a foot rule on any section that precedes a heading; an admin-local rule (`[data-portal="admin"]`, inside the ≥900 tier, `:not(#_)` to clear §14's high `:has()`/`:not()` specificity without `!important`) suppresses just the top block's. (Maryam 15 Sep 2026, tn-admin-portal.html)
- NO NATIVE DATE INPUT ON THE SUPER ADMIN PORTAL — every `t:'date'` field renders through `formDate`/`dateGrid` (our compact month-grid popup, opening on the RIGHT under a calendar icon), the same principle as `formDd` for selects. DOM-only (never `render()`): picking writes the hidden `data-f` input as ISO `yyyy-mm-dd` (what native stored) and an MM-DD-YYYY trigger label; the viewed month lives in the popup's `data-ym`. `data-datetoggle`/`data-datenav`/`data-dateset`. (Maryam 15 Sep 2026, tn-admin-portal.html)
- A `formDd` SELECT MAY START UNSELECTED, AND A RECORD SHEET'S SUBMIT BUTTON GATES ON ITS REQUIRED FIELDS (Maryam 20 Sep 2026: "nothing should be selected automatically in the first 2 fields ... in that case the button will be disabled"). A `t:'select'` field that carries a `ph` renders EMPTY — `formDd` shows the `ph` as placeholder text (`.fdd-val.ph`, `--text-placeholder`) and the hidden `[data-f]` holds `''` — instead of auto-picking the first option; a select WITHOUT `ph` still defaults to `opts[0]`, so this is opt-in and no existing select moved. `recSheet` returns `gate:true` + an initial `disabled` (any required field empty at open), and the footer submit button is stamped `data-submit="1"`; `syncSheetSubmit()` re-checks the same required-field rule `saveRecord` enforces and toggles `btn.disabled` — called after every `render()` and, WITHOUT a render (trap 9), on every dropdown pick (`data-formddset`), date pick (`data-dateset`) and keystroke (the `input` listener, gated to `.sheet-b [data-f]`/`.peek-form [data-f]`). So a sheet opens with its button disabled until every required field is filled and disables again if one is cleared; an EDIT sheet (fields prefilled) opens enabled. The refund sheet has TWO entry points (Maryam 20 Sep 2026): the generic **"Issue a Refund"** button opens BLANK ("do not select the payment refund automatically"), while a specific **row's Refund action pre-fills that charge and its amount** ("the form should have that record's selections already"). A dedicated flag `S.refundTxn` — set ONLY by `startrefund`, cleared by the generic `data-sheet` open and on sheet close — disambiguates the two (no reliance on a lingering `S.id`); `fields` reads it for the `txn`/`amount` defaults. `rtype` (Type) stays blank in both cases — it is a refund attribute, not a property of the charge. The submit button still gates on all required fields, so a pre-filled row refund is enabled only once Type + Reason are added. **The placeholder modifier class is `.fdd-ph` (and the date picker's is `.fdate-ph`), NEVER a bare `.ph`**: `.ph` is the DS page-header component (`padding:40px 16px 24px` + decorative `::before`/`::after`), so a bare `.ph` on `.fdd-val` inherited that and rendered the field ~87px tall with a big gap under the label — the "odd/broken" Type field (Maryam 20 Sep 2026). Portal-wide for record sheets, admin-local. (tn-admin-portal.html, Maryam 20 Sep 2026)
- A DROPDOWN OPTION MAY CARRY A FACE — `formDd` options are `[value,label]` or `[value,label,imgSrc]`; the 3rd element renders a round avatar (`fddAv`) in the option and the trigger (`vl.innerHTML`, not textContent). The Leader picker (`leaderOpts`) shows each leader's `agentFace`. A MEMBER ROW's avatar takes a photo via `memRow(...,img)` → the DS's `.mem-av.mem-ph > .av-ph > img` structure (NOT a bare img — the DS's `.mem-av.mem-ph{width:auto}` collapses one). (Maryam 15 Sep 2026)
- COURSE COVERS + COMPLETION-CERTIFICATE BADGES now ship in the design system — `COHORT_ART` and `CERT_ART` embedded by `build-ds.py` off `hifi/build/cohorts`+`/certs` (like `PF_ART`), in `DS_HAVE`. The Super Admin candidate detail draws a Courses accordion (cover + name + status; a completed course opens to its black completion-certificate card + the four-figure insight band) like the candidate portal's My Courses; each cohort member shows a per-member course (`memCourse`). (Maryam 15 Sep 2026)
- THE SUPER ADMIN COHORT FORM (`KINDS.cohort`): a "Maximum members" field (`cap`, default 10 — every "/10" reads `c.cap||10`), the "Cycle start" date field uses our date picker, the create button reads "Create Cohort", and the list's add button reads "Add New Cohort". The cohort DETAIL shows a four-block `.facts pf-facts` band (Leader / Cycle start / Progress / Members — no Length row), NO Interventions section, and a "Cohort Members" roster (photos + per-member course; each member's kebab has Open record / Move / Remove — Move opens `moveco:cohort:member` with the member locked). The Course Catalogue drops "Import From Library" and its "Add Course" hands off to LightSpeed (no in-portal add). Cohort Leaders (`cohortsAssign`) is a TABLE (no bootstrap-paradox banner; tabs All / Awaiting Interviews / Interview Pending; the "All"/approved pool carries no recommendation prose). (Maryam 15 Sep 2026)
- THE SUPER ADMIN COHORT LEADERS TABLE (`cohortsAssign`) treats its rows AS cohort leaders (Maryam 15 Sep 2026): the first column is **"Cohort Leader"** (was "Candidate"), and a **"Current cohort"** column names the cohort(s) each currently leads — `leaderCohorts(name)` = any non-archived `COHORTS` whose `leader` (minus its "(bucket)" suffix) is this person (a candidate-leader leads one, a moderator several), or "—". Row actions: **"Allot a cohort"** shows ONLY for a leader not currently leading one (gated on `leaderCohorts(...).length`); the removal action is **"Remove as cohort leader"** (was "Decline") — `askremovelead`/`removeAsLeader` revokes the leader (any cohort they lead returns to Unassigned, the `leader` role comes off their account, they leave the queue; High audit). The table also drives its own **bulk action bar** via `opts.bulk` — "Remove as cohort leader" over the selection (`bulkleadremove` → one `.conf` → `removeAsLeader` each); rows carry `id:r.id` so the select keys are ids. (tn-admin-portal.html, Maryam 15 Sep 2026)
- THE SUPER ADMIN COURSE DETAIL (`cohortsCourseDetail`, `cohorts/coursedetail/<id>`): NO Tal summary (`detailHead` with no `sum`, back arrow only). It leads with an `.idhead` carrying the course COVER on the LEFT of the name (`.crs-idphoto`, a 96×64 rectangular artwork thumb via `courseArtAdm` off the first mapped level — a course is not a person, so no disc), name only, NO track subtitle (anyone can take the course, so which track it is for is not shown). The actions ride the right (`.idhead-act`): Edit and **"Delete Course"** (was "Delete"); the RETIRE button is gone (a live course is retired from the catalogue kebab), Publish stays for a draft. The Track / Maps-to-levels / Content rows are removed; Price / Length / Status show as THREE blocks in a row — the candidate-detail `.facts pf-facts` band with a `.crs-facts` 3-across modifier (repeat(3,1fr) at ≥900, so the row fills with no empty trailing cell), Status the `tag()` word in the third cell. The EDIT window (`KINDS.course.fields`) drops the Track and Maps-to-levels fields to match — name / price / length / status only; the seed's `track`/`levels` keys stay on the record (the catalogue Track column and the cover derivation still read `track`), and `create` defaults `track:'Explorer'` so a course still resolves a cover. (tn-admin-portal.html, Maryam 15 Sep 2026)
- THE SUPER ADMIN LEVELS & BUCKETS page (`cohortsLevels`): FIVE levels per bucket (E1–E5 / B1–B5 / T1–T5, `[1,2,3,4,5]`, was three). The divider goes AFTER each bucket, not between its level rows (Maryam 15 Sep 2026): each bucket is a `.lvl-bucket` whose child `.kv` rows drop their per-row hairline (`border-bottom:0`) and which carries one `--rule` line below the block (`:not(:last-child)`), so a bucket reads as one clean list. (tn-admin-portal.html, Maryam 15 Sep 2026)
- A paginated table's COLUMN WIDTHS HOLD ACROSS PAGES — the columns must not re-size to each page's content as you page through (Maryam 12 Sep 2026). `table()` sizes the columns ONCE off the whole dataset (`ordered`, every row, not the visible slice) — a text-length estimate per column, header included, clamped 72–340px (the kebab `act` column a fixed 56) — and pins them with `table-layout:fixed` + a `<colgroup>` on `<table class="tbl" style="width:100%;min-width:<colSum>px">`. Same colgroup on every page → the page-1 grid, held steady. `width:100%` fills the container when it can (columns scale up on the same ratios, no gratuitous scroll); `min-width` lets `.tbl-wrap` scroll sideways when the container is genuinely too narrow, never squeezing a column below its content width. Different tables get different, content-proportional profiles; within one table every page is identical. (`table()` in tn-admin-portal.html, Maryam 12 Sep 2026)
- THE SUPER ADMIN PORTAL IS ALIGNED TO THE USER-STORIES DOC (Talentnext_UserStories, 11 epics / 50 stories; Maryam 15 Sep 2026, "align our super admin portal according to these user stories"). Build every story's flow on EXISTING components, keep copy scoped to the doc (no filler sections, no invented info). Two standing rulings override the doc: (1) ~~the talent agent's call on a candidate's level is FINAL~~ **REVERSED 15 Sep 2026 (client): the agent does NOT set the level — Tal proposes and the final level is set admin/moderator-side after analysis.** The Levelling decision surface now LIVES in the admin portal (Maryam's active build); the agent portal owns none of it. See §3 "Talent Agent — V1 model". The candidate detail still shows level history, and the admin sets the level rather than only overriding it. (2) The only hidden nav modules are **Levelling and Vetting & Approvals**; everything else is surfaced (CRM, Employers, Finance Transactions/Payouts, the System tree incl. Audit Log). Drill-in detail pages (staffdetail/detail/coursedetail/…) stay hidden — opened from a row, not the rail. Candidates and Talent Agents stay as their own top-level modules (not folded into one Users directory). (MODULES, Maryam 15 Sep 2026)
- THE SUPER ADMIN "ONBOARDING QUESTIONS" MODULE is a FORM BUILDER for the intake quiz (Maryam 22 Sep 2026). It sits **directly after Interviews** in `MODULES` (`{k:'onboarding', t:'Onboarding Questions', ic:'document'}` — `ic:'document'`, NOT `quiz`: the DS icon set has no `quiz` glyph so it rendered blank; check `ic(name)` resolves to a path, not the empty-svg fallback, trap 7). One visible sub `questions` (rail leaf), no hidden page — the editor is the **right slider**. `ONBOARDING_Q` holds the questions (`{id, text, type, weight, options:[{label, score}], placeholder?, other?}`), seeded with the client's five. **Three answer types** (`OB_TYPES`): `single` (scored options + a per-question weightage), `text` (one-line free text with a placeholder, no weight), `dropdown` (a fixed option list, unscored, optional "Other" with a text field). The list (`onboardingQuestions`) is a `table()` inside **`<section class="sec">`** (every admin list is wrapped — without it the heading loses the `.modhead + .sec` top-padding and §128 layout) — # / Question / Answer type / Options / Weight / Actions (Edit + Delete kebab), an **Add Question** primary button, and a "Quiz total: up to N points across M scored questions" line (`obMaxScore` = Σ weight × best option score). The **editor is the RIGHT SLIDER** (`SHEETS.obedit`, key in `SIDE_SHEET_KEYS`, rendered by `sidePeek`) — like Add User / Staff, NOT a page (Maryam 22 Sep 2026). A row (`sheet:'obedit:<id>'` — `table()` now renders a row's `sheet` as `data-sheet` on the `tr`, like `go`) and the Edit kebab and Add Question (`obedit:new`) all open it. A working copy lives in `S.qEdit` (loaded once per id, kept across in-place renders — trap 9; cleared on sheet close). A segmented **Answer type** control (`.qb-types`, `data-act="obtype:<v>"`) swaps the form; text/weight/placeholder written caret-safe via a `data-qbf` input branch (no render); the add/remove **option list** uses `data-qbopt="<i>:<field>"` (caret-safe) + `oboptadd`/`oboptdel`. The in-slider actions RETURN `'keep'` so `doAction` does not close the sheet; `obsave` writes to `ONBOARDING_Q` + an audit line and lets `doAction` close it. **Classes are `qb-*`, NOT `ob-*`** — the design system already ships `.ob-opt` for the candidate onboarding SURVEY (§107, a pill), so `ob-*` here dressed every option row as a pill. Scores/weights are AUTHORED PLACEHOLDER (§74). Admin-local. (tn-admin-portal.html, Maryam 22 Sep 2026)
- EPIC 1 — SA ACCESS & PROVISIONING (staff lifecycle; built 15 Sep 2026, `tn-admin-portal.html`). The internal-staff directory is its OWN sub-page `users/staff`; **All Users (`users/users`) is EXTERNAL accounts only** — the doc's rule "an account never appears in both", so `usersAll` filters out `u.internal`, drops the Admin role tab, and Add User offers external roles + non-internal origins only. Internal accounts are created and managed only through Staff. Built on the shared table/dropdown/sheet/confirm components. Pieces: `usersStaff` (directory — columns Name+email / Role / Job title / Department / Status / Last signed in / Added; Role/Status/Department `filterDd`s; row actions by status + guards), `staffDetailView` (the 1.7 detail — reached via `staffdetail`, branches on `u.internal`), and sheets `addstaff` (1.1), `editstaff` (1.8), `changerole` (1.3), `deactivate` (1.9), `reactivate` (1.10), `delstaff` (1.11). `addstaff`/`editstaff` open in the SLIDER (matching Add User, `SIDE_SHEET_KEYS`); the four guarded two-step actions stay centre `.conf` modals. Status vocabulary: `invited` / `active` / `inactive` → "Invited / Active / Deactivated" (`STAFF_STATUS_LABEL`). Guards: `staffProtect` (the original Super Admin `u.orig` and the actor's own account — no role change / deactivate / delete) and `lastSAGuard` (never remove access from the only Super Admin). Every write logs audit (invite/change-role/deactivate/reactivate/delete = High, edit = Medium `'med'`). Seed: u9 Derek is the original SA (`orig:true`, protected), u43 a second SA (so the last-SA guard is demonstrable), u46 Invited, u47 Deactivated. (Epic 1, Maryam 15 Sep 2026)
- STAFF AUTHENTICATION IS PASSWORD + A ONE-TIME CODE BY EMAIL OR SMS — no authenticator app, no trusted device, the channel chosen every time (stories 1.2 / 1.4 / 1.5). The auth scene (`authView`, `S.auth`): login → `deliver` (channel choice, none pre-selected) → `code` (6 digits, 40s resend gate) → `setpw` on the reset flow; `S.authFlow` says sign-in vs reset. Activation (1.2) is a full-frame PREVIEW scene (`activateView`, `S.activate`) reached from an Invited row's "Preview activation" (there is no invited persona in the console); completing it flips the account to Active. A prototype accepts any six digits and stores no real password (ruling: build the flow and its screens, not the send). The resend cooldown is enforced behaviourally (an early press is refused), not ticked live, to keep the OTP field's caret. This REPLACED the old "authenticator app + backup code" sign-in and the "reset link by email" flow. (Epic 1, Maryam 15 Sep 2026)
- STEP-UP (a fresh code before the action commits, `askConfirm({stepup:true})`) is on the doc's FOUR highest-severity actions (story 1.6): change a fee (`feeStepup` → `dosavefee`), approve a refund (`askrefappr`), delete an account (staff `delstaff`, external `askdel:user`) and delete a role (`askdel:role`). The doc says "no other action triggers the prompt", but the OLD SA-journey step-ups on grant / revoke Super Admin / View As / release-all payouts were LEFT in place (removing them is the riskier direction) — FLAGGED for Maryam to rule on. `SEC.stepup` toggles the gate. (Epic 1, Maryam 15 Sep 2026)
- EPIC 2 — ADMIN CONSOLE (built 15 Sep 2026). 2.1 Navigate: the console shell/rail already existed and follows standing ruling #2, NOT story 2.1's exact 12-item list (the doc hides Employers/CRM/System and shows Levelling/Consultant Requests; the ruling reverses that) — nav left as-is, the discrepancy is deliberate. 2.2 Dashboard: a period selector (This week / This month / This quarter, This month default, `S.period` + `setperiod`) and an "as at <time>" stamp on `homeOverview`'s first panel — PRESENTATIONAL (the seed has no time series; SA-03 E1), the surface not a live feed. 2.3 Notifications: the bell's "View all" opens a full list page `home/notifs` (`homeNotifs`, hidden sub) — All/Unread filter, per-account read state (`S.read`), 90-day-retention note, "not the audit log". 2.4 My Profile (`settingsProfile`): only First/Last editable; Email/Job title/Department read-only with a note on where each is changed; the role shown WITH its permission chips; Save logs Medium (`'med'`), no notification. 2.5 Login & Security (`settingsSecurity`): three groups — Password (current/new/confirm, rules, differ-from-current, ends every other session, High audit), Phone number (current masked, new + current password → OTP sub-step `S.secPhone='code'` → verify, High audit, old number holds until verified), Signed-in sessions (device/browser/location/started/last-active, current marked no sign-out, per-row Sign Out + Sign Out Everywhere Else, auto-sign-out read-only) — plus a Two-factor group (always on + the step-up toggle). Nobody changes anyone else's credentials. (Epic 2, Maryam 15 Sep 2026)
- THE SUPER ADMIN DASHBOARD (`homeOverview`) MIRRORS ONLY THE LIVE MODULES, OPERATIONAL-FIRST, WITH LEAN CHARTS (Maryam 21 Sep 2026). Every band maps to one of the nine VISIBLE modules and routes into it; NOTHING on the dashboard reads a hidden module. Employer analytics (Employer-unlocks revenue stream, employer-verification attention row, Placement fees) and the whole CRM "Growth & acquisition" group (Leads by source, the `newLeadsCard`/`trendChart` new-leads line, the quiz→placed acquisition funnel, the "Where users come from" origin `.stats`) are REMOVED — they surfaced modules the admin can no longer open (Employers "not in the system for now" 19 Sep; CRM hidden). Order, top→bottom: (0) period bar (kept, presentational — no time series, SA-03 E1); (1) **Needs your attention** — the operational LEAD: a `.tile clk gcard` tile-stack (NOT `.qa-c` quick-action cards, whose copy may not carry live queue data §98.3), one row per queue, drawn ONLY when its count>0 (§60), all routing to live modules (`cohorts/assign` leaders-ready `LEADER_REQS` feedback-stage + cohorts-without-a-leader; `market/bookings` overdue held interviews `bkOverdue`; `finance/payouts` `heldPayouts()`; `home/rulings` blocked+open `DECISIONS`); (2) Platform at a glance — TWO `.stats` rows of exactly four (people row + money/throughput row); (3) People & access — Users by role + by status (`hbars`, employers excluded); (4) Candidates & cohorts — bucket `donut` + "where candidates sit" `kv` + the cohort-health `table`; (5) Marketplace & interviews — booking-mix `donut` + interviews-by-state `kv`; (6) Finance & settlement — revenue streams `hbars` (Course / Interview / Scene only) + payments & payouts `kv`; (7) System & activity — platform-health `kv` (`SVC_HEALTH`/`INTEGRATIONS` live counts, degraded, open DSARs) + recent-audit timeline; (8) Open decisions & risks — the blocked `DECISIONS` tiles + "Open the register". Charts stay `donut` (2–4 slice split) / `hbars` (ranked list) per "data over decoration" (§5); NO trend line (placeholder). Do NOT re-add employer/CRM/placement panels. (tn-admin-portal.html `homeOverview`, Maryam 21 Sep 2026) — UPDATED 22 Sep 2026 (Maryam, inspo graphs): the dashboard now runs Needs your attention → Platform at a glance → **Platform Users** (`barChart`, populations by month) → **Earnings & Payouts** → **Candidates by Track** (`bucketCard` sparklines) → **Interviews** (`lineChart`); Cohort health, System & activity and Open decisions are HIDDEN (kept behind `false`). The **Earnings & Payouts** section (`group('Earnings & Payouts')` heading over a `.pay-row`) sits directly after Platform Users: LEFT a Platform-earnings breakdown card (headline + `.pstat-chip` + stacked revenue-source bar + % legend — bar palette **black / accent / #6B6B6B** matching the Platform Users chart, NO "the platform's take" sub-line), RIGHT a Payouts card (headline + `.pstat-chip`, NO note line, NO "Agent payouts" legend — the period filter alone rides the legend row via `lineChart`'s `noLeg`). This is the exception to the 21 Sep "NO trend line" clause — the client asked for the inspo statistics graphs; figures + % change are AUTHORED PLACEHOLDER (§74, SA-03 E1). The **% chips are one component everywhere** — `.pstat-chip` matches the Candidates-by-Track `.bcard-chip` (11px, `padding:3px --s03`, light-green success). The two cards are **equal width** (`.pay-row` `1fr 1fr` ≥900, stacking on a phone). The Payouts graph is a **semicircle gauge** (`gauge()`, Figma node 1-601), NOT a line chart: a 180° arc where **only Settled fills the arc** in `--accent` over a light-grey `--gray-20` **background track** (In progress is that grey background, not a coloured segment — Maryam 22 Sep 2026 "use light grey instead of pink and it should be in the background"), a **circle pointer** (white knob + accent ring) at the tip of the filled arc, a centred "Total payouts / N", and a legend showing **only Settled + In progress** (Maryam 22 Sep 2026 "we are only showing payouts that are pending and settled" — no Failed). The card also carries a `.pbrk-sub` "**$X pending to release**". Arc-length dash math uses the real `L = π·R`, NOT `pathLength` (unreliable — it let the `50 50` dash repeat and paint a stray accent cap at the far end). Counts/amount read off the `PAYOUTS` seed (4 settled · 4 held · $744 pending · 8 total; SA-03 E1 demo ledger). NO period toggle (a status snapshot). The earlier `payoutChart`/`ampPath`/`PAYOUT_PATH` line renderer was REMOVED. **Every dashboard graph carries a no-background accent nav link** (`chartHead(title, {go, label})` → `.acc-link`) to the page its data belongs to: Platform Users→users/users, Platform earnings→finance/transactions, Payouts→finance/payouts, Candidates by Track→candidates/candidates, Interviews→market/bookings (Maryam 22 Sep 2026). The Platform Users bars were also toned down so black Candidates sit just above the accent Talent-agents bars (base 10–18 vs 7–15, axis 0–25). (tn-admin-portal.html `homeOverview`/`gauge`/`chartHead`, Maryam 22 Sep 2026)
- EPIC 3 — USERS (external directory; 3.1–3.6 built 15 Sep 2026, 3.7/3.8 detail enrichment still TODO). 3.1 `usersAll` is the EXTERNAL directory: role TAB strip (Maryam's rule overrides the doc's Type dropdown), Status dropdown (All/Invited/Active/Deactivated), columns User / Type (`typeLabel`) / Cohort leader (a `.tag` marker) / Level (`candLevel`, candidates only) / Status (`userStatusTag`) / Joined / Last active; a row opens the OWNING module's rich detail via `userDetailRoute` (candidate→`candidates/detail`, agent→`agents/detail`, else the generic `staffdetail`) — CANDS/AGENTS ids match USERS ids, so this is safe. Screen actions: Bulk Import · **Add User** (Maryam 15 Sep 2026 overrode the doc's agent-only invite — she wants to add any external type, so the generic `rec:user:new` with a role picker is the primary CTA, not Invite Talent Agent; `SHEETS.inviteagent` stays defined but unused). 3.2 `SHEETS.inviteagent` (first/last/email → Invited agent, High audit, slider) is BUILT but not surfaced, per that override. **EXPORT is hidden everywhere** (`exportBtn` is a no-op — it was a toast-only placeholder with no CSV and no audit line; Maryam 15 Sep 2026 "hide the export button, it's still not working"); make it real to bring it back. 3.3 `askmarkleader`/`askremoveleader` + `leaderEligible` (candidate at E5+ via `LVL_CODES`, or any internal staff) — a capability, not a main role; the button is on the candidate detail (disabled with the E5 reason when below) and the staff detail; removal is refused while `leadingCount>0`. 3.4/3.5/3.6 `deactext`/`reactext`/`delext` (reason note + `extClearing` summary of an agent's upcoming interviews+listing and a leader's live cohorts; delete adds step-up; High audit) — wired to the directory row actions (Invited rows offer Resend/Cancel invite instead). The candidate/agent MODULE detail pages keep their own suspend/erase for now. NOTE: the doc's per-role permission gating of panels/actions is NOT simulated (single Super Admin, ruling #3). (Epic 3, Maryam 15 Sep 2026)
- The TALENT AGENTS module page (`agentsAll`) heading is "Agent directory", NOT "Marketplace listings" (Maryam 15 Sep 2026: "this is a talent agents page, do not name it as marketplace listing") — parallel to the candidate module's "Candidate directory". It carries an **"Add Talent Agent"** primary button (`addBtn('agent', …)` → the `KINDS.agent` create sheet, which makes an APPROVED active agent — distinct from the Users page's Invited-only invite flow; the KIND help says use it "for a known agent you have already vetted elsewhere"). `KINDS.agent.label` is "talent agent" (was "listing"), so its sheet reads "Add a talent agent" / edit / audit lines consistently; `okNew` is "Add Talent Agent". The "Listing" column and the Listed/Paused/Not-listed states keep the listing vocabulary — that is the agent's marketplace state, not the page's name. (tn-admin-portal.html, Maryam 15 Sep 2026)
- An admin rail is a tree: the open module lists its sub-pages as `.sn-sub` rows, queue counts as `.badge`; the render gates the sub-rows, not the CSS. (§120)
- In the COLLAPSED 72px rail, hovering a module icon that has sub-modules opens its sub-list as a FLYOUT beside the icon (a `.sn-subs` panel floating right of the rail, no module-name header — the sub-list names itself). The sub-block now renders for EVERY parent (the render drops the old `on && S.nav` gate); the CSS routes it — inline under the current module in the open rail / mobile drawer, hover-only flyout in the collapsed rail. The parent's `.sn-node` wrapper is the hover region and is armed in `HOVER_KEEP`; the collapsed rail goes `overflow:visible` so the flyout escapes its 72px. (§120.1c, Maryam 7 Sep 2026)
- The rail is the ONLY sub-navigator: a module with sub-pages draws NO in-page tab strip for them, even when the rail is shut. `subTabs()` is a no-op; to switch sub-page you open the side nav and pick one. (§120, Maryam 7 Sep 2026) — ONE EXCEPTION: the SETTINGS module (below).
- THE SETTINGS MODULE IS ONE PAGE WITH INNER TABS, mirroring the candidate profile — NOT a rail sub-tree (Maryam 21 Sep 2026: "instead of having submodules of settings, profile and notifications ... follow the candidate profile structure where the profile module has inner tabs"). All five Settings subs (`general` / `profile` / `notifications` / `security` / `payments`) are marked `hidden` in `MODULES`, so the rail draws the footer gear as a single leaf (no chevron, no flyout); the subs stay in `_sub` so `settings/<key>` is still routable + deep-linkable and the account-menu / rulings-register links keep working. `V.settings` (no longer `modView`) renders `head + settingsTabs(cur) + SETTINGS_PANES[cur]()`: a `.sec.sec-cs.set-tabs` `.cs` tab strip (General / My Profile / Notifications / Login & Security / Payments & Cards) whose tabs are `data-go="settings/<key>"` buttons (so a tab click navigates and the URL follows), over the active pane (the pane renderers are unchanged). `.set-tabs` shares the interview page's `.iv-tabs` spacing (strip `::after` off, one `--s05` gap to the pane). This is the single reversal of the "rail is the only sub-navigator" rule, Settings-only. The Settings rail item (the footer gear) is now the PROFILE/person icon (`ic:'profile'` → `user`, since Settings is the account page and the System module already owns the gear), and because the admin hides Tal's summary portal-wide, `V.settings` draws a plain `aiHead('Settings', …)` page heading + description above the tabs (Maryam 21 Sep 2026: "the settings module icon should be profile icon", "the page heading and desc is missing"). The strip is balanced --s06 (24) above AND below — the `.set-head` heading's bottom padding + `.sec-h` margin are zeroed (`!important`, the section-rhythm chain again), leaving the strip's own top padding as the single gap above, and the pane's top padding the gap below (Maryam 21 Sep 2026: "unnecessary space after page desc to the tabs, and less space between tabs and content"). (tn-admin-portal.html, Maryam 21 Sep 2026)
- A module WITH sub-pages carries a chevron at the row's end (`.sn-chev` in a `.sn-trail` group), so you can see it opens a tree; its queue count sits to the chevron's LEFT (`badge` then `chev`). The chevron flips down→up when the module is expanded; it shows wherever the labels do (the open desktop rail and the mobile drawer) and is hidden in the collapsed 72px rail, where the count keeps floating on the icon (`.sn-trail` goes `display:contents` there, so the collapsed rail is byte-for-byte as it was). (§120, Maryam 7 Sep 2026)
- The admin's open drawer is WIDER (`--drawer-w:310px`, admin-scoped) so its long module names fit on one line — the candidate/leader/agent rails keep 260/280; and the open-rail count circle is a size smaller (18px box, 10px numeral via §63 §55). (§120.1b, Maryam 7 Sep 2026)
- Status is plain COLOURED text, never a chip: `tag()` stamps `.statw` (green `--support-success-ink` / amber `--support-attention` / red `--support-error`, inks in §63 §53), no box. The categorical label chips that remain (roles, levels, tracks — `.tag`) are ROUND (`border-radius:999px`), admin-only via `[data-portal="admin"]`; the other portals keep the platform's radius-0. (§126, Maryam 7 Sep 2026)
- A rating reads star-first: `★ 4.9`, the star inline (`display:inline-block` over the DS `svg{display:block}`), never stacked. The rating VALUE star is FILLED gold — `star` at `--star` in `starMark` (Maryam 15 Sep 2026: "i never said to remove the fill of the yellow stars, fill them again", reversing the same-day `starOutline`), so every admin rating (the agents table + the agent detail figure) matches. What is OUTLINED instead is the DECORATIVE Rating-block mark — the star icon on the LEFT of the agent-detail Rating figure cell is `'starOutline'`, so it loses its black FILL (Maryam 15 Sep 2026: "remove the black fill from the star icon on the left of the rating block"); a decorative mark is inked #111 (§29), so it reads as a black outline. (`ratingCell` / `starMark`, Maryam 7 & 15 Sep 2026)
- A COHORT IS NOT BASED ON A TRACK OR A COURSE (Maryam 15 Sep 2026: "cohorts are now not based on a track or a course ... remove the track column and implement this ... wherever in the flows it is stated"). The Track column is gone from every cohort table (the Cohorts list and the dashboard's Cohort-health table); the New Cohort / Edit sheet has no Track field; the cohort detail drops its Track fact and its "{track} cohort" subtitle (now "Led by …"); the cohort Tal summaries no longer name tracks; and the leader flows (invite, allot, merge, move) no longer tie a leader or a target cohort to a track — merge offers any active cohort with room, not a same-track one. The `track` key lingers in the seed data, unread by the UI (a cohort is just a named peer group with a leader and its own sessions). COURSES keep their own track (the course catalogue is a separate entity). (tn-admin-portal.html, Maryam 15 Sep 2026)
- COHORT NAMES DO NOT RELY ON A LEVEL OR TRACK — they are generic "Cohort N" (Maryam 17 Sep 2026: "cohort names are strictly not relying on the levels, so please give other names to cohorts. like Cohort 1 and so on"). The four seed cohorts renamed Explorer 12 → **Cohort 1**, Explorer 14 → **Cohort 2**, Builder 3 → **Cohort 3**, Explorer 15 → **Cohort 4** (the orphan CANDS membership "Builder 4" → **Cohort 5**); every reference moved with them — `CANDS[].cohort`, `USERS[].cohorts` (keeping the `(member)`/`(leads)` suffix), the audit line, the seed comments, and the two "Led Explorer 9/7" past-cohort mentions in leader pitches (generalised, not renumbered). The New Cohort name placeholder is "e.g. Cohort 6". The leader's own `(Builder)`/`(Trailblazer)` bucket suffix is a person's level bucket, not a cohort name, and stays. The hifi leader portal already used generic names (Cohort 41/33). (tn-admin-portal.html, Maryam 17 Sep 2026)
- THE SUPER ADMIN TAL SUMMARY IS HIDDEN PORTAL-WIDE (Maryam 17 Sep 2026: "hide tal summary from the whole super admin portal"). The "Summary by Tal" `.talsum` band is dropped from BOTH `head()` and `detailHead()` (the `sum` arg is kept in the signature but no longer drawn, so no call site changes); the `dsTypeSummary` render pass is guarded to no-op when the band is absent. A list-page head is then only an empty breadcrumb, so an admin-local rule (`.app .modhead:not(:has(.ai-aura)):has(> .ph.ph-bare){display:none}`, `#admin-nosum`) hides that empty band while keeping the element in the DOM so `.modhead + .sec` first-section spacing still fires; a detail head keeps its back arrow. Admin-only — the other three portals keep their summaries. (tn-admin-portal.html, Maryam 17 Sep 2026)
- THE SUPER ADMIN ADD-SESSION SHEET (`SHEETS.addsess`) schedules by a concrete DATE + TIME, not a "Week" (Maryam 17 Sep 2026: "instead of week selection we need to give date and time fields to logically set when this session will happen"). Fields are Session title / Date (`t:'date'`, our month-grid picker) / Time (`t:'select'`, business-hours dropdown) / Length; the **Mode** field and the "added to this cohort's outline" note are removed. `doaddsess` stores `{date, time, dur}` and derives a `week` from the cohort start (`sessionWeek`) so the roster table can still sort/label; a session with no date falls back. The Sessions table on the cohort detail: its "Week" column becomes "**When**" (shows `dpFmt(date) · time`, else "Week N" for a seed session), Mode renders `s.mode || '—'`, and it carries **no row-select checkboxes** (`{select:false}` — Maryam 17 Sep 2026: "remove the checkboxes from this table"). (tn-admin-portal.html, Maryam 17 Sep 2026)
- ON THE SUPER ADMIN COHORT DETAIL, "Add Session" and "Add Member" are ACCENT LINKS, not black buttons (Maryam 17 Sep 2026: "give add session text and icon accent color", "this add member button should also be like the add session button"): `.btn.acc-link btn-sm noic` — transparent, the leading `+` glyph and the label both `--accent-text`, no fill/border/padding (`.acc-link`, the payouts Release pattern). (tn-admin-portal.html, Maryam 17 Sep 2026)
- The TALENT AGENT DETAIL (`agentsDetail`) drops the Tal summary band (`detailHead` gets no `sum`, keeping only the back arrow) and leads with an `.idhead` — the agent's `agentFace` PORTRAIT on the LEFT of the name, the state tag + listing actions on the right — mirroring the candidate detail (Maryam 15 Sep 2026: "remove tal summary from this page" / "add image of the agent ... on the left of the name part"). (tn-admin-portal.html, Maryam 15 Sep 2026)
- THE VERIFIED MARK beside a candidate or talent agent NAME (`.idverify` on the detail `.idhead`) is the scalloped **`verified` seal** glyph in green (`--support-success-ink`), NOT a green disc with a bare `check` (Maryam 21 Sep 2026: "use the highlighted icon as the verified icon against candidate or talent agent", off a Hugeicons stroke-rounded reference). The seal carries its own tick, so `.idverify` sheds its background circle (just `color:--support-success-ink`) and the glyph draws at 18px. Shown on BOTH the candidate detail (`candidatesDetail`) and the talent agent detail (`agentsDetail`, before the rating). The DS icon set this portal links is stroke-based on a `0 0 24 24` box (`inner('verified')` → a stroked seal), so the marker keeps `viewBox="0 0 24 24"` — this is NOT the hifi portal's Material Symbols set. Admin-local. (tn-admin-portal.html, Maryam 21 Sep 2026)
- A CLICKABLE admin table row (`tr.clk`) lights its FIRST-COLUMN NAME on hover — the black anchor text turns `--accent-text` (#c2332d, the accent as ink, §3); the grey `.t-caption` sub-line beside it keeps its own colour (its own rule beats the inherited accent, so nothing names it). `cursor:pointer` on the row too. Written as a PLAIN `:hover` in the admin portal's inline `<style>` because the DS's `:hover`s are disarmed (`.__nh`, trap 6) and never armed in this file, so a portal-local rule is the one that fires. Admin-only. (tn-admin-portal.html `#admin-row-hover`, Maryam 13 Sep 2026)
- Every list a Super Admin sees has create, edit, delete and status changes — through the `.sheet` and the `.conf` dialog, and every change writes an audit line. (admin 4 Sep)
- THE SUPER ADMIN PORTAL IS BUILT AGAINST THE SUPER ADMIN JOURNEY (Draft v1.0, 6 Sep 2026: sixteen use cases SA-01…SA-16, 51 alternatives, 37 exceptions; 2 confirmed, 7 open, 5 blocked). Every case gets its SURFACE with its primary alternative drawn; every OPEN or BLOCKED branch is a line on the Rulings Register (`DECISIONS` tagged `sa` + `kind`, drawn at `home/rulings`), never an invented control. The register (in the Dashboard sub-nav) is the ONE place those branches live: module pages carry NO inline ruling note — Maryam, 7 Sep 2026, "remove all such sections like 'SA-09 · Account actions — Open ruling' … this is not the info a user would want to see." This REVERSES the earlier "one `ruling('SA-nn')` note per waiting page"; `ruling()` is now a no-op, its call sites removed, and the SA data behind the register is untouched. (Maryam 7 Sep 2026, §129)
- The register's owners are FIRST NAMES (Umair · role scope, ACL, portal architecture; Derek · override authority, who holds the role; Reed · pricing and settlement) — the seed and the journey spell the surnames differently and the portal does not pick. (§129)
- The four highest-severity actions — grant Super Admin, revoke it, start View As, release all payouts — confirm through a STEP-UP authenticator code (`askConfirm({stepup:true})`, six digits), switchable in Login & Security (`SEC.stepup`). A two-step sheet carries its values in `S._pending` from the sheet to the confirmation. (SA-02 A1, 7 Sep 2026)
- A Super Admin grant is TIME-BOXED BY DEFAULT (30/90 days or permanent), needs a second approver and a reason, and expires on its own (`expireGrants()` runs on every render). Refused: self-escalation and a holder who is also an agent or leader; a sole holder grants single-approver, logged high. Revocation names its kind (planned / emergency / expiry) and refuses the last remaining holder. (SA-01, SA-16)
- A capability granted across the Program / Business wall (`CAP_SIDE`) is WARNED and asked, not refused — the refusal is the open ruling. (SA-10 E1)
- VIEW AS is read-only by default: `S.viewAs` opens the `.viewas` bar (§129) under the app bar naming who, the scope, that interview video is excluded, and since when; the router refuses every write while it is open; another admin and a user with an interview today are refused; both ends and every line in between are audited. (SA-11)
- A level override offers MVP levels only (`levelOpts` — Trailblazer stays in the data model, off that picker), asks a DOWNWARD move twice, records the disclosure choice, says when it lands mid-cohort, keeps a per-record history and can be reversed with both lines kept. Bulk correction is its own sheet. (SA-04) — EXCEPTION: the Add/Edit Candidate record form (`KINDS.cand` Level field) offers the FULL ladder `LVL_CODES` (E1–E5, B1–B5, T1–T5), not the MVP subset (Maryam 15 Sep 2026: "show all levels here, from e1 to t5"), so a candidate can be created or edited at any level including Trailblazer. The COURSE field is READ-ONLY WHEN EDITING (Maryam 20 Sep 2026: "the course row should show the course name and it should not be editable" / "the candidate is taking Communicating with Impact ... show this in the edit window ... but it should not be editable") — a `t:'text'` `ro:true` locked field via `get: () => candActiveCourse() || 'No course yet'`, where `candActiveCourse()` reads the candidate's IN-PROGRESS course (else most-recent completed) off `CAND_COURSE_LIST` (the admin's authored per-candidate course read, §74 — so it agrees with the detail page's Courses section, e.g. "Communicating with Impact"). `update` no longer writes `c.course` (a candidate's course follows their own enrolment, not the admin). On CREATE it stays the live-course picker. (tn-admin-portal.html, Maryam 20 Sep 2026) — The COHORT picker names each cohort's MEMBER COUNT, RIGHT-ALIGNED on the option row (Maryam 22 Sep 2026: "show the member count against a cohort name in the cohort dropdown" / "the count should be right aligned on the row"): the count (`${x.size} / ${x.cap||10}`, the same `size / cap` the roster and cohort tables show) rides the option's 4th tuple element (`ometa`), NOT baked into the label. `formDd` renders a 4th element as a `.fdd-meta` span and puts `.has-meta` on the row (and on the trigger's `.fdd-val` when that option is selected) — `.fdd-opt.has-meta` goes `display:flex` and `.fdd-meta{margin-left:auto}` pins the count to the row's right edge; the pick handler carries `.has-meta` onto the trigger so the collapsed field shows the count right-aligned too. The option VALUE stays the bare name (what `create`/`update` write to `c.cohort`), and "No cohort yet" takes no meta. Admin-local CSS + `formDd`; a 4th tuple element is a general, backward-compatible `formDd` feature (plain selects are untouched). `c.size` is kept current by `syncCohortSizes()` in the render pipeline, so it is accurate at open. And the candidate form carries NO info block — the `KINDS.cand` `help` note ("A candidate is also an account…") is removed (Maryam 22 Sep 2026: "remove the info block in the form"); other KINDS keep their own `help`. (tn-admin-portal.html, Maryam 22 Sep 2026)
- THE SUPER ADMIN CANDIDATE DETAIL SHOWS "LINKED ACCOUNTS" = the candidate's SOCIAL accounts (Maryam 20 Sep 2026: "linked accounts means social accounts ... show the linked accounts after the skills section", with the candidate-portal Linked Accounts tab as the reference). `candSocials()` renders the candidate portal's `.lacc`/`.lacc-grid` cards (§105, shipped in the DS this portal links) READ-ONLY, appended inside `candCV` AFTER the Skills section (order: … Skills → Linked accounts → Quiz results). ONLY THE CONNECTED accounts are shown (Maryam 22 Sep 2026: "only show the linked accounts") — `candSocials` filters `SOCIAL_ADM` to `CAND_LINKED[s.k]`, so the "Not connected" cards (their placeholder-disc + no-CTA state) are dropped; the whole section is omitted when the candidate has linked none (no empty grid under the heading). Each surviving card is a brand LOGO on its own `0 0 24 24` box (trap 7 brand-mark exception) tinted by `--brand`, showing the handle + the candidate's photo (`agentFace`) + a green "Linked". Non-interactive: `.lacc-ro` gives a default cursor, no `data-link` toggle. `SOCIAL_ADM` (SVGs + handles) and `CAND_LINKED` (linkedin/instagram/twitter on) are authored placeholder (§74) mirroring the candidate UI. (tn-admin-portal.html, Maryam 20–22 Sep 2026)
- Deactivating an account states its ROLE-SPECIFIC consequences before the press (candidate paused from visibility; an agent's in-flight interviews marked `reassign`; a leader's cohorts unled; sponsored candidates unsponsored; a mid-flight payment named). Merge is a sheet; hard delete keeps receipts and lets a recording run out its retention. (SA-09)
- A refund carries its KIND (interview / cohort pro-rata / sponsored employer pays / goodwill credit) and its source; approval is refused while a chargeback is in flight on the same charge and warns when the agent's payout has already settled; with no threshold set every refund is the Super Admin's. (SA-06)
- `audit()` carries a before/after (`{from, to}`); the log has a date facet and a READ-ONLY line sheet — a sheet with no `ok` draws no save button and its Cancel reads Close. (SA-15)
- The admin signs in through the design system's auth composition: `.auth-card` is a CHILD of `.app` (one element wearing both classes matches nothing), email + password then the authenticator code; log out returns to it; a deep-linked hash skips it. No role picker for a one-role account. (SA-02)
- The TAL ONBOARDING gate opens on CHAT, never the spoken voice intro (Maryam, 9 Sep 2026: "hide this screen, the user will directly land on the chat screen"). ob.js's `S.obMode` default is `'chat'` AND `setStage('onboard')` sets `'chat'` too — the reset was the half still forcing `'voice'`, so the STAGE picker walked back into the orb. The voice machinery stays, unreachable. The chat WELCOME (`obChatScreen`, `S.obChatOpen===false`) draws NO dock/field at the foot — the way forward is the body's "Let's Get Started!" button; the composer and the question thread arrive together only once it is pressed (`live`). Its greeting is **"Welcome to TALENTnext, Maryam!"** — `OB_CHAT[0]`, kept EXACTLY as supplied (the wordmark casing is Maryam's; supplied copy is never silently normalised). (§107, ob.js + views.js `setStage`, Maryam 9 Sep 2026)
- **THE LOAD CONVENTION — "add a load on this click" MEANS THE TN LOADER FOR 2 SECONDS BETWEEN THE TWO SCREENS** (Maryam, 18 Sep 2026, a standing default). When Maryam says to add a load / loader on a click, it means: on that click, play the three-blade `.tn-loader` (the `loader.html` asset — near-black `--icon-primary` blades, 90px, `tn-relay` animation, centred on the destination's ground) for **2 seconds**, then complete the navigation to the next screen. Implement it by holding a loading flag, `render()` to paint the loader, and a `setTimeout(…, 2000)` (never rAF — trap 17) that clears the flag and performs the real navigation; keep the loader on the CURRENT stage's render path so it paints before the handover. The hifi onboarding is the worked example (`S.obLoading` + `obLoaderScreen`, on both "Let's Get Started" and "Find an Agent"). 2000ms is the one duration; do not invent per-click timings unless Maryam names one.
- THE ONBOARDING QUESTIONS ARE A STEPPED SURVEY, NOT THE CHAT THREAD (Maryam, 18 Sep 2026, with a reference). IT IS THE REFERENCE IN LIGHT MODE — the reference is dark, the product is light, so the palette is translated, not copied (Maryam 18 Sep 2026: "we need to show it on light mode so the colors will be different"): white `--background` ground, `--text-primary`/`--text-secondary` ink, `--layer-02` pills on a `--rule` hairline, an accent-tinted selected pill (`color-mix(--accent 8%, --background)`) with an `--accent` radio disc, `--rule` progress track with an `--accent` fill, and the platform's own `.btn-s`/`.btn-p` for Back/Next. "Let's Get Started" on the welcome (`obChatScreen`) now plays a **2-second loader** (`obLoaderScreen`, `S.obLoading` — the three-blade `.tn-loader` relay off the `loader.html` asset, on the survey's light ground) and then advances `S.obStep` to 1 (with §107 §0g's leave animation) instead of opening the old chat thread; `obScreen` paints `obSurveyScreen()` for steps 1..`OB_N` and keeps the read-back on its `.auth-card`. Each survey screen is: a SINGLE progress bar pinned to the frame's TOP WALL (`.ob-sv-top`, `top:0`, full width) whose fill TRACKS THE STEP — `(S.obStep - 1) / OB_N` — so it fills on Next and RETREATS on Previous, and does NOT move when an option is selected (Maryam 18 Sep 2026: "the top bar should fill in when I click on next, not when I select an option ... on going back the bar should also go back"); question one is 0% and the completion screen 100%; it ANIMATES from its last width via a keyframe fed `--ob-from`/`--ob-to` (the bar node is recreated each render, so a plain transition can't tween — `S.obBarFrom` carries the previous %); a brand row under the bar (`.ob-sv-head`) with the TALENTnext wordmark LEFT and **"You're almost there!"** (`.ob-sv-hint`, `--t-desc` `--text-secondary`) RIGHT, aligned (Maryam 18 Sep 2026); the `borb` orb TOP-LEFT (`.ob-sv-orb`, `borbMark('tal-mk', live)`) so it reads as Tal asking; the QUESTION ITSELF is the heading (`obTitle` in `.ob-sv-title` at `--t-h1`) — NO theme title and NO description line (Maryam 18 Sep 2026: "i want the questions to be a heading and forget about a description"); the question column is vertically CENTRED (`.ob-survey .main{align-items:center}`) and ~720px wide; radio PILLS (`.ob-sv-o`, `data-ob`/`data-obv` — the existing selection handler; `--layer-02` fill, NO border in the default view, an `--accent` edge + accent-tint fill + filled `--accent` disc when chosen; label `--t-body` 13.5, desc `--t-label` 12.5 `--text-secondary`); and a foot (`.ob-sv-foot`) with NO Previous on question one, a light-grey `--layer-02` **"Previous"** BUTTON with BLACK text + arrow from question two (`data-obgo`, LEFT), and a black `.btn-p` **Next** (RIGHT) — both content-width, gated by `obReady`. The left rail divider (`.app::after`) is suppressed on `.ob-survey` like the other full-bleed onboarding scenes (§14). The terminal screen is `obDoneScreen` (replacing the old read-back, Maryam 18 Sep 2026): the same light `.ob-survey` composition with the orb, a **"You're all set"** heading + one line, the journey steps (`obJourney` — quiz done, Interview & Levelling next), and a **"Find an Agent"** `data-obdone` CTA that sets `S.recKey`, writes the profile fields, then plays the 2s loader again before landing on the dashboard. It is a NEW SKIN over the existing step machinery — selection, Back/Next, the read-back, completion and the answer readers are unchanged; the chat machinery (`obThread`/`obCompose`/`obProgress`/`obAsk`/`obSay`) is kept defined but unreachable, and `obQuestion` (the old stepped screen) is removed. Type from §63's role tokens, radius 0 on pills and buttons (only the radio marks are round — CLAUDE.md's "build the reference in our components"). No free-type composer on the survey (client chose the full stepped model). (§107, ob.js, Maryam 18 Sep 2026)
- ~~The LIVE onboarding chat carries a PROGRESS BAR at the top~~ (SUPERSEDED 18 Sep 2026 by the stepped survey above; `obProgress`/the chat thread are no longer reached). The LIVE onboarding chat carries a PROGRESS BAR at the top (`obProgress`, §107.p, Maryam 9 Sep 2026, with a reference): an h3 **"You're almost there!"** left-aligned above a segmented bar, both centred on the same `minmax(0,720px)` column the thread and field use. One segment per question (`OB_N`); a filled segment is `--accent`, the rest are a plain `--rule` (#efefef) fill with NO border (Maryam 9 Sep 2026 — reversing the earlier #fcfcfc fill + #efefef hairline). Segments are 999px pills (a stated §56 curve, off the reference). `done` is `min(OB_N, max(answered, S.obQi))` — NOT `S.obQi` alone, because `obAnswer` zeroes `obQi` for the typing beat, and a bar keyed on it drops to zero while Tal loads the next question (Maryam 9 Sep 2026: "this should not happen"); the answered count only grows, so the bar holds through the beat and still lights the live question. §63 owns the h3 type (`.t-h3`). Live chat only. (§107, ob.js, Maryam 9 Sep 2026)
- THE ONBOARDING QUESTIONS ARE THE CLIENT'S FIVE (`OB_Q`, Maryam 21 Sep 2026, supplied list) — SUPERSEDES the quiz-led six below: (1) **Where are you right now?** (In a job I want to move up in / In a job I don't plan to stay in / Running something of my own, or trying to start it / Between things); (2) **Which of these sounds most like you? Pick one.** (Top Performer / Athlete / Leader / Entrepreneur / Not Sure / Team Player); (3) **What do you do now?** — FREE TEXT (`t:'text'`, placeholder "Operations coordinator, student, own a detailing business"); (4) **What industry is that in?** — our DROPDOWN (`t:'dd'`, `dd()`; Security / Franchise ownership / Retail / Food and hospitality / Trades and construction / Healthcare / Transportation and logistics / Sales / Education / Technology / Student / Other) whose **Other** opens a text field; (5) **How many years have you been working?** (Under 1 / 1 to 3 / 3 to 7 / 7 to 15 / 15 or more). NO option DESCRIPTIONS — every option is `[key,label]` and the survey no longer draws `.ob-sv-od` (Maryam 21 Sep 2026: "remove the desc against each option as well"). The completion screen (`obDoneScreen`, "You're all set" + journey + "Find an Agent") is KEPT unchanged (Maryam: "just keep the last one where we tell the step they are on"). The two free-text fields (Q3 + Q4's Other) write into `S.ob` on each keystroke WITHOUT a render — a `[data-obtext]` input listener toggles the Next button's `disabled` in place off `obReady()` (the caret trap, trap 9/17); the dropdown re-renders on pick like a radio, its value read back from `S.ddVal['ob-'+k]`. `data-obdone` writes `PF.general.role` (Q3), `.industry` (Q4's label / Other text) and `.years` (Q5 label); the quiz-led `band`/`why`/`intent` and their readers are gone — `obAgent` falls back to the first recommended agent and `obMemo` (guarded on `S.ob.why`) returns nothing. hifi-only. (§107, ob.js, Maryam 21 Sep 2026)
- The onboarding QUESTIONS (`OB_Q`, 6 now): where → **industry** → **years** → band → why → **intent** (Maryam 9 Sep 2026: "What industry are you in? should come right after Where are you right now? and then the experience will come after the industry question"). `intent`'s heading is now "What do you want out of the next 90 days? This will help choose you your course category." (renamed from "Where are you trying to go?"). The old `want` question ("What do you want out of the next 90 days?") is REMOVED; its readers went with it — the `obMemo` row (ai2.js) and the read-back's "What you want" row now read `intent`, `OB_SPINE`/`obHeard` were reordered to match (each `st` is the question's step), and `obFit` quotes `obLabel('why')` by key rather than `obHeard()[2]` (the reorder moved `band` into that slot). `OB_N`/`obLast`/the "of N" count and the progress bar all derive from `OB_Q`. The onboarding welcome LEDE (`.ob-chat-lede`) is `--text-secondary` #828282 (a description, §63 §1). A new Tal turn UNFOLDS its height (`.ob-m:last-child .ob-m-t`, `ob-unfold` max-height) so a tall question card grows in rather than popping (Maryam 9 Sep 2026: "smoothly appear not a jerk"). (§107/§63, ob.js + ai2.js, Maryam 9 Sep 2026)
- The candidate PROFILE tabs are My Profile / **My Courses** / Notifications / Privacy Settings — "Become a Cohort Leader" is HIDDEN (Maryam 9 Sep 2026); `pfLead` stays defined and unreachable. **My Courses** (`pfCoursesView`, tab `courses`) lists the candidate's courses as `.acc.ol` accordion rows — collapsed to name + status (Completed / In progress / Not started), open to the four-cell `statCell` insight band the course pages draw (Chapters done / Assessment average / Time invested / Tasks on time). Each row leads with the course COVER on the left (`.crs-thumb`, 56×38, `courseArt` by the row's level — Maryam 9 Sep 2026). `MY_COURSES` is authored placeholder progress (flagged, §74 — no per-candidate course telemetry in the seed). The candidate NAME carries a `.rank-chip` "Rank #N" beside it (`GAME[stage].rank`, the real star-rank index) — a light-red pill (`color-mix` accent 12%) with `--accent-text` ink, `.t-label` size, only on stages that have a rank. (§105/§63, views.js, Maryam 9 Sep 2026)
- The COHORT LEADER dashboard carries a **"Certified Cohort Leader"** badge right after the call cards (`leadCertBanner`, lead.js) — the candidate's `.certban` completion banner reused (the shape ships in the DS), with no dismiss (a standing credential). The mark reuses `CERT_ART.explorer` as a placeholder (§74 — no Certified-Cohort-Leader asset in the seed; a real WebP is the file to drop in). "View" opens `leadProfile`. (lead.js, Maryam 9 Sep 2026)
- A `.rec-lab` "Tal recommends" label inks `--on-dark` on ANY `.dark-card`, not only `.rec-dark` (§63, Maryam 9 Sep 2026: "tal recommends text is missing with the star") — the enrolment card is a `.dark-card` and its label was setting `--text-primary` black-on-black, so only the sparkle showed. Both dark selectors are (0,5,0), beating the base (0,4,0).
- The Tal CHAT empty-state description (`.ask-page .tal-hero p`) follows the onboarding lede's size — `--t-sec-size` / 26 (§63 §5275, Maryam 9 Sep 2026, reversing the 4 Sep drop to `--t-desc-size`); the two greeting-plus-lede screens read at one size. And the chat header's frost is LIGHTER: `.ask-top` white .72→.55 / blur 12→8, its `::after` fade .72→.4 / blur 6 (§53, Maryam 9 Sep 2026: "lighten the tal row white blur"). (§63/§53, Maryam 9 Sep 2026)
- The Tal chat screen: the WASH is full-bleed (painted on `.main`, under the margins) but the CONTENT keeps its normal column — `.main`'s `clamp(--s06,12cqw,200px)` gutter stays, do NOT zero it (tried, reversed). There is NO divider under the `← Tal` header (§51.2's border off). The ground is now a TOP-AND-BOTTOM gradient, NOT a centred ellipse (Maryam 9 Sep 2026, Figma 892:7220, "follow the same values"): two `--accent`-at-5% bands over white — `linear-gradient(186.615deg, rgba(189,42,42,.05) 11.018%, rgba(255,255,255,.05) 52.274%)` at `top`, the same mirrored (angle `353.385deg`) at `bottom`, each `100% 590px` `no-repeat`. Bleeds down from the top edge and up from the bottom, white between. This REPLACES the 8 Sep centred rose ellipse (`radial-gradient(100% 40% at 50% 46%, rgba(245,75,135,…))`). The SAME two bands are the onboarding ground (§107) — one frame feeds both. Chat is candidate/leader only (§53); the token is `--askv-ground-wash`. (Maryam 9 Sep 2026)
- The Tal FLOATING FIELD (`.askdock > .askline`, on every page, all portals) follows Figma 875:6598 (Maryam 9 Sep 2026, "follow this ui completely"): a white 999px pill, a solid **1px `--rule` #efefef border** (Maryam 9 Sep 2026: the static edge is a plain hairline, not the old gradient border; a same-day #828282 was tried and reversed — the animating comet rides over it), padding 16/24. Left group (**12** apart): the 32px Tal orb, the label **"Ask Tal"** (14px Regular `--text-secondary`, NOT "Ask Tal anything"), then the example question (14px SemiBold `#626262`). Right group (**16** apart): mic then arrow, bare glyphs (no box) from our Material Symbols set at **22px** (a step over the node's 20 — "a little larger than current", which was 16/20). The ANIMATING LINE (`.ai-run` comet, `AI_RUN` in ai4.js + each hand-written portal) takes the node's gradient — `linear-gradient(90.13deg, rgba(255,255,255,.7) 0%, rgba(255,110,36,.7) 49.04%, rgba(255,55,51,.7) 75%, rgba(255,255,255,.7) 100%)` — as **four** SVG stops at 70% (white/`#ff6e24`/`#ff3733`/white; the 22.6% `#d551d7` magenta stop was DROPPED, Maryam 9 Sep 2026) on a HORIZONTAL gradient (`x2=1 y2=0`, replacing §70's diagonal). Same ramp on all three portals. (§63, §117, ai4.js + agent/admin, Maryam 9 Sep 2026)
- THE MIC IS SMALLER THAN THE SEND ARROW in the Tal bars (Maryam 21 Sep 2026: "reduce the icon size of the mic icon" — the floating bar AND the chat-screen field). The mic is **18px**, the send stays **22px**. Two classes in two layers: the floating dock's `.askdock .askline-mic svg` in §117 (kept at (0,3,1) to beat §105.548's 16px dock rule), and the chat field's `.ask-page .askfield-mic svg` in §118 (the chat field uses `.askfield-mic`, not `.askline-mic`). Crosses to the DS (agent portal's bar follows). (§117/§118, Maryam 21 Sep 2026)
- The Tal chat header has NO BLUR — it is a SOLID white bar (Maryam 21 Sep 2026: "remove the blur from the top header"). `.ask-top` stays `position:absolute` over the top of the page (`.ask-page` is `relative`) so the thread still scrolls behind it and `.ask-thread` still opens at `padding-top:92px`, but the band is now `var(--askv-column)` (#fff, opaque) with NO `backdrop-filter`; its `::after` keeps a plain white→transparent GRADIENT fade (no blur) so a line rising into the bar softens to white before the cut. This SUPERSEDES the 8 Sep frosted `rgba(255,255,255,.72)` + `blur(12px)` band and the 9 Sep lighten (.55 / blur 8) — the `backdrop-filter` read as a grey smudge over the top-right of the conversation. §53, candidate/leader (crosses to the DS). (Maryam 21 Sep 2026)
- The chat thread and rail draw NO SCROLLBAR — the platform is scrollbar-less (§12 hides `.main`'s, §02/§03/§09 the strips'), and `.ask-thread` (the chat scroller, §53) + `.sn-main` (the rail) were the two vertical scrollers §12 missed, so on a machine set to always-show scrollbars the chat had the OS grey slab down its right. §136 hides them exactly as §12 hides `.main` (`scrollbar-width:none` + `::-webkit-scrollbar{display:none}`); `.tal-body` (the floating panel thread) too. Scrolling still works (wheel/trackpad). NOT a slim custom bar — the rest of the app shows none. `.calw`'s deliberately-thin bar (§41) is left alone. All portals via the DS. (§136, Maryam 21 Sep 2026)
- A Tal chat response follows Figma 882:6701 (Maryam 9 Sep 2026, "apply the same look and feel to the other responses ... follow everything"). Its parts, down the content column, sit **20px** apart:
  - TITLE (`.tw-h`) is Medium (`--t-w-strong`) in `--text-primary`, sentence case, 13.5px — no longer the small grey uppercase eyebrow. The `twIc` mark is KEPT (§39.13's legibility argument stands; the reference simply did not draw it). (§63)
  - The STAT TABLE (`.tw-lines`) is a CARD again — `--surface-2` fill, 1px `--rule`, **radius 16** (a stated Tal-widget radius exception), padding 12/20, rows 12 apart with a `--rule` HAIRLINE between each. Label (`.tw-lines b`) is strong `--text-primary` in a 50px column; value is Regular `--text-secondary`. This REVERSES §39.1's "no inner frame" — the card returns, around the figures only (title and action stay on the bubble ground). (§39, §51, §63)
  - BULLETS (`.tw-list`) lead with a 12px red sparkle (`--ai-star` mask in `--accent`), 10px gap, `--text-secondary` text. (§39)
  - The ACTION button (`.tw-btn`, primary) is a LINK now: NO fill, NO border, NO pill — `--accent-text` text with a trailing `--accent-text` arrow (`I.arrowRight`, re-added to hifi's `twBtn`; agent/admin already had it), sentence case (`.tw-btn` dropped from §63's capitalize list). This REVERSES the 8 Sep red-fill pill. The ghost stays a quiet `--accent-text` label, no arrow. (§39, §63, views.js)
  - FOLLOW-UP CHIPS (`.tw-chips .chip-tal`) are the white rounded pill with a red star + red text — the greeting chips' shape (rounded to 999px here). (§39)
  - The LEVEL LADDER (`.tw-rungs`) is h40 segments in three states — passed `.done` black (`--gray-100`), current `.on` `--accent`, future `--rule` fill with muted text; `wLadder` marks the rungs before the current one `.done`. (§39, views.js)
  All four portals via the DS. (Maryam 9 Sep 2026, Figma 882:6701)
- The Cohort Leader has its OWN chat answers, not the candidate's: `lead5.js` (loads after ai8) wraps `talReply` with a `S.portal==='leader'`-gated `LEAD_ROUTES` table that intercepts before the candidate routes and answers the leader's questions with leader widgets (brief, attention list, cohort table, evaluations, draft check-ins), reading `LEAD_*` data. A leader question that matches no leader route falls through to the shared chain, which already owns the leader's REFUSALS (`cand()`→`leadNA`, off-topic, the support catch-all). Spec + coverage: `docs/tal-scenarios/02-cohort-leader-tal-scenarios.md` §4 (L-01…L-24). (Maryam 8 Sep 2026)
- The chat field's mic records a voice message in place (`.askfield.rec` + `.askrec`), built to the ChatGPT reference: a cancel disc, a LIVE waveform driven by the real mic amplitude (newest bar right, quiet past trailing left as dots; a smoothed synthetic meter when the mic is denied), a stop that transcribes into the field to edit, a send that transcribes and posts. Between press and words is a `.rectx` "Transcribing…" beat. Content is vertically centred in the pill. Over http/https the words are the reader's; where the mic or speech service is unreachable (e.g. `file://`), one of Tal's suggested questions stands in so stop/send always land a real, sendable message — never a silent no-op. The floating bar never records inline — its mic opens the chat and begins the take there. Candidate/leader only (ai4.js), not in the DS. (§121)
- Confirm every delete and log-out in a modal before acting. (memory)
- A confirmation is the design system's `.conf` centred-icon dialog (icon disc, title, description, button row — no header rule, no `.x`), NOT a plain `.sheet`; the disc icon names the action (logout → the logout glyph). The agent portal's logout reads "Are you sure you want to log out?" / "If you want to logout please confirm your action below". (Maryam 7 Sep 2026, agent portal)
- THE `.conf` MODAL IS ONE TREATMENT ON EVERY PORTAL, and it is the Super Admin's (Maryam 21 Sep 2026: "follow the same padding, same spacing/rows gaps, same icon ui of single solid bg and white icon on the other portals modals as well"). The disc mark is a SINGLE SOLID `--accent` circle with a WHITE (`--on-accent`) glyph — no light-tinted inner disc, no §105/§109 halo rings, no per-kind glyph hue (success/danger/neutral all read accent + white; the glyph carries the meaning). The frame is a 16px (`--s05`) outer padding on `.conf`, the body pays none (`.conf-b` padding 0, its flex column keeping the 12px `--s04` row gap between mark/title/fee/desc), and the button row sits 16px (`--s05`) below the text (`.conf-a` padding-top). This SUPERSEDES §105's tinted-red halo disc and §109's green success halo; it is stated once in `134-confmodal.css` (crosses to the DS) so candidate/leader/agent match the admin, whose own local copy (`tn-admin-portal.html`) is now the same values. (§134, all four portals, Maryam 21 Sep 2026)
- On the SUPER ADMIN portal the `.conf` disc icon is `--on-accent` WHITE on the accent circle, in every confirm modal (Maryam 15 Sep 2026: "the icon in the circle needs to be white in all modals" — reversing the 14 Sep black glyph); and the DESTRUCTIVE confirm button (`.btn-p.danger`) fills the brand `--accent`, not the DS's darker `--danger-ink` maroon (Maryam 15 Sep 2026: "the deactivate button color should be accent"). So a danger confirm reads as the accent circle + white glyph over an accent Deactivate button. Admin-local, in `tn-admin-portal.html`. (Maryam 15 Sep 2026)
- The agent Profile edits IN PLACE, never in a modal, following the candidate profile's exact UI: each editable section's Discard/Save live in its heading row as a `.pfe-acts` group — "Discard Changes" (danger + `I.delete`) and "Save Changes" (`.btn-p` + `I.check`) — and the read control is a PENCIL-only `.pfe-ic` icon. "General details" edits Name, read-only Email (`[readonly]`+`.pfe-ro`) and About (photo carries the `.idphoto-edit` pencil). "General details" also edits the **Interview fee** — an ordinary `.inp` prefilled with `money(v.fee)` in the same auto-fit grid as Name and Email (Maryam 7 Sep 2026); the read view's "Your fee" card and this field open at the same number, and `SHEETS.fee`'s note carries the new-fee/escrow consequence so it is not repeated here. Specialities is the candidate Skills section: `.skl-c.pfe-chip` pills with `.pfe-chip-x` remove and a trailing `.pfe-chip-add` "Add a speciality +" that opens an inline field — live over `S.spec`, Discard restoring the snapshot. `S.pfEdit` is the open section. A section whose in-place form has no `.skl`/`.facts` trigger carries `.sec-h-act` on its heading actions so §10.15 does not squeeze the heading into the label column. (Maryam 7 Sep 2026, agent portal)
- In the training black card the step names align to the stepper: the label row is the SAME equal-width flex as `.prog-seg` (`flex:1 1 0`, 2px gap), so each name shares its segment's left edge. (`readyBand`, Maryam 7 Sep 2026, agent portal)
- The training lesson list is part of "Your progress" (no separate "The four lessons" heading, no `tint` ground); a locked lesson's tag leads with the lock glyph and is a round bordered pill. (Maryam 7 Sep 2026, agent portal)
- The COHORT LEADER Messages page (`.msg-mod`) is FULL-BLEED: NO page side margins. `.main`'s `clamp(--s06,12cqw,200px)` centring gutter is zeroed while it holds the `.msg-mod` page, so the two-pane inbox (conversation list rail + thread) runs from the nav rail to the frame edge, not the ~900px reading column. Only the OUTER margin goes; the panes keep their `--pad-x` inner padding. hifi leader only (§36.msg). (Maryam 9 Sep 2026)
- A whole-page empty state drops the Tal summary band — pass `noSum` from a view whose body is only a `.sec-fill` (agent Earnings before a payout destination). A page that keeps a tab strip or other content is not empty and keeps its summary. (Maryam 7 Sep 2026)
- The RATING + REVIEW section (`reviewCard`, all three surfaces — course / cohort leader / talent agent) is a CENTRED WHITE BOX that hugs its content, with CENTRE-ALIGNED content: `.review` is a `max-width:700px` (`--rev-w`) `margin-inline:auto` panel on `--background` (WHITE — Maryam 14 Sep 2026, was grey `--surface-2` for two turns) inside a `--rule` #efefef hairline, `--s06` inner inset, radius 0; the wrapping `.sec` is plain (not `tint`) so it carries the section gap above and below. Floated (capsule mode) it also carries the soft `--rev-shadow` (a lightened three-layer lift). A gap sits between the heading and the stars when no sub-line is between them (`.rev-head + .rev-rate`). The note field has NO visible label (aria-label + placeholder only). A LIT star is the ACCENT (`--accent`) — a scoped exception to "the rating star is always gold" (§3): that governs the read-only `.stars` rating; this is the review widget's own interactive picker (`.rev-star.on`), and the global `.stars` still reads `--star`. The textarea's FOCUS edge is a thin black `1px var(--text-primary)` (not the field default 2px accent), scoped `.app textarea.rev-ta:focus`. On SUBMIT the "Thanks for your review" state shows, then clears itself after ~3.5s (`setTimeout` → `r.done` → `reviewCard` returns nothing, so the whole float disappears; ai3.js); that thanks card is sized to its content (`.rev-float .review-done{width:auto}`), not the review measure. Centring: `text-align:center` on `.review`, `justify-content:center` on the head/stars/actions rows; the textarea keeps text left and fills the box. Below 650px it is full width. The card carries the heading + `.rev-head` mark (16px), the star row and the note only — NO "Rate your experience" label, NO character counter, and NO sub-line on the course surface (the `sub` param is empty; the other two surfaces may still pass one). The course heading is "How would you rate this course?" (matching the capsule). Stars are 26px (thinner outline). Below 700px it is full width. This SUPERSEDES the earlier full-width grey band and the 13 Sep "out of box". On the Course Progress page (`V.transcript`) the rating is moved to the FOOT (after the chapter list), day90 only; the other two surfaces rate in place. (§130 + views.js, Maryam 14 Sep 2026)
- On the Course Progress foot the rating opens COLLAPSED as a CAPSULE (Maryam 14 Sep 2026, with a reference): `reviewCard` takes a `capsule` label and, while `!r.open` and unsent, draws a centred pill (`.rev-cap`, radius 999px, WHITE ground `--background` + `--rule` hairline + the soft `--rev-shadow`, the `chat` mark + the label in `t-body`) sitting above the Tal dock; text "How would you rate this course?". Clicking it (`data-revopen` → `r.open`) replaces the pill IN PLACE with the full card, which then carries a top-right close X (`.rev-x`, `data-revclose`) that collapses back to the pill. `r.open` is a pure toggle (score/note preserved). ALL THREE surfaces pass `capsule` now (Maryam 14 Sep 2026): course "How would you rate this course?"; cohort (day90) "How was your experience with {leader first name}?"; interview report "Rate your interview with {agent first name}". None passes a `sub`. Handlers in ai3.js. (§130 + views.js + ai3.js, Maryam 14 Sep 2026)
- The capsule (and its expanded card) FLOAT PINNED above the Tal dock (Maryam 14 Sep 2026: "the capsule should be fix above the tal floating field with some gap"). `reviewCard` renders `.rev-float` and ai4.js `placeReviewFloat` lifts it into `.view-col` (the dock's own container — a fixed pin would centre over the whole frame including the rail, so it must share the dock's containing block); §130 pins it `position:absolute; bottom:calc(--s05 + 66px + --s04)` (one dock-height + the dock's floor offset + a gap) with the dock's exact width/centring box. On a page with NO dock (the cohort page), `placeReviewFloat` stamps `.rev-float-nodock` and it drops to `bottom:--s06` (Maryam: "since we do not have tal chat here ... the capsule will come little down"). The cohort page summary also switches at day90 from the Thursday-call line to a course-wrapped line (`PAGESUM.cohort` is a function of the stage, ai6.js). The capsule<->modal swap ANIMATES (`revMorphIn` keyframe, `.rev-anim`): render() rebuilds the DOM so the two cannot tween, so the element that appears on the toggle render plays a one-shot fade+rise+scale entrance; `.rev-anim` is stamped only on that render via the `S.revMorph` one-render marker (ai3.js), never on unrelated re-renders. The capsule ICON is accent ink (`--accent-text` fill, §130) and SIZED TO THE TEXT (14px, was 20px); the LABEL is accent ink too (`.rev-cap-t` → `--accent-text` in §63, since §63 owns text colour). (§130 + §63 + ai4.js, Maryam 14 Sep 2026)
- Data over decorative charts: `.kv` rows and tables unless a proportion needs a chart. Admin-configurable numbers stay out of copy. (memory)
- A DASHBOARD is the "unless a proportion needs a chart" case: a split or a funnel reads faster as a shape. Two chart shapes, both admin (§120.1f) — `donut(segs)` for a 2–4 slice split (arc as a stroke-dasharray length, total in the hole, figures in the legend) and `hbars(rows)` for a ranked list or a funnel (`[label, valueText, barValue]`). Both draw from the `--dv-*` data-viz ramp — a green scale, NEVER the brand red (categorical data-viz never rides the brand colour) — and keep the figure in the legend / bar label, so the graph adds the shape without losing the number. Money lists (Revenue by line, Payments) stay `.kv`; wide stat rows and tables stay full-width. (§120.1f, Maryam 8 Sep 2026)
- A booking/interview card shows the interview FEE only (`v.fee`), never the agent's split (`v.share` / "of it yours") — the split is admin-configurable and belongs on the Earnings module, the one page whose subject is the agent's take. (Maryam 6 Sep, agent portal)

Data and copy
- Nothing is typed twice: read the record (`bkStamp`, `AGENTS[k].price`, `CH`, `PF`, `COHORT_LEAD`). (§76, `bkStamp`)
- No invented claims or data; authored placeholder copy is flagged in its source note (`CH_SYL`, `COURSE_NAME.E1`). (§74)
- A dead control on a live surface is worse than a missing one; a control that can never act is not drawn. (§60)
- Dates are `MM-DD-YYYY`; the course is `90 days`; the document is the `90-day summary`; a small count is spelt when it opens a sentence. (memory, ai6)

How to read an ask
- "Reuse X" = port X's declarations exactly and change only the dimension named; extras in the screenshot are the other surface's. (memory)
- A reference screenshot is structure inspiration, built in our components at the reference's proportions. A **third-party product** in a reference is embedded, never redrawn (Calendly). (§76)
- "This portal is a previous design" = port the current component generation wholesale (`/portal-sync`). (memory)
- Design arrives in corrections: act on the newest message, keep the old shape's argument in a note, do not ask which she meant. (memory)
- Artwork must be a file on disk; a pasted image cannot be embedded — name the folder and stems. (memory)
- **Responsive is part of done**: 390 / 744 / 1024 / 1280 in the same task. (memory 31 Aug)

Talent Agent — V1 model (client, 15 Sep 2026). Four rulings, each REVERSING an earlier one:
- **THE AGENT SENDS A RECOMMENDATION AGAIN — RECOMMENDATION, NOT RECORDING-ONLY (Maryam, 18 Sep 2026, reversing the 15 Sep client "recording only").** On the interview screen (`V.interview`, `st:'report'`, `tn-agent-portal.html`) the agent RECOMMENDS a level across the FIFTEEN rungs (E1–T5), with **strengths**, **growth areas** and an **overall read** ("what you make of {first}"). Tal's suggested level is marked on a read-only LEVEL BAR (the DS `.ladder` markup, rung computed off the code so B/T mark correctly — the DS `dsRungOf` only maps E1–E5), and the picker + the three fields PREFILL from Tal's read (`talLevel` / `seedStrength` / `seedGrowth` / `seedWhy`) so the agent edits rather than starts blank. The level picker is the shared `dd()` dropdown (`LVL_OPTS` = the 15 codes as "Explorer – E1"…"Trailblazer – T5"; store `S.slotForm.evalLevel`, no new handler). The page also carries an **attached interview-recording THUMBNAIL** (`.iv-rec`, a 16:9 card reusing §111's `.scv-play`/`.scv-at` marks; geometry-only CSS in §124, `recStill(x)` = the candidate's own photo as the frame stand-in until a real captured frame is dropped in — a pasted image can't be embedded, so the file goes on disk). The CTA is **"Send Recommendation"** (`data-confirm="sendrec"`); Tal's read stays but is reworded to "weigh it against the call and set your own recommendation below" (no longer "not a decision you make here"). **TalentNext still CONFIRMS the final level after analysis** — the agent recommends, TalentNext confirms — so the candidate-side "TalentNext sets the level" strings are untouched. This REVERSES the ruling below for this screen; the level picker / strengths / growth / overall are BACK on the agent portal. (Maryam, 18 Sep 2026)
  - **The agent interview screen follows the candidate portal's block structure, translated to LIGHT MODE (Maryam, 21 Sep 2026).** (1) "Tal's read" is a SECTION HEADING (`.sec-h > h2`) like "Your recommendation" below it — not the blob + `.ai-label.bare` mark — and its read is ONE flowing paragraph, not separate lines (`talEval` a single `<p>`; `.tal-eval` kept only so `.tal-eval .ai-body p b` still highlights the accent phrases). (2) The recommendation LADDER (`.iv-ladder`) gets §59.2b's light-ground treatment restated for it (`59-priority.css`, crosses to the DS): a `--layer-accent-01` track for rungs ahead, solid `--accent` for rungs passed, a light accent tint for the suggested one, and grid-centred UPRIGHT tabular codes — the DS default (`--on-dark-fill` track, the `<i>{font-style:italic}` trap) is drawn for the dark level card and left the future rungs invisible and the codes slanted on the white section. (3) The interview-recording THUMBNAIL is a real captured frame of the video call (`IV_REC_STILL`, a 16:9 WebP supplied by Maryam, embedded as a data URI; source at `assets/agent/iv-recording-still.webp`), so `recStill(x)` returns it for every recording preview instead of the candidate-photo stand-in. (tn-agent-portal.html + §59, Maryam 21 Sep 2026) (4) The **"Interview recording" and "Tal's read" sections STACK** — heading above, content full width (Maryam 22 Sep 2026): §10.15's label column keys its opt-out on the content class and `.iv-rec`/`.tal-eval` are not on that list, so an agent-portal-local `<style>` block restates the DS opt-out (`display:block` + the `> .sec-h` reset) for `.sec:has(> .iv-rec)` / `.sec:has(> .tal-eval)` inside the ≥900 tier — they now read like "The interview" (`.stats`) and "Your recommendation" (`.eval-form`), and the Tal's-read paragraph runs full width. (5) NO divider between "The interview" and "Interview recording" — that section carries `.sec-noline` (Maryam 22 Sep 2026). (tn-agent-portal.html, Maryam 22 Sep 2026) (6) **The T·A·L·E·N·T TYPE** (Maryam 22 Sep 2026) — six lenses spelling TALENT: **Top Performer · Athlete · Leader · Entrepreneur · Not Sure · Team Player**, each with Maryam's supplied meaning (`TALENT_TYPES`, kept verbatim, no em dashes). Tal names which one the candidate falls in on the read ("Tal reads {first} as a **{type}**. {meaning}", the type in Tal's accent ramp; `talTypeOf(x)` = the record's `talType` else a stable per-id pick, §74 — Sofia is `talType:'Leader'`), and **the agent picks their own on the recommendation** — a "TALENT type" `dd('evalType', TALENT_OPTS, curType, {desc: talentDesc})` under the level picker, prefilled to Tal's read, its store `S.slotForm.evalType` (reuses the `dd()` handler, no new one). Refinements 22 Sep 2026: Tal's read shows only "Tal reads {first} as a **{type}**." (the meaning is NOT repeated there); the `dd()` gained a `cfg.desc` option that stacks each option's **description under its label** in the menu (`.dd-opt-2`/`.dd-opt-main`/`.dd-opt-d`, agent-local CSS), so the meanings live in the dropdown — there is NO separate meaning line under the picker. (tn-agent-portal.html, Maryam 22 Sep 2026) (7) **The level ladder IS the picker** (Maryam 22 Sep 2026): the "Your suggested level" label sits ABOVE the blocks and the fifteen rungs are **clickable** (`ivLadder(curCode, true)` — each `<i>` carries `data-ddset="evalLevel:<name>"`, caught by the shared `dd` handler; `.ladder-pick i` gets cursor/hover/focus), marking the agent's own pick (`curCode` off `curLvl`), not Tal's. The old `dd('evalLevel', …)` dropdown is REMOVED. Unreached rungs are `--rule` #efefef, not `--layer-accent-01` (Maryam 22 Sep 2026). (8) The recommendation form **breathes** — `.eval-form > .f` fields are 20px apart with a label-to-control gap (Maryam 22 Sep 2026). (9) Tal's-read paragraph runs **full width** — the DS caps `.ai-body`/`.ai-body p` at 68ch and lifts only `.ai-aura`; an agent-local rule lifts `.tal-eval` too. (10) **In-place renders keep the page's scroll** — `render()` captures `.main` scrollTop and restores it (with `scroll-behavior:auto` so it is instant, no jump-to-top-and-back — `.main` is `scroll-behavior:smooth`); real navigation still resets via the `data-go` handler's `scrollTop=0` after render (Maryam 22 Sep 2026). (tn-agent-portal.html, Maryam 22 Sep 2026)
- ~~**THE AGENT DOES NOT SET THE LEVEL.**~~ **SUPERSEDED for the agent screen 18 Sep 2026 (above): the agent recommends a level + strengths/growth/overall again; TalentNext still confirms the final level.** (Original 15 Sep ruling, kept for its argument:) The agent's job is to show up, run the interview well, and deliver a clean recording. Tal PROPOSES the read; the final level is set admin/moderator-side after analysis (the Super Admin portal Maryam is building). On the agent portal the level picker, "Send Evaluation", and the Why-this-level / Strengths / Growth-areas decision fields are GONE; Tal's read shows READ-ONLY, framed "proposed — TalentNext sets the level after analysis". The agent's post-interview surface is: confirm the recording is clean, plus an optional free-text notes field that feeds the admin decision (never a level, never strengths/growth-as-decision). This REVERSES §3's "the talent agent's call on a candidate's level is FINAL" ruling (line ~181) — that ruling and the "Levelling module stays hidden" clause are superseded; the admin gains the level-setting surface. (client, 15 Sep 2026)
- **PAYOUT RELEASES ON A CONFIRMED CLEAN RECORDING, not on a level report.** With the agent no longer filing an evaluation, the escrow-release trigger is the interview being held + the recording confirmed clean; the three-working-day clock starts at interview end. Across Earnings / Dashboard / Standing the "Report due / blocks your payout" state becomes "recording to confirm" (then Releases). This REVERSES the "the report is what releases the money" rule. (client, 15 Sep 2026)
- **NO ACCEPT/DECLINE — A PAID BOOKING IS A COMMITMENT.** A candidate books an open slot the agent published and pays at that moment; there is no request for the agent to accept or decline. The agent controls supply by which slots they publish (Availability), not by vetting who books. The release valve is CANCEL-WITH-A-REASON (reason recorded, within-24h counted as a late cancellation and seen at review, candidate refunded and returned to browse) — story 13.5. Remove the "New Requests" tab and the accept/decline step; diary tabs are Upcoming / To confirm / Past. This REVERSES the 4 Sep "New Requests" tab. (client, 15 Sep 2026)
- **NO IN-PORTAL TRAINING — HIRING IS OFFLINE FOR V1.** Agent hiring, onboarding and training run through LightSpeed offline; the platform just creates the user with the agent role. Remove `V.training`, the four lessons, the calibration, the certification GATE/journey, and the Training rail item. Certification survives only as a READ-ONLY status on the profile (13.9: set outside the platform). The `setup` stage stays (profile / fee / payout gate, Not listed until done); the `training` stage is deleted. This REVERSES the training/enablement journey. (client, 15 Sep 2026)
- **THE PAYOUT DESTINATION IS ONE STRIPE REFERENCE, NOT A CARD WALLET (13.1).** Earnings shows a single destination as Stripe describes it (bank + last four) with its STATE and, when verified, the date; `S.payout.state` is one of none / pending / verified / failed (`DEST_STATE` maps each to a `.flag-t` hue + icon; `failed` shows Stripe's own reason via `payoutDest`). The action is **"Update payout destination"** (the shared Stripe modal); changing it returns the state to Pending while the agent stays listed and bookable. The old `S.payCards` card-list is gone (data kept, unused). (client, 15 Sep 2026, 13.1)
- **LISTING = DESTINATION + CERTIFICATION + PROFILE (13.1/13.9).** `listingLine(v)` states whether the agent is "Listed and bookable" or "Not listed", naming the missing piece (a verified payout destination, certification, or a written profile) — surfaced on Earnings under the destination and in the gate. Either missing is Not listed. (client, 15 Sep 2026)
- **THE LEDGER CARRIES SIX SETTLEMENT STATES (13.7):** In escrow, Recording to confirm (the one ON the agent), Releases [date], Paid [date], Returned, On hold (an admin hold, no reason shown to the agent). A **"What's holding up your pay"** panel lists every held interview whose recording is unconfirmed with a route to confirm. (client, 15 Sep 2026, 13.7)
- **THE FEE SHOWS ITS NET LIVE, AND A CHANGE CONFIRMS (13.9).** The Interview fee field carries a live "You keep $X of $Y. TalentNext keeps 20%." line (`feeNetText`/`feeNet`, inline `oninput`, no render so the caret holds). Saving the General form with a changed fee routes through the **`feechange` confirmation** ("interviews already in escrow keep the rate they were booked at; the new fee applies only to bookings made after this") whose OK carries `data-feedone` to clear both the confirm and the edit. (client, 15 Sep 2026, 13.9)

Candidate portal — story-gap builds (Maryam, 15 Sep 2026, from the candidate user-stories gap analysis):
- **There is a `held` stage between `booked` and `assessed`** — interview held, report pending (story 17.1). Leaving the level interview call (`ai10 callLeave`, `kind==='iv'` from `booked`) advances to it, not back to the booked page. It carries `held:true`, `nav:'early'`, no level; the dashboard, Interviews and My Level all say the report is on its way and there is NO route to the agent. Needs `PAGESUM.dashboard.held`, `NOTIF.held`, a `journey()` case (interview step still `on`). Scoped to the first interview; the re-interview keeps its path to `promoted`. (data.js/ai10.js/ai6.js/views.js)
- **Cancel an interview with a reason** (17.4/16.4) — a "Cancel interview" ghost under the booked scheduled card opens `ivCancelModal()`: a `.conf` dialog with our `dd()` reason dropdown (Something came up / different agent / not ready / Other, note on Other), then confirm drops the booking and `setStage('new')` (back to the marketplace). Reason in `S.ddVal.ivcancel`, note in `S.ivCancelNote`, open flag `S.ivCancel`; `.dd-open` on the body so the menu is not clipped (§123.4). (views.js)
- **A receipt is a MODAL, never a route** (22.1) — the Payments row's "Receipt" carries `data-receipt="<i>"`; `receiptModal()` draws that row (payee/item, date, card, amount, Paid) over the page. `payRows(f)` is the one ledger source both the table and the modal read. A complimentary interview shows an empty card cell and amount "Complimentary". (views.js)
- **The legal notices are one shared `legalDoc()`** with four working tabs (Terms/Privacy/Data use/Cookies, `S.legalTab`, `data-legaltab`), drawn on the pre-auth `AUTH.terms` screen AND — for a signed-in candidate — in the **Privacy Settings tab** (`pfPrivacy`), which is where they live for signed-in users. The Data use notice promises ONLY controls that exist: the removed Profile controls (pause Tal, level review, download everything, self-serve delete-a-recording) are gone from the notice too, and the agent's level is stated as final. (views.js)
- **"What to talk about" is captured at booking** (16.3) — a textarea on `V.agent` (`data-ivtopic` → `S.ivTopic`, persisted by the input listener), echoed on the scheduled and held cards via `ivTopicBlock()`. (views.js)
- **`howItWorks()` is one helper on BOTH interview surfaces** (16.2) — the four-figure/four-step `.found` disclosure (`key='how'`) is drawn on `V.interviews` and `V.agents`. (views.js)
- **Notification settings state each toggle's timing plus an "Always on" block** (22.4) — under the three email toggles, a read-only list of the service messages that cannot be switched off (payments, level, booking, cohort) with the reason. (views.js `pfNotif`)
- **First interview is COMPLIMENTARY via a config default, not a hardcoded free branch** (16.3, client 15 Sep 2026: "keep the first interview complimentary for now ... charge on the re-interview ... keep the ability to charge for the first interview later"). `IV_FIRST_FREE` (default true) + `ivCharged(isRe)` / `ivFeeLabel(isRe, price)` in data.js; `isRe = !!cfg(S.stage).reinterview`. `talRec`, `V.agent`, `V.checkout` and `V.billing` all read it — first interview shows "Complimentary" and asks for no card; the re-interview charges the agent's fee. Flip the flag to charge first interviews again with no other edit. CONFIRMED by Maryam 15 Sep 2026 (this supersedes the earlier "no interview is free"): client feedback is the governing input over the user stories and the portal's current flow. (data.js/views.js)
- **THE CANDIDATE PORTAL CREDITS TALENTNEXT, NOT THE AGENT, WITH SETTING THE LEVEL** (client Talent-Agent V1 ruling, applied candidate-side; Maryam 15 Sep 2026: client feedback governs over the user stories, which say the agent sets and signs it). The agent INTERVIEWS the candidate; **TalentNext sets the level from that interview, after analysing it, and the decision is final** (no review, no appeal). Every candidate-facing level-attribution string is reframed off the agent: the Data use notice (short version + clause 3) and My Level's "Who decides"; the provenance line ("Confirmed 21 August, set by TalentNext" — was "signed by Priya Nair") in `lvlWing`; the journey rows ("set by TalentNext"); the `held`/assessed dashboards + `PAGESUM`; `signedSummary`'s header ("Interviewed by Priya Nair" — was "Assessed and signed by"); `howItWorks` step 3; the `SUMDROP` level lead and the `MEMO` growth-areas source. Priya stays named as the INTERVIEWER (booking, the call, "your interview with Priya"); only who SETS/SIGNS the level changed. Course-completion CERTIFICATES (LightSpeed-issued) keep their own signature and are out of this scope. (views.js/ai6.js/ai2.js)

## 4. Component recipe index — before inventing a class, open the gallery

`design-system/gallery.html` renders every component with its markup. The CSS is half of each;
the other half is below.

| Component | Gallery | The other half |
|---|---|---|
| Head band, Tal summary, journey `.jrn` | `#signature` | `placeBand` / `placePageSummary` in `hifi/`; a hand page emits `.modhead` itself; copy is `PAGESUM` |
| Typing summary | `#signature` (replay) | `dsTypeSummary(p, key)` in `talentnext-ds.js` |
| `aiHead` | `#signature` | `aiHead({mark, title, desc, act, extra})` — `.aih-mk` is NOT `.ai-label` |
| Black card `.dark-card` (+ `.crow-dark`, `.rec-dark`) | `#signature` | `.dc-hd > .dc-hd-r > .dc-t` + ONE of `.dc-act` / `.dc-when`; §63 §6a inks by token |
| Call row `crow(kind, o)` | `#rows` | record fields `img`/`cover`/`v`/`xl`; `o.gate`; `data-call`; `joinLive` |
| `.stat` figure cell, `.qa-c` quick action | `#figures` | `.stats` takes exactly four; `--mk` inline per cell |
| `.stps` steps row / `.stp-all` popup | `#signature` | `.stp-all` INSIDE `.stp-top`; words in `.stp-t-l`; `stepIcon` table copied per portal |
| Disclosure `.found` | `#signature` | `foundHead(title, key)`; `S.disc[key]` + DOM class |
| Accordion `.acc`, course outline | `#rows` | `S.outl`; the `data-outl` branch runs BEFORE the generic `.acc-h` one |
| `.plate` + quiet state | `#signature` | `data-when` attribute; `dsPlateQuiet(el)`; `.plate-h`/`.plate-when` emitted by hand |
| `.ring` | `#figures` | two SVG circles; `--arc` is a dasharray length `163.36 × pct/100` |
| Agents table `.agt` | `#tables` | rows are `subgrid`; `--agt-act-w` reserves the hover |
| Confirmation dialog `.conf` | `#signature` | `.modal > .sheet.conf`; `.conf-ok` for success |
| Account menu `.acct-t` | `#nav` | `acctMenu()`; `data-swap` (repaint) vs `data-doc`/`data-portal` (load) |
| `.row-cta`, `.rec-tag`, `.tbl-flag` chip | `#rows`, `#chips` | `<span>` never `<button>`; hue written inline as `--mk` |
| Cohort cover `.gcard-art` | `#rows` | `gcard(..., {src, i})`; label is the `onerror` fallback |
| Interview scenes carousel `.scv-row` / `.scv` | `#scenes` | **`dsScenes(title, scenes, opts)`** in the DS — each scene `{img, i, name, at, title}`, `opts.role` (attribution label) + `opts.foot`; chevrons `.scv-nav > .scv-ch[data-scv]` scroll the row via **`dsScvScroll(this)`** (pure DOM, no render); card = `.scv-art` (img + `.scv-play` + `.scv-at`) → `.scv-b > .scv-h` → `.eo-lead` attribution. Candidate's `pfScenes()` is the caller. Layer §111 (in the DS) |
| Rail tree `.sn-subs` / `.sn-sub` | `#nav` | emitted only while `S.nav`; module row keeps the bar, sub-row keeps the accent ink |
| Sortable table `th[data-sort]`, action cell `td.tbl-act` | `#tables` | `table(cols, rows)` in the admin; `S.sort[page]` asc → desc → off; buttons sit nearer than `tr.clk` |
| Row action menu `.rowmenu` / `.rowmenu-list` (admin) | — | `rowActs(acts)` emits one kebab + a menu of `[icon, label, attr]` rows; `S.rowMenu` the open id; `placeRowMenu` lifts it into `.main` and positions it; the "Actions" column is `ACT_COL`; layer §125 (in the DS) |
| Row-select checkbox `.tbl-cb` (admin) | — | `selBtn(attr, on, ind, label)` — a `role="checkbox"` button, NOT a native input; `table()` prepends a `.tbl-sel` header + per-row cell; `S.sel[key]` holds the picks, `TBL_ROWKEYS[key]` the ordered keys the select-all reads; `data-sel`/`data-selall` matched before the row's `data-go`; CSS `#admin-tbl-sel`, admin-local |
| Bulk action bar `.actbar` (admin) | — | `bulkBar()` replaces `askDock()` while a `selectKind` table has a selection; `BULK[kind].single(id)`/`.multi(ids)` give the buttons; `bulkCtx()` finds the active table via `SEL_TABLES`; `bulkstatus`/`bulkdel` handlers confirm then apply to each; `data-selclearall` clears; content-width pill in `.askdock` with the dock shadow, admin-local |
| Table pagination `.pgn` (admin) | — | `table()` slices to `S.pg[key]` (page + size, key = `pageKey()#TBL_SEQ`); `pagination()` draws the bar; `data-pg`/`data-pgsize`; controls are `.btn`; layer §127 (in the DS) |
| Filter strip `.cs.lst-filt` under `.hd-srch` | `#tables` | counts in `.lf-n`; `filterStrip(name, def, opts)` |
| Record engine (admin) | — | `KINDS[kind]` = fields + create/update/remove; `rec:<kind>:<id>` sheet, `save:` / `askdel:` / `askstatus:` actions |
| Interview request card `.rqc` (agent) | — | `reqRow`; three zones (who / when+quote / decision) in a `.rqc-list`; desktop grid split by `--rule`, stacks below 900; the quote wears the Tal star + `--accent-text` ink (§63 §7), Decline is `.danger` red (border off), Accept is `.btn-p.ok` green with a tick; `.rqc-list` is a §10.15 label-column opt-out; layer §122 (in the DS) |
| Custom dropdown `.dd` / time range `.tm-row` (agent) | — | `dd(key, opts, val, {ic})`; `S.dd` is the open key, `S.slotForm` the values; `data-ddtoggle` / `data-ddset` (split on FIRST `:`), outside-click closes; layer §123 (in the DS) |
| Stripe "Add Payment Method" modal `.pay-modal` | — | `payForm(tab, open)` (views.js) → `dsPayForm` in the DS; embeds `PAY_ART` (Card/US-bank); `data-paytab` toggles, `data-payclose`+backdrop close; per-portal open flag |
| Saved-cards picker `cardPicker()` | — | `S.cards`+`BMK`, default via `payWithIdx()`, `data-paypick`→`S.payWith`, "Add a card"→the modal; on `V.payment` and `V.checkout` |
| Rulings Register (admin) | — | `SA_CASES` + `DECISIONS` (`sa`, `kind`, `blocks`); `decisionTile`, `homeRulings`, `SHEETS.sacase`; `saTag`. `ruling()` is a no-op — module pages no longer show inline notes (7 Sep) |
| View-as bar `.viewas` (admin) | — | `viewAsBar()` in `.view-col` before `.main`; `S.viewAs`; `.viewas-mk` 20px glyph, `.viewas-t` line (§63 §57), `.viewas-end`; layer §129 (in the DS) |
| Step-up confirmation | — | `askConfirm({stepup:true})` adds a `[data-f="code"]` field to `.conf`; the router refuses `data-do` without six digits while `SEC.stepup` |
| Sign-in scene (admin) | — | `authView()` → `.app > .auth-card > (brand, .auth-col > .shell + .main.form-page)`; `dsPh`, `.auth-id` panel; `S.auth` login → code |
| Voice recorder `.askfield.rec` / `.askrec` | — | ai4.js `askRecStart`; waveform from getUserMedia+AnalyserNode (synthetic when denied), transcript from Web Speech; `.rectx` transcribing; empty take → `askRecFallback()` suggested question; stop → `#askIn`, send → `ask()`, cancel discards; NOT_IN_DS |

Three that bite: `.ai-label` needs `.bare` outside the JS-assembled band; `.plate` takes
`data-when` as an attribute; a new page needs a host with `container-type:inline-size;
container-name:app` or no breakpoint fires.

## 5. Copy and voice

- A page head has two slots: `ph(title, sub)` is the factual spine as a `·` row (or `title` alone), `PAGESUM` is Tal's reading — two sentences, 18–28 words, real figures, `<b>` on the actionable phrase.
- The page-summary head band (`.ai-aura.talsum`) is labelled **"Summary by Tal"**, not "Tal" — platform-wide, all four portals. Only the head-of-page summary; every other Tal card (chat author, offer/advice cards, `V.interview`'s "Tal's draft") keeps the bare name "Tal". In `hifi/` `placePageSummary` sets it (`ai6.js`); the hand-written agent/admin portals write it into their `head()`/`detailHead()` `.talsum` band directly. (Maryam 6 Sep 2026)
- NO EM DASHES in anything Tal says — summaries, chat answers, drafts, all four portals: they read AI-generated. Use a period (two shorter sentences), a comma, or a colon instead; put the rule in the copy, not a global replace. En dashes ("Explorer – E3"), empty-value placeholder dashes and non-Tal UI labels are untouched. (Maryam 9 Sep 2026)
- Four bans in a summary: no framing, no policy, no pointing at the UI, nothing from the other portal.
- Tal answers six subjects; anything else gets one of three refusals; Tal never reads the billing ledger. (`ai8.js`)
- Chip text and skills are sentence case; titles are Title Case; buttons capitalize by CSS.

## 6. Change protocol

0. **First decide where the change lives**: `python3 design-system/where.py <class>` says which
   layer states it, whether the box ships it and which portals write it. Shipped or written by
   more than one portal = a design-system change: the layer, both builds, verify on every portal
   named. A screenshot of one portal is almost never a one-file change.
1. Change the layer in `hifi/build/` that states the rule — never `talentnext-ds.css`, never the built portal HTML.
2. A NEW layer goes in `build.py` AND `build-ds.py`'s `LAYERS` (with a note), or `NOT_IN_DS` with a reason.
3. `cd hifi/build && python3 build.py` then `cd design-system && python3 build-ds.py`; read `layer coverage: N of M`.
4. `cd hifi && node respcheck.mjs --quick`; the agent portal follows by the rebuild; the admin portal is by hand.
5. Write `:hover` plain; arm it in `build.py`'s `HOVER_KEEP` by a narrow class.

## 7. Verification protocol

Sweep at 390 / 744 / 1024 / 1280 (`--edge` adds 899 / 900 / 940). Look for: horizontal overflow,
the label column live below 900, a heading past three lines in it, content escaping its section,
a button label on two lines, a divider still drawn after the columns stacked, `console.warn`.
Measure with the Browser pane OPEN (`window.innerWidth === 0` means nothing is measurable), with
motion off, using `offsetWidth`. `/design-check <portal>` runs the computed rule sweep.

## 8. Flagged, not fixed

the staff `DEPARTMENTS` list (Operations / Talent Success / Trust & Safety / Finance / Engineering) is an AUTHORED PLACEHOLDER — the user-stories doc flags the real department list as pending from the client (Epic 1, 1.1) · E1 course name is a placeholder · the E1/E2 course ratings (`COURSE_RATE_PH`, the all-courses list) are authored placeholders — the seed rates only E3/E4 · `CH_SYL` is authored copy · the admin sign-in intro (`AUTH_ART_ADMIN`) is authored copy · the journey document names owners Umair Bashir / Derek Branch and the seed names Umair Khan / Derek Sole — first names only in the portal · the dashboard's figures are read off the seed and are not signed-off KPIs (SA-03 E1) · the Talent Agent Standing module's "This quarter" funnel counts and the eight-week "Profile views" trend (`STAND_TREND`) are AUTHORED PLACEHOLDERS — the seed has no view/request telemetry, so the shape is real (Upwork "My stats" adapted) and the numbers stand in; the Agent Score gauge and its three inputs are the real `v.follow`/`v.depth`/`v.rating` · the Calendly URL 404s · `V.welcome`
is reached by nothing · data-use clause 6 names switches Profile no longer holds · `reddemo`
previews the previous accent, and carries TWO trials on that one stage: §67/§83 (the red
colour) and §131 (`131-tmpui.css` + a swap in `tmpaccent.js`, 14 Sep 2026 — a UI trial:
a 2/3/4px `--radius-sm/md/lg` ladder on buttons/cards/modals; a flat #fcfcfc ground where the
header, side nav and content sheet are painted #fcfcfc and every SECTION paints no ground of its
own (transparent, so only CARDS are white — the `.prog-ic` figure chips drop to bare black
glyphs, the filled task check becomes a lined `I.check`, the "Day 34" read-out goes black); the
two content-window rails removed and every section divider inset to `--pad-x`; and the head-band
summary made a discrete white card — `--rule` border, `--radius-md`, detached from the header,
its foot divider dropped, its column divider a `--rule` hairline inset top/bottom, and its wash a
top-left corner glow (this stage's `--accent` at 5%, Figma 578:6047's hue/strength, radial so it
stays on the summary side and the progress wing stays clean white) in place of §67's radial).
Both are `.tmp-accent`-scoped, out of the design system, and deletable independently; §131's head
has the promote/drop note · `--ai-amber`, `.cert-btn` read by nothing · `PF.general`'s
`location` / `phone` / `tz` are retained but no longer surfaced (the General edit form dropped them, 7 Sep) · `tn-portals.html` and
the frozen `tn-admin.html` are not on the design system (`tn-admin-portal.html`, the hi-fi Super
Admin portal, is).
